---
tipo: registro-de-acompanhamento
autoridade: nenhuma
normativo: nao
atualizavel_sem_rito: sim
criado_em: 2026-08-01
---

# ROADMAP CANÔNICO — LucaX Enterprise OS

> Instrumento de trabalho, não norma. Atualizar marcando `[x]` conforme cada
> item fecha. Não exige ADR, hash, baseline nem ato. Se algum dia precisar de
> rito para ser atualizado, foi posto no lugar errado.

**Legenda:** `[ ]` pendente · `[x]` feito · `[~]` em curso · `[!]` espera decisão do Fundador

> **A regra de manutenção deste arquivo vive em [`CLAUDE.md`](../CLAUDE.md), na
> raiz.** Conferir e assinalar **na mesma sessão que fecha** a missão; fechar
> missão sem conferir o roadmap é entrega incompleta.

> **🧹 Conferência de 2026-08-12 (segunda do dia), ao fechar a MANUTENÇÃO sob token 40 — na mesma sessão.**
> **Nenhum Goal muda de estado.** A missão, aprovada pelo Fundador, fechou num ciclo só: **§10.0
> ROTACIONADO** *(cinco emissões herdavam `BL-2026-08-02-03` no rótulo "vigente"; agora §10.0 aponta
> `BL-2026-08-10-01` e o bloco antigo vive em §10.0.0, valores intactos)* · **`RD-80` FECHADO** *(este
> arquivo entrou no catálogo, §4.6 — a única ausência histórica acabou)* · **`MSG-2026-0009`
> meio-fechada** *(correção declarada no catálogo: caminho do nxtrack no lucaX MORTO e repositório
> próprio EXISTE em `E:\LucasIA\Projetos
xtrack`, medidos hoje; a emenda da Carta de Produto é rito
> e fica para o ato da migração; preservar a âncora `tree b9b36be9…fb4b` vira exigência do desmonte
> do lucaX, que **vai deixar de existir** por decisão do Fundador)* · **quarta cegueira**: gatilho NÃO
> disparou, segue com o Agente 7, caso conferido vivo em `_missao-1-14-1-2026-08-05/` · **colisão de
> número RESOLVIDA por convenção**: faixa **`M-`** para missão que não pertence a Goal nenhum —
> **adotada por exercício**, não por decreto: `M-01`, `M-02` e `M-03` já usados em artefatos; Goal
> `1.14` continua com o nome que tem. Em `docs/`: entrada 2 da NUNCA-APAGAR fecha a condição *(o
> `basckup antigo` foi ACHADO em `H:\` na principal, 2.594 MB batida exata, e ganhou SEGUNDA CÓPIA
> verificada `468/468`)*; caminhos da purga S1.1 emendados *(mudança de sede `b14a0da` da Oficina)*;
> 2b/2c do sensor do lucaX **cancelados** *(o conceito virou o teto inteligente E4.T no Corpo)*.
> `0` atos · `0` ADR/RFC/FIT · `0` bytes no medidor · nenhuma baseline emitida *(delta declarado no
> lease)*.

> **🔄 Conferência de 2026-08-12, ao fechar a Onda 3 — na mesma sessão que a fechou.**
> **Nenhum item muda de estado.** A sessão executou a **Onda 3 do plano de 8 ondas do Corpo** *(plano que vive em
> `lucax-enterprise/CLAUDE.md`, fora deste acervo)*: rito C2 **inteiro** — **`RFC-0033` → `ADR-0038` → `FIT-2026-031`**
> *(`apto-com-ressalva`)* — sob `fencing_token` **38**: **a Mente reconhece o Corpo**, as 4 camadas com caminhos reais, a
> membrana `M1–M5` e a Policy Engine como projeção de `FND-04`. O rito inteiro foi **escolha expressa do Fundador** entre três
> opções apresentadas com a regra exata (`FND-04` linha `C2` + `CV-07`); a dispensa de RFC estava disponível e **não foi usada**.
> ⚠️ **Os TRÊS contadores de sequência estavam defasados** *(família `RD-32`/`RD-95`, quinta ocorrência)* e foram corrigidos pelo
> `V1` contra a cópia datada. **Nenhuma baseline emitida** *(precedente do token 30)*; o delta fica declarado na liberação do
> token 38. **Assinalado: nada** — `Corpo`, `lucax-enterprise` e `Onda ` têm **`0`** ocorrências neste arquivo, medido antes de
> escrever esta nota; os quatro `[!]` (Goals `1.14`, `1.16`, `1.17`, `1.18`) seguem **intocados**. `0` atos · `0` Fundacionais ·
> `0` bytes no medidor · `0` bytes em `_SAIDA-COMPANY-OS/`.

> **🔄 Conferência de 2026-08-10, ao emitir `BL-2026-08-10-01` — na mesma sessão que a emitiu.**
> **Nenhum item muda de estado, e a sessão não era missão.** O Fundador pediu **sincronizar três
> fontes divergentes** — a máquina principal, a **estação espelho encerrada em 2026-08-09** e o
> GitHub —, e a sincronização produziu uma **emissão de baseline** no caminho. **Assinalado:
> nada** — `espelho`, `GitHub`, `sincroniz` e `push` têm **`0`** ocorrências neste arquivo, e a
> conferência que o `CLAUDE.md` exige registra-se **aqui**, não em item. **Conferido e ainda
> correto:** os quatro `[!]` — Goals **`1.14`**, **`1.16`**, **`1.17`** e **`1.18`** — seguem
> `[!]` e **intocados**; as três menções a `IR-BL/3` e `IR-BL/5` *(linhas 149, 402 e 1149)* estão
> **todas em passado**, registrando o que valia no fechamento de cada missão, e por isso
> **nenhuma ficou falsa** — **`0` afirmações no PRESENTE** sobre instrumento vigente.
> **O que a sessão produziu:** **`IR-BL/6`** declara **`docs`** em **`NAO_ACERVO`** e desbloqueia
> a baseline, que estava em **`EXIT=2`** desde o merge — **nona ocorrência da família
> `RD-53`/`RD-81`, e a primeira ANUNCIADA POR ESCRITO antes de ocorrer**, pelo próprio autor que
> a causou (`docs/README.md`). **A emenda é de classe mais fraca que as duas anteriores:**
> `products` e `skills` entraram em `ACERVO`, o lado **medido**, e **mudaram** a contagem;
> `docs` entra em `NAO_ACERVO`, e como `ALVOS` é construído só a partir de `$ACERVO`, ele **não
> pode** mover número nenhum — **quatro provas de inércia** medem isso em vez de afirmá-lo.
> ⭐ **O `ADR` foi cogitado, autorizado, MEDIDO e descartado — e a medição poupou `3` artefatos:**
> **`0` de `6`** gerações do instrumento jamais tiveram `ADR`, e os atos que os precedentes citam
> *(nono ato, `ADR-0034`)* são sobre o **fato** — Produto admitido, `Skill` criada —, **nunca
> sobre o script**. O veículo correto é a **inscrição no catálogo**, e é o que se fez.
> **Duas afirmações falsas saíram do `CLAUDE.md`, ambas família `RD-101`:** o cabeçalho
> *"ESPELHO DE LEITURA"*, que chegou da máquina que deixou de existir, e a nota que apontava
> **`IR-BL/3`** como vigente — **vencida havia duas gerações**, no parágrafo que instrui quem
> mede. ⚠️ **A dívida de rotação de §10.0 do catálogo foi HERDADA por decisão expressa do
> Fundador**, entre três opções apresentadas: **agora são CINCO emissões** com §10.0 rotulada
> *"Baseline vigente"* nomeando `BL-2026-08-02-03`. **A fonte da baseline vigente é §2, nunca
> §10.0.** **`0` atos · `0` `ADR` · `0` `RFC` · `0` `FIT` · `0` Fundacionais · `0` bytes no
> medidor `IR-BL/5` · `0` bytes em `_SAIDA-COMPANY-OS/` · `0` achados novos inscritos.**
> Detalhe integral em [`PT-2026-025`](relatorio-transicao-2026-08-10-sincronizacao-e-ir-bl-6.md).

> **⚖️ Conferência de 2026-08-03, ao registrar a DECISÃO DO FUNDADOR sobre a promoção a `FND` —
> na mesma sessão que a recebeu.**
> **`Q1` deixa de estar aberta.** ⛔ **NÃO PROMOVER `ADR-0033` e `ADR-0037` a `FND` agora.**
> **Fundamento do Fundador, medido três vezes:** as **`30`** regras seguem **determinadas e não
> observadas** — **três instâncias, não população**. Canonizar antes do exercício repete **`L1` de
> `FND-11 §14`** e **`RD-107`**, e **foi o que `ADR-0021` pagou e `ADR-0022` desfez, a `1.328`
> linhas de retrabalho**. A decisão **acolhe** `FIT-2026-029 §8.6` e `FIT-2026-030 §7.5`, que
> recomendavam **NÃO**, e **é do Fundador porque promover é `C3 · Tipo 1` com ato** — nenhum
> Departamento podia tomá-la.
> **CUSTO ACEITO, e ele é registrado em vez de dissimulado:** o Framework **vive em duas sedes**
> — `22` regras em `ADR-0033` e `4` em `ADR-0037` —, e **quem ler `ADR-0033` isolado lê `SK-09`
> defeituosa SEM AVISO**, porque **`CC-01` proíbe o aviso dentro dele**. **O aviso permanece no
> catálogo mestre**, e o Fundador declara isso **suficiente enquanto os consumidores forem o
> Fundador e as sessões, que leem o catálogo**. ⚠️ **A suficiência é condicional, e a condição é
> justamente o gatilho `(b)` abaixo** — não é promessa de que o custo desapareceu.
> **GATILHO DE REAVALIAÇÃO, ESCRITO e não deixado a critério:** promover quando **(a)** as `30`
> regras tiverem **população e não amostra** — **piso a definir na própria promoção, coerente com
> `SK-27`** —, **ou (b)** surgir **consumidor que leia `ADR-0033` isolado, sem passar pelo
> catálogo**. **O que ocorrer primeiro.**
> ⭐ **O gatilho `(a)` aplica ao próprio Framework a regra que o Framework acabou de instituir:**
> `SK-27` manda declarar o piso antes de aplicar regra que dependa de população, e a promoção
> **é** uma decisão que depende de população. **O Fundador não fixou o piso aqui — reservou-o para
> a promoção —, e reservar não é omitir: é a própria `SK-27` dizendo que piso não se estima**
> (`CE-04`).
> **ACHADO REGISTRADO, com dono e gatilho, e DELIBERADAMENTE NÃO CORRIGIDO** — ver a seção
> [ACHADO REGISTRADO](#achado-registrado--sem-número-rd-e-a-razão-está-escrita), abaixo.
> **Assinalado: nenhum item de Goal muda de estado.** `Skills Framework` já estava `[x]`; a
> decisão **fecha uma pergunta, não abre nem completa etapa**. **`0` artefatos criados · `0` `ADR`
> · `0` hash de rito · `0` atos · `0` baseline nova · `0` bytes no catálogo mestre, em Fundacional,
> em `ADR`/`RFC`/`FIT`/`PT`/`MSG`, em `TPL`, em `skills/` e em código.** **Escrita mínima: este
> arquivo, e só ele.**
> ⚠️ **CONSEQUÊNCIA DECLARADA, e ela não é escondida atrás de *"sem rito"*:** este arquivo **é
> medido** pelo instrumento *(está em `governance/` e é `.md`)*, de modo que escrevê-lo **muda
> `linhas`, `manifesto` e `impressão digital` da árvore viva**. Portanto, dos quatro valores
> publicados de **`BL-2026-08-03-06`**, **`artefatos` = `253` CONTINUA reproduzindo** — nenhum
> artefato foi criado ou removido — e **os outros três NÃO**. **Nenhuma baseline nova é emitida
> porque nenhum artefato foi criado, e o único arquivo tocado tem `autoridade: nenhuma`**; é o
> mesmo desfecho do `fencing_token` 14, que fez escrita mínima idêntica.
> **Os números exatos do delta vivem no `fencing_token` 30 do lease, FORA do acervo** — e ficam
> lá de propósito: escrevê-los aqui os tornaria falsos no instante em que fossem escritos, porque
> **este arquivo é medido por aquilo que ele estaria descrevendo**. **É a mesma auto-referência
> que `ADR-0033 §N4` registrou ao medir portão por curinga: o instrumento contando a si mesmo.**
>
> **SEM MISSÃO ATIVA a partir daqui.**
>
> **⭐ Conferência de 2026-08-03, ao fechar a Missão 1.13.14 — na mesma sessão que fechou a missão.**
> **O `ADR` sucessor de `ADR-0033` foi ABERTO, e ele SUPERA PARCIALMENTE em vez de superar o todo.**
> Assinalado: **nenhum item de Goal muda de estado** — `Skills Framework` já estava `[x]`, e
> **`GO-TO-SKILLS` nem chegou a ser exercido: `0` `Skill`s criadas**. **O instrumento foi
> DETERMINADO antes de redigir, e a norma o nomeia:** `FND-07 §8.1` enumera três resultados de
> revisão, e o do meio é **`AJUSTAR` — *"novo `ADR` que supera **parcialmente** o anterior"***; a
> revisão **era devida**, porque o gatilho do próprio `ADR-0033` *("a primeira `Skill` real")*
> **disparou três vezes**. **Corrigido pelo que a missão produziu, e são três itens:** *(a)* o corpo
> do item `Skills Framework` registrava até a **segunda** `Skill` — o exercício da **terceira** vivia
> só no bloco de conferência do topo, e agora está no corpo; *(b)* os contadores de `§4.2`, `§4.5` e
> `§4.7` do catálogo **estavam defasados desde a emissão anterior** — somados davam **`244`** quando
> as linhas já davam `249`, porque a emissão anterior inscreveu `5` linhas e atualizou **`1` de `4`**
> cabeçalhos; **corrigidos os quatro, descobertos por EXERCÍCIO** *(contar por ferramenta em vez de
> somar o declarado)*; *(c)* a afirmação *"primeira superação de `ADR` do acervo"* foi escrita,
> **medida, achada FALSA e retirada antes de publicar** — são **três** *(`ADR-0022`, `ADR-0027` e
> este)*, e **`superado_por` é `null` em `37` de `37`**, com os três superados **`ativo`**: **o
> acervo já praticava esta construção duas vezes**. **AS QUATRO CORREÇÕES:** **`SK-28`** — `11`
> blocos de corpo mais `1` atributo de frontmatter **não é `12`**, e o defeito custou `gatilho`
> materializado **`3` de `3`**; **`SK-29`** — o rito da classe vem **inteiro**, `5·5·5` com `0`
> redução; **`SK-30`** — saídas plausíveis e erradas no **plural**, insuficiente em **`2` de `3`**;
> **`SK-27`** — o remédio **geral**: *toda regra cujo antecedente dependa de população ou de aresta
> declara o seu **piso**, e abaixo dele é **`INAPLICÁVEL`**, nunca **satisfeita***. ⭐ **E a regra
> geral foi VARRIDA: `5` membros, não `2`** — `SK-21 (a)`, `SK-21 (b)`, `SK-22`, `SK-24`, `SK-25` —,
> com **três achados que exercício nenhum produziria**. ⚠️ **`FIT-2026-029 R4` AVALIADO e deixado de
> FORA por `1` de `3` instâncias — a regra nova aplicada CONTRA o interesse da própria missão**, que
> perde a economia de um `ADR` futuro e a recusa assim mesmo. **`0` atos · `0` Fundacionais · `0`
> bytes em `TPL-skill` · `0` bytes em `ADR-0033` a `ADR-0036` · `0` bytes de código · `0` bytes no
> medidor · `0` `Skill`s criadas · segundo candidato da F8 NÃO admitido · `ADR-0033` NÃO promovido a
> `FND` · `RD-122` NÃO sanado · `RD-116` NÃO decidido · `0` achados novos.**
>
> **⭐ Conferência de 2026-08-03, ao fechar a Missão 1.13.13 — na mesma sessão que fechou a missão.**
> **A TERCEIRA `Skill` do acervo existe, e com ela `SK-24` deixa de ser uma regra que só podia
> responder *"não"*.** Assinalado: **nenhum item de Goal muda de estado** — `Skills Framework` já
> estava `[x]`, e **exercer o portão três vezes continua não sendo liberá-lo** (`FND-01 §6.2`).
> **Corrigido pelo que a missão produziu, e são dois itens:** *(a)* o item **`1.19`** passa a contar
> **`3`** Skills, não `2`; *(b)* a razão registrada para `SK-21` nunca ter sido exercida estava
> **incompleta** — não espera só agentes, espera a **primeira dependência entre componentes**, e
> `0` existem. **AS CINCO MEDIÇÕES:** **(1) `SK-24` calculada com `n = 3`** — mediana **`188`**
> *(`175`, `188`, `231`)*, limiar **`376`**, maior **`231`**: **não dispara, mas pela primeira vez
> PODERIA** — o conjunto-solução deixa de ser vazio, e **o piso de `n` é `3`, provado**;
> **(2) `SK-09` e `SK-10` reprovaram pela TERCEIRA vez, identicamente** — e desta vez **o autor
> conhecia os defeitos e escreveu `§0` e `§11` para antecipá-los**, e eles ocorreram assim mesmo:
> **elimina-se a hipótese de defeito de LEITOR**; **(3) `SK-21` segue não exercida**, e a terceira
> ficha é a primeira a **citar** outra `Skill` — **referência de fronteira não é aresta de
> dependência**; **(4) custo `5` contra `5` contra `5` — `0` redução em três medições**, e a
> terceira é conclusiva porque o candidato **declarou o preço antes**; **(5) nascer sob as `26`
> reduziu o retrabalho de REDAÇÃO a quase zero — `1` → `0` reprovações por regra, e `SK-03` passou
> pela primeira vez em três — e reduziu o custo do RITO em NADA.** **Conferido e NÃO corrigido: o
> número *"109 itens"* NÃO existe neste arquivo nem no acervo** — `0` ocorrências, com controle
> positivo *(`itens` = `6` aqui)*. **`0` atos · `0` Fundacionais · `0` bytes em `TPL-skill` · `0`
> bytes de código · `0` bytes no medidor · `0` bytes escritos no `nxtrack` · segundo candidato da
> F8 NÃO admitido · `ADR` sucessor NÃO aberto — mas a recomendação MUDA: `FIT-2026-029` recomenda
> ABRIR AGORA, e não esperar a quarta.**
>
> **⭐ Conferência de 2026-08-03, ao fechar a Missão 1.13.12 — na mesma sessão que fechou a missão.**
> **A SEGUNDA `Skill` do acervo existe, e com ela o Framework de Skills vira o primeiro framework
> do acervo medido DUAS vezes.** Assinalado: **nenhum item de Goal muda de estado** — `Skills
> Framework` já estava `[x]`, e **exercer o portão duas vezes continua não sendo liberá-lo**.
> **Corrigido pelo que a missão produziu, e são três itens que ficaram desatualizados:** *(a)* o
> item **`1.15`** afirmava *"não há `SKL` nem `WFL` no acervo, logo não pode haver sinal"* — **a
> premissa caiu**: há `2` `SKL`, e a primeira observação empírica de `SK-12` **existe**; *(b)* o
> item **`1.19`** contava **`0` Skills** entre os cinco elos sem instância — são **`2`**, e os elos
> sem instância caem para **quatro**; *(c)* o achado `AS-1`/`RD-122` foi **exercido pela segunda
> vez** e deixa de ser peculiaridade do caso. **As quatro medições que só a segunda instância
> permite:** **`SK-24` calculada e provada VAZIA até `n = 3`** *(mediana `181,5`, limiar `363`,
> maior `188` — e disparar exigiria `a < 0`)*, o que **corrige a palavra *"incalculável"*** do
> registro anterior sem mudar a conclusão; **`SK-09` e `SK-10` reprovaram identicamente em caso
> disjunto — o defeito é do FRAMEWORK, não do caso**; **`3` regras exercidas pela primeira vez**
> *(`SK-05`, `SK-12`, `SK-22`)*, cobertura acumulada **`25` de `26`**; e **o custo não caiu —
> `5` contra `5`, o custo é do RITO**. **`0` atos · `0` Fundacionais · `0` bytes de código · `0`
> bytes no medidor · `ADR` sucessor NÃO aberto: a recomendação é ESPERAR a terceira `Skill`.**
>
> **⭐ Conferência de 2026-08-03, ao fechar a Missão 1.13.11 — na mesma sessão que fechou a missão.**
> **A PRIMEIRA `Skill` do acervo existe, e `GO-TO-SKILLS` passa a EXERCIDO.** Assinalado: nenhum item
> de Goal muda de estado — `Skills Framework` já estava `[x]` desde a admissão —, mas **o portão que
> ele guardava foi exercido**, e as **26 regras saem de determinadas para observadas**. **Corrigido
> pelo que a missão produziu, e são duas correções de leitura:** *(a)* dizer que `Skill` é *"o
> componente mais barato"* vale para a **ratificação** *(`0` atos)* e **não** para os instrumentos —
> a primeira custou **5 artefatos, os mesmos de `SPC-001`**; *(b)* **`SK-03` reprovou o nome da
> capacidade externa** e a renomeou — primeiro caso de regra do Framework corrigindo o objeto que
> recebia. **O Framework NÃO saiu ileso do primeiro uso: `SK-09` defeituosa, `SK-10` e `SK-24`
> insuficientes — 3 em 26 (11,5%), contra 5 em 32 (15,6%) de `SPC-001`.** Corrigi-los exige **`ADR`
> sucessor**, porque `ADR-0033` é `M1`. **O portão de raiz do medidor recusou medir** *(quinta
> ocorrência de `RD-53`)*, e `IR-BL/5` declarou `skills` com as 4 provas. **`0` atos · `0`
> Fundacionais · `0` bytes de código no acervo · `RD-122` exercido e não sanado.**
>
> **✅ Conferência de 2026-08-03, ao fechar a Missão 1.13.10 — na mesma sessão que fechou a missão.**
> **O Framework de Skills foi ADMITIDO, e o primeiro item da cadeia nova passa a `[x]`.** Assinalado:
> **`Skills Framework` sai de `[!]` e entra em `[x]`** — `ADR-0033`, `ativo`, `C2 · Tipo 2`,
> `ratificacao: nao-exigida`, **`0` atos**. **A cadeia começa a andar.** **Corrigido pelo que a missão
> produziu:** o candidato afirmava ser **`M2`**, e isso é **falso para a sede `ADR`** — `SK-26` foi
> admitido como **`M1`**, com a alteração **isolada e declarada**; **a `Skill` — o tipo — continua `M2`**.
> **Conferidos e ainda corretos:** `1.16`, `1.14`, `1.17` e `1.18` seguem `[!]`; `1.15` e `1.19` seguem
> `[ ]` fora da cadeia; a primeira DIVERGÊNCIA segue **RESOLVIDA**. **`GO-TO-SKILLS` NÃO foi liberado**
> — passa de *exercível quando houver norma* a **EXERCÍVEL**, e exercê-lo é criar a primeira `Skill`.
> **`RD-122` confirmado e AMPLIADO** *(de `gatilho` para `gatilho` + `capabilities`)*, **não corrigido**.
> **`RD-116` não decidido. `0` Fundacionais tocadas. `0` outros candidatos admitidos.**
>
> **⚖️ Conferência de 2026-08-03, ao registrar as DUAS DECISÕES do Fundador — na mesma sessão.**
> **A *Sequência* mudou de ordem pela primeira vez desde que existe.** Assinalado: a cadeia passa a
> **Skills → 1.16 → 1.14 → 1.17 → 1.18**, com `Skills` **primeiro e sem número** — o despacho
> numerou os outros quatro e não este, e atribuir um seria decidir o que ele não decidiu.
> **`1.15` e `1.19` saíram da cadeia ordenada** e ganharam seção própria: *não esperam vez, esperam
> fato*. **A primeira DIVERGÊNCIA REGISTRADA passa a ✅ RESOLVIDA**, com os três fundamentos do
> Fundador escritos e medidos — custo *(Ferramenta é o único `Tipo 1` com ato sempre)*, sinal
> *(só `SKL`/`WFL` produzem o que `TE-7` exige)* e precedente *(4 artefatos, com `PT-2026-007` e
> `PT-2026-008` conferidos por `id`)*. **`RD-116` fica ABERTO por determinação expressa**, com
> gatilho *"próxima emenda Fundacional"*, para não gastar um ato `C3` isolado. **Corrigido pelo
> que a decisão produziu:** os enunciados que diziam *"é decisão do Fundador"* e *"onde Skills
> entra … fica aqui, sem gerar missão"* estavam certos e **caducaram** — preservados, não
> apagados. **`0` candidatos admitidos · `0` Fundacionais tocadas · `0` atos · `0` missões
> abertas · `0` baselines emitidas.** **`RD-124` fecha quanto à posição e ao portão e segue
> ABERTO quanto ao número de Goal.**
>
> **Conferência anterior de 2026-08-03, ao fechar a Missão 1.13.9 — na mesma sessão que fechou a missão.**
> **A régua foi consertada, e ela precedeu os candidatos por determinação do Fundador.** Assinalado:
> **nenhum item de Goal mudou de estado** — 1.14, 1.16, 1.17 e 1.18 seguem `[!]`, 1.15 e 1.19 seguem
> `[ ]` —, porque esta missão **não admitiu candidato algum**, por restrição expressa. **Corrigido
> pelo que a missão produziu, e a correção é minha:** o enunciado de `AI-1` na conferência anterior
> dizia *"descoberto por exercício"* e sugeria achado **novo**. **Não é.** O limite está declarado no
> acervo **10 vezes em 4 artefatos desde 2026-07-28**, incluindo **`MSG-2026-0001`, o primeiro ato
> soberano. Era limite conhecido, declarado e compensado** — o que estava errado não era a
> ignorância, era a régua obrigar toda missão a carregar um segundo instrumento. **`AI-1` foi
> inscrito como `RD-108` e CORRIGIDO** por `IR-BL/4`; **`BL-2026-08-03-01` é a primeira baseline do
> acervo cuja impressão digital lê os bytes**. **17 achados inscritos** (`RD-108`–`RD-124`), **279
> linhas reconciliadas**, **0 baselines históricas reescritas**. **Conferidos e ainda corretos:** a
> Sequência, as cinco divergências, `GO-TO-SPECS` EXERCIDO. **`RD-80` continua aberto.**
>
> **Conferência anterior de 2026-08-02, ao fechar a Missão 1.14 — na mesma sessão que fechou a missão.**
> Assinalado: **a *Sequência* deixa de estar integralmente `[ ]` pela primeira vez.** `1.14`,
> `1.16`, `1.17` e `1.18` passam a **`[!]`** — candidato redigido, espera decisão do Fundador.
> `1.15` e `1.19` **continuam `[ ]`**, e o enunciado de cada um agora diz **por qual norma ou
> fato** estão bloqueados. **Corrigido pelo que a missão produziu, e a correção é de conteúdo:**
> a premissa de `1.14` — *"Model é classe de Ferramenta"* — **não é matéria a decidir**, é norma
> vigente desde `ADR-0003`; e `1.15` **não espera decisão**, espera **fato**. **Criada a QUINTA
> DIVERGÊNCIA**, com o custo medido de cada componente. **Conferidos e ainda corretos:** 1.13.5.2
> `[x]`, 1.13.5.1 `[x]`, 1.13.5 `[x]`, `SF-01`–`SF-32` em vigor, `GO-TO-SPECS` EXERCIDO,
> `ADR-0032` `ativo` · `ratificada`. **Não conferida a trilha SSC+**, pela mesma razão de sempre.
> **`RD-80` continua aberto**, e agora com companhia medida: **9 achados novos desta missão
> vivem fora do catálogo**, porque o congelamento proíbe inscrevê-los sem missão própria.
>
> **Conferência anterior de 2026-08-02, ao fechar a Missão 1.13.5.1 — na mesma sessão que fechou a
> missão.** Assinalado: **1.13.5.1 entra em *Concluído*** com decisão
> **READY-FOR-RATIFICATION**, e *Em curso* volta a ficar vazio, apontando o que espera o
> Fundador. **Corrigido pelo que a missão produziu, e a correção é de conteúdo, não de
> marcação:** a linha *"a segunda Spec, sem colisão própria, custa 2"* estava **certa na
> aritmética e errada na condição** — a colisão de `RD-91` **não é própria de `SPC-001`**, é
> estrutural, e enquanto não for sanada **toda** Spec custa 5. **Criada a segunda DIVERGÊNCIA
> REGISTRADA**, com o alcance medido de `RD-91`, e ela já nasce **resolvida quanto ao
> instrumento**: o Fundador reafirmou o menu depois de ler o relatório do Item 0, e a emenda
> foi redigida no escopo mais estreito que a medição prova eficaz. **Conferidos e ainda
> corretos:** 1.13.5 `[x]`, 1.13.4.4 a 1.13.4.6 `[x]`, `SF-01`–`SF-32` em vigor,
> `GO-TO-SPECS` EXERCIDO. **Não conferida a trilha SSC+**, pela mesma razão de sempre.
> **`RD-80` continua aberto e o gatilho disparou pela QUARTA vez.**
>
> **Conferência anterior de 2026-08-02, ao fechar a Missão 1.13.5 — na mesma sessão que fechou a
> missão.** Assinalado: **1.13.5 sai da *Sequência* e entra em *Concluído*.** A primeira `Spec`
> real do acervo existe — `SPC-001`, sobre `LM-6(a)` do nXtrack —, e com ela **`GO-TO-SPECS` passa
> de *exercível* a EXERCIDO**. **Corrigido pelo que a missão produziu:** a seção **ONDE O SISTEMA
> COMEÇA A DEVOLVER** falava no futuro *("a partir dela")*; agora tem resposta medida, e ela está
> lá embaixo, não aqui. **Conferidos e ainda corretos:** 1.13.4.4, 1.13.4.5 e 1.13.4.6 `[x]`,
> portão `G0 a G5`, `SF-01`–`SF-32` em vigor, `ADR-0027` e `ADR-0029` `ativo`. **A DIVERGÊNCIA
> REGISTRADA sobre `GO-TO-SKILLS` mudou de estado, e a mudança está anotada lá:** o fundamento
> com que DEP-QAR o recusou — *"o portão anterior foi liberado e não pode ser exercido"* —
> **caiu**, porque agora `1` Spec existe. **Isso não libera `GO-TO-SKILLS`**: onde Skills entra
> continua sendo decisão do Fundador. **Não conferida a trilha SSC+**, pela mesma razão de
> sempre. **`RD-80` continua aberto e o gatilho disparou pela TERCEIRA vez** — este arquivo
> segue sendo medido sem ter entrada de catálogo.
>
> **Conferência anterior de 2026-08-01, ao fechar a Missão 1.13.4.6 — na mesma sessão que
> fechou a missão.** Assinalado: **RD-33 sai de *Em curso* e entra em *Concluído*.**
> **Corrigido pelo que a missão produziu — e o enunciado anterior estava errado num
> ponto:** ele dizia *"a missão própria é a 1.13.5"*. **Não é.** O item VII e `LA-3`
> reservam *"missão própria"* **sem nomear qual**, e `MSG-2026-0009 §8` põe *"RD-33
> destravado"* **antes** de 1.13.5. A missão própria foi a **1.13.4.6**, aberta só
> para isso, e o rito foi **ministerial** — `0` atos emitidos. **Conferidos e ainda
> corretos:** 1.13.4.4 e 1.13.4.5 `[x]`, portão `G0 a G5`, `SF-01`–`SF-32` em vigor,
> `ADR-0027` e `ADR-0029` `ativo`. **1.13.5 continua `[ ]`**, e agora nasce com
> `GO-TO-SPECS` **exercível**. **Não conferida a trilha SSC+**, pela mesma razão de
> sempre. **`RD-80` continua aberto e o gatilho disparou pela segunda vez** — este
> arquivo segue sendo medido sem ter entrada de catálogo.
>
> **Conferência anterior de 2026-08-01, ao fechar a Missão 1.13.4.5 — segunda passagem,
> na mesma sessão que fechou a missão.** Assinalado: **1.13.4.5 concluída**, com
> o primeiro Produto do acervo em vigor. Corrigido pelo que a missão produziu:
> *"Aplicação ministerial"* sai de **Em curso** *(estava `[ ]` e está feita)*;
> **RD-33 continua em Em curso, com o enunciado trocado** — a condição de fato
> caiu, e o que o mantém aberto agora é a **reserva do item VII do ato**, não a
> ausência de Produto. **Conferidos e ainda corretos:** 1.13.4.4 `[x]`, portão
> `G0 a G5`, `GO-TO-SPECS` liberado, `SF-01`–`SF-32` em vigor, `ADR-0027` e
> `ADR-0029` `ativo`. **Não conferida a trilha SSC+**, pela mesma razão de
> sempre: vive fora do acervo. **Este arquivo continua sendo medido pelo
> instrumento sem ter entrada de catálogo** — achado `RD-80`, **aberto**, e o
> gatilho *"próxima emissão de baseline"* **disparou** com `BL-2026-08-01-02`.
>
> **Conferência anterior de 2026-08-01 — uma vez, contra o catálogo vigente.** Corrigidos:
> a marcação do portão *(era `G1 a G5`; é `G0 a G5`, e `G0` é o que sustenta a
> admissão)*, o item 1.13.4.4 *(de `[!]` a `[x]` — o ato foi emitido e
> registrado)* e a seção de divergência *(o enunciado anterior não se
> sustentou)*. **Conferidos e corretos:** 23 Capabilities, 9 Departamentos,
> 11 fundacionais, `GO-TO-SPECS` liberado, `SF-01`–`SF-32` em vigor, `ADR-0027`
> e `ADR-0029` `ativo`, `RD-33` bloqueante.

---

## ÉPICO 1 — Camada Organizacional v1

### Concluído
- [x] Fundação, 23 Capabilities, governança C0–C3
- [x] GO-TO-SPECS
- [x] Specification Framework SF-01–SF-32
- [x] E1 e E3 em vigor — classe G0/RECOGNIZE e caminho de superação de ato
- [x] Portão ADR-0007 aplicado ao nXtrack — **G0 a G5**, `G0` = IDENTIDADE
      declarado antes de `G1`, `G3` = RECOGNIZE, `G5` preparado e não
      consumido na missão; decisão READY-FOR-RATIFICATION
- [x] **1.13.4.4 — reassinatura do ato de admissão do nXtrack.** O ato foi
      **emitido** sobre `PS-2026-016 §6` 1.2.0, itens I a VII, linhas 185–328,
      e **registrado** em `MSG-2026-0009` — nono ato soberano. `CA-1` a `CA-6`
      em 6 de 6; `Q2` gravada como artefato pela primeira vez. **`G5` consumido
      aqui**, e nada foi aplicado

- [x] **1.13.4.5 — aplicação ministerial da admissão do nXtrack.** `O4` em
      `RFC-0025` *(pela variante — `aprovado`, nunca `ativo`)* e em `ADR-0030`
      *(`ativo` · `ratificada`)*; **`products/nxtrack/carta.md` criado** —
      **o primeiro Produto do acervo** —, com o `H-A` do **arquivo aplicado**
      publicado *(`fca656a9…39e2`, distinto do `H-A` do candidato)*; catálogo e
      cinco projeções `M3` reconciliados na mesma mudança; **`products` e
      `CLAUDE.md` declarados no `baseline.sh`** *(achado RD-81 **fechado** pelo
      próprio dono, o Fundador)*; **`BL-2026-08-01-02`** reproduzida em duas
      execuções. `H-P` 2/2, `H-N` invariante 2/2, `IR-09` 3/3, candidato intacto,
      **0 bytes fora do conjunto autorizado**. Decisão **APLICADO** —
      `PT-2026-015`

- [x] **1.13.4.6 — fechamento de RD-33.** Fechado por rito **ministerial**,
      determinado **antes** de exercido: `PA-01`, `PA-03`, `PA-07` e `PA-13` de
      `ADR-0020`, `AU-06`, `FND-04 §4 [7]` e `RG-01`/`RG-03`/`RG-04`/`AC-09` de
      `FND-10`. **`0` atos emitidos, `0` fontes emendadas.** A reserva do item VII
      era **temporal e de sede**, nunca de classe de rito. Prova **por exercício**:
      `DoR` de `SF-23` reexercido, item **(9) passa**. **O acervo fica sem
      pendência bloqueante pela primeira vez desde 2026-07-29**, e `GO-TO-SPECS`
      passa de *liberado e não exercível* a **exercível**. O resíduo — a Spec de
      matéria **não-produto**, que só `S2` cria — **migrou para `RD-88`, aberto**.
      `BL-2026-08-01-03`. Decisão **APLICADO** — `PT-2026-016`

- [x] **1.13.5 — a PRIMEIRA Spec real do acervo.** `SPC-001` criada em
      `products/nxtrack/specs/`, sobre a lacuna **LM-6(a)** do nXtrack — matéria que o nono ato
      fixou *"com prioridade sobre as demais de LA-7"*. Rito: `RFC-0026` → `ADR-0031` →
      `SPC-001` → `FIT-2026-024` → `PT-2026-017`. **10 requisitos** com os 6 campos de `SF-12`
      *(60 campos, 0 ausentes)*, **4/4** categorias de `SF-25`, **7/7** perfis de `SF-17`,
      **5/5** métodos de `SF-14`, **9/9** elos de `SF-20`, **21/21** blocos de `SF-09`,
      **DoR 9/9** e **DoD 10/10** — exercidos, não afirmados. Classe **C2 · Tipo 2**, elevada do
      piso `C1` porque a coluna `C1 · T2` de `SF-10` produz **aprovação nula** para todo `SPC`
      (`RD-91`, dono SOBERANO). `LM-6(a)` **remedido e reproduzido**: 0 nos seis termos, em duas
      varreduras, com **controle positivo** aplicado antes de se acreditar em qualquer zero.
      **GO-TO-SPECS passa de exercível a EXERCIDO.** 5 achados novos (`RD-91` a `RD-95`),
      0 fechados, nenhum gera missão. `BL-2026-08-02-01`. Decisão **APLICADO** — `PT-2026-017`

- [x] **1.13.5.1 — a emenda que sana `RD-91`, produzida pelo rito `C3` completo e NÃO
      aplicada.** O Item 0 mandava medir antes de redigir, e a medição **mudou a sede da
      emenda**: a célula `C1 · T2` de `FND-11 §5`, que o achado nomeava, **reproduz
      literalmente** `FND-09 §8.2` linha `SPC` *(`DEP-PRD`)* e `FND-04 §2.1` linha `C1`
      *(`proprietário + revisor`)* — e por `PJ-03` com `FND-01 §10` **emendar só a projeção não
      sanaria**. A emenda foi redirecionada **para a fonte**: `FND-09 §8.2` linha `SPC` passa a
      dizer *"em `C1`, **DEP-EXE**"*, com cascata em `FND-11 §5` e propagação a **2 Cartas
      ratificadas** cujas **6** linhas ficariam falsas. A varredura das **21** linhas de
      `FND-09 §8.2` achou o mesmo colapso em **`PRJ`** e **`TPL`**, fora de `SPC` — **reportado
      ao Fundador antes de redigir**, e mantido fora da emenda por decisão dele.
      Rito: `RFC-0027` → `ADR-0032` → `FIT-2026-025` → `PS-2026-017` → `PT-2026-018`. Classe
      **`C3 · Tipo 2`**, determinada por **4 fundamentos citados**, nunca por analogia.
      **Os três números: a 1ª Spec custou 5 · a 2ª custará 2 · esta emenda custa 7.**
      **0 atos emitidos · 0 fundacionais tocadas · 0 Cartas tocadas · 0 regras de conteúdo de
      Spec · `SPC-001` não reclassificada.** 4 candidatos fora do acervo, com `H-A`/`H-N`/`H-P`.
      5 achados novos (`RD-96` a `RD-100`), 0 fechados, nenhum gera missão.
      `BL-2026-08-02-02`. Decisão **READY-FOR-RATIFICATION** — `PT-2026-018`

- [x] **1.13.5.2 — a APLICAÇÃO MINISTERIAL do item VI: o décimo ato foi CONSUMIDO e `RD-91`
      fecha parcialmente.** Rito **ministerial** (`ADR-0020`, `PA-01`): executa o ato, **não o
      interpreta**. `O4` em **5** objetos — as **quatro** versões promulgadas pelo item I
      *(`FND-09` **1.6.0** · `FND-11` **1.1.0** · Cartas `DEP-PRD` e `DEP-EXE` **1.2.0**)* **e
      `ADR-0032`**, que o mesmo item **RATIFICA**: *"RATIFICO o `ADR-0032` … **e** promulgo as
      quatro versões abaixo"* — **dois verbos, dois conjuntos**, e por isso 5 e não 4.
      **`H-P` conferido 4 de 4 · `H-N` invariante 5 de 5 · `IR-09` 4 de 4** por DEP-QAR.
      Os `H-P` foram lidos **do próprio arquivo, de duas sedes** — §4 do ato e §3.1 do pacote —
      e conferidos contra medição nova: **12 de 12, 0 de `hashes-candidatos.txt`**, que carrega
      6 valores superados de 24 e faria **2 de 4 falharem, parando uma aplicação correta**
      (`RD-101`). **A transição foi testada FORA do acervo antes de entrar**, de modo que
      **nenhum byte errado chegou a existir dentro dele**. Catálogo, índices e contadores
      reconciliados **na mesma mudança**. **`RD-104` INSCRITO** *(item 127)* — o Soberano nomeou
      **7** achados na ciência e **6** estavam inscritos; **nomear não inscreve**. 2 achados
      novos: **`RD-105`** *(§13.2 das duas Cartas declara 445/506 onde há 446/507 — corrigir
      quebraria o `H-P`)* e **`RD-106`** *(o ato move 5 objetos e o pacote publicou `H-P` para
      4)*. **`RD-91` FECHA PARCIALMENTE** — só quanto a `C1`; **`C0 · T2` segue ABERTA** pelo
      item V. **0 Specs criadas · 0 missões abertas · 0 bytes em `PS-2026-017`, `FND-04` e
      `SPC-001` · congelamento em vigor.** `BL-2026-08-02-03`. Decisão **APLICADO** —
      `PT-2026-019`

### Em curso
*(nada em curso. **O ato foi ASSINADO e APLICADO** — `ADR-0032` está `ativo` · `ratificada`
desde 2026-08-02. **O que espera o Fundador agora é o DESPACHO que abre a missão do item IV**
*(estender a emenda a `PRJ` e `TPL`)*: o **item VII** a proibia até esta aplicação estar
concluida **e conferida**, e a aplicação está concluída — **conferi-la é do Fundador**.
*(Enunciado anterior, preservado: o que esperava o Fundador era a ASSINATURA do ato, e
`ADR-0032` seguia `aprovado` · `ratificação: pendente`.)* **As três questões de `PS-2026-017 §7` foram
RESPONDIDAS por despacho de 2026-08-02**, e a minuta foi **reemitida em `PS-2026-017` `1.1.0`**:
`Q1` **estende** a `PRJ` e `TPL` com missão própria ordenada; `Q2` mantém `C0` **declarado**
em `RD-91`; `Q3` fixa o incremento **MENOR**, em **item VIII** criado para não decidir `Q3` em
silêncio. Os quatro `H-P` foram **reconferidos** e os quatro reproduziram. **Nada foi aplicado,
ativado, ratificado ou emitido.**

**Atualizado em 2026-08-02 — SEGUNDO despacho, e a minuta está em `PS-2026-017` `1.2.0`.** As
**4** determinações pré-assinatura: **3 cumpridas**, **1 impossível**. Item **VII** mantido como
reconciliado. As duas divergências que `1.1.0` deixara declaradas **saíram do limbo**: a
evidência que não reproduz virou **`RD-101` REGISTRADO** *(catálogo §7, item 124, sem correção —
os valores superados só existem fora do acervo)*, e a edição sem cópia datada virou **`RD-102`
REGISTRADO** *(item 125)*. **⛔ A determinação de mover a cópia de rollback não pôde ser
cumprida: `_to_delete/` foi apagado — `rd /s /q`, sem lixeira — ANTES de o despacho chegar,
e a cópia `..._pre-instrumentos-1-13-5-1` não existe mais.** §4 foi reescrito com o que sobrou,
medido: o manifesto reproduz, **595 de 602** arquivos ainda carregam os bytes pré-escrita,
**0 ausentes**, **7 divergem** — nenhum deles fonte normativa nem objeto do ato. Sede vigente
publicada: `_backups/LucaX-Enterprise-OS_2026-08-02_pre-reemissao-minuta-1.2.0`, **608**
arquivos, conferida 608 de 608. **`git` foi RECUSADO como âncora, por medição:** o commit
`3f6effb`, que se chama *baseline do acervo*, não reproduz **161 de 607** arquivos — **6 deles
normativos e todos `CRLF`**, incluindo **`FND-10`, uma Fundacional, com 785 bytes `CR` no disco
e 0 no commit**. **`RD-103`** *(rollback de ato destruído)* e **`RD-104`** *(o commit que se
chama baseline e não reproduz)* ficam **declarados e NÃO registrados** — o despacho mandou
registrar dois, e dois foram registrados. Os quatro `H-P` foram reconferidos **sobre a versão
final**, e a cobertura fechou: **20 de 20** valores publicados reproduzem. **Nada foi aplicado,
ativado, ratificado ou emitido. Congelamento em vigor.**

**Atualizado em 2026-08-02 — TERCEIRO despacho, e a minuta está em `PS-2026-017` `1.3.0`.**
**4 de 4 cumpridas na letra, nenhuma impossível.** A determinação **(c)** foi executada
**primeiro, porque é ela que autoriza as outras**: **`fencing_token` 16** adquirido às
**`13:24:25`**, com **0 bytes escritos no acervo antes dele** — **a primeira emissão do acervo
sob lease adquirido** —, e a regra permanente gravada no `CLAUDE.md` da raiz: *"Nenhuma escrita
no acervo sem lease adquirido. Vale para correção, registro e faxina, não apenas para missão."*
O fundamento é medido: as três escritas de `10:47`, `11:50` e `11:59` ocorreram com o lease
**liberado**. **(b)** `baseline.sh` foi a **`IR-BL/3`**, com `.git` e `.gitattributes` em
`NAO_ACERVO` pelo mesmo fundamento de `RD-81` — **`ACERVO` inalterado, portão não afrouxado** —,
**calibrado antes do uso com 4 provas, entre elas reversão**, e a baseline foi medida **pelo
instrumento vigente, sem variante, em 2 execuções**: **228 · 67.771 · `1513fd45…eb5f`**,
**+492 linhas** sobre `BL-2026-08-02-02` com **contagem de artefatos inalterada** e delta
**atribuído** aos 3 únicos `.md` tocados após `BL-02`. **(a)** a cópia parcial de `12:04` foi
**apagada**, com prova prévia **e posterior** de que **nenhum byte único se perde**. **`RD-103`
foi REGISTRADO** *(catálogo §7, item **126**, severidade **Alta**, elevada de Média)* — **e a
premissa do despacho foi corrigida por medição no próprio script**: ele **pede** confirmação
digitada; o defeito é que a listagem exibida era de **primeiro nível**, e o operador
**confirmou sem poder ver** que um ponto de retorno declarado estava dentro. **`RD-104`
continua declarado e não registrado** — o despacho mandou registrar um, e um foi registrado.
**Escritor concorrente detectado e declarado:** sessão alheia tocou `_SAIDA-COMPANY-OS/` às
`13:04` e `13:07`, com **0 arquivos do acervo tocados**. Os quatro `H-P` reconferidos pela
terceira vez: **20 de 20**. **0 baselines emitidas** — reconciliar é a aplicação ministerial do
item VI, depois do ato. **Nada foi aplicado, ativado, ratificado ou emitido. Congelamento em
vigor.**

**⚖️ Atualizado em 2026-08-02 — QUARTO despacho: o ATO FOI EMITIDO.** O Soberano (1) mandou
**corrigir a regra do `CLAUDE.md`**, trocando *"acervo"* pelo **recurso inteiro que o lease
fenceia** — a folga que esta sessão mediu **foi exercida** por sessão alheia às `13:04` e
`13:07`, e está **✅ FECHADA**; e (2) **EMITIU o ato dos itens I a VIII** da minuta de
`PS-2026-017` **1.3.0**. **`MSG-2026-0010` criado** — **décimo ato soberano**, e o terceiro do
acervo registrado **antes** da aplicação —, ancorado no `H-A` do pacote
**`c48cf443…0298`**, **medido no arquivo e nunca lido de transcrição**, itens **I a VIII**,
linhas `571`–`655`, com **4** `sha256` de recorte. **Os quatro `H-P` foram extraídos do
próprio arquivo, de duas sedes independentes, e conferidos contra medição nova: 8 de 8.**
**⏳ NADA APLICADO, por determinação expressa:** os quatro objetos seguem em **1.5.0, 1.0.0,
1.1.0 e 1.1.0**, com `H-A` medido **depois** do registro; a âncora continua reproduzindo. **A
missão ministerial do item VI vem em mensagem separada.** **`RD-91` NÃO fecha aqui** — fecha
**pela aplicação**, e **parcialmente**: `C0 · T2` segue aberta pelo item V. **`RD-104`
continua apenas declarado — nomear na ciência não inscreve.** **Declarado e não corrigido:**
a nota do item I do texto assinado ficou **superada** *(diz 2 reconferências sobre a `1.2.0`;
foram 3, a terceira sobre a `1.3.0`)* — o ato veio *"sem alteração de termo"*, e a nota
**subestima os fatos sem contrariá-los**. Escrito sob **`fencing_token` 17**, adquirido antes
da primeira escrita. **0 baselines emitidas · 0 missões abertas · congelamento em vigor.**

**✅ Atualizado em 2026-08-02 — o item VI foi APLICADO, e o décimo ato está CONSUMIDO.** A
**Missão 1.13.5.2** moveu `status` e `ratificação` dos **quatro** objetos e de **`ADR-0032`**,
reproduziu os **quatro `H-P`**, reconciliou catálogo e índices **na mesma mudança**, inscreveu
**`RD-104`** e o **fechamento parcial de `RD-91`**, e emitiu **`BL-2026-08-02-03`** em 2
execuções. **`fencing_token` 18**, adquirido antes da primeira escrita, com cópia datada
**provada fiel** — 236 arquivos, e a cópia reproduz os três valores do estado de partida.
**As duas frentes do item VII: uma FECHOU, a outra está DESTRAVADA e não aberta.** A missão do
item IV — estender a emenda a `PRJ` e `TPL` — **não foi aberta**, e não o foi de propósito: o
item VII a condiciona a esta aplicação estar **concluída e CONFERIDA**, e **conferir é do
Fundador, não de quem aplicou**. **`C0 · T2` não foi decidida** *(item V)*, **`FND-04` não foi
emendada** *(item VIII, `RD-99` aberto)*, e **`RD-91` não fechou além do que o ato alcança**.)*

**⛔ Atualizado em 2026-08-02 — a Missão 1.13.6 exerceu o rito barateado e devolveu `BLOCKED`.**
Primeira tentativa de `Spec` sob `C1`: matéria — o critério da **primeira rodada real do
`consult`**. **O rito saiu EXATAMENTE em `C1`, e a emenda produziu o efeito pretendido** —
verificado na fonte, célula a célula *(`FND-09 §8.2` **1.6.0**, `FND-11 §5` **1.1.0**,
`FND-04 §2`, `FND-09 §10.2`)*. **A `Spec` não nasceu por razão anterior à emenda:
`PRO-consult` não existe**, e `FND-04 §6` linha *Spec* exige *"Produto existe"* com *"todas
precisam ser verdadeiras"*, `SF-23` **(9)** o repete no `DoR`, e `FND-03 §3.6`/`FND-10 §4.4`
não dão caminho canônico fora de um produto. **Não é `RD-88`** — a matéria é de produto; falta
o Produto, e o caminho é **`S1` exercida uma segunda vez**, `C2 · Tipo 1` do SOBERANO com o
portão de `ADR-0007 §5.3`. **A matéria foi entregue integralmente fora do acervo** — candidato
de **348 linhas**, **13 requisitos**, **12 invalidantes**, em `_missao-1-13-6-2026-08-02/`.
**`0` artefatos criados · `0` emendados · `0` atos · `0` baselines emitidas · `0` bytes no
`consult` · portão de chamada real intocado.** Escrito sob **`fencing_token` 19**, adquirido
antes da primeira escrita, com cópia datada **provada fiel** *(649 arquivos; a cópia reproduz
`230 / 68.797 / e4c307e0…787a`)*. **Congelamento em vigor; a missão do item IV continua não
aberta.** Medição completa na **QUARTA DIVERGÊNCIA**.

**✅ Atualizado em 2026-08-02 — o Fundador DECIDIU sobre a 1.13.6: opção `(b)`, e o candidato
BASTA.** `PRO-consult` **não é admitido agora**; o critério de **348 linhas** fica **fora do
acervo**, como critério **operacional**, sem autoridade canônica e sem custo de rito. Fundamento
escrito: *rodar, medir, e só então decidir se vale admitir* — porque admitir custa **8 artefatos
/ 2.338 linhas** para governar um produto que ainda não provou que executa. **Gatilho registrado
(`GATILHO-1.13.6-PRO-consult`), dois e alternativos:** a rodada real **passar**, ou o `consult`
exigir **mais de uma `Spec`** — e o gatilho manda **reavaliar**, nunca admitir. **A medição virou
achado: `RD-107`** *(catálogo §7, item **130**; Média; dono **SOBERANO**, instrui **DEP-PRD**)* —
**o piso barateado só entrega onde já há Produto: `8 + 2 = 10` na primeira, contra `5` com
Produto existente.** Catálogo em **2.25.0**. **`0` artefatos criados · `0` fontes emendadas ·
`0` atos · `0` baselines · `0` missões abertas.** **O Agente 1 fica SEM MISSÃO ATIVA.**

**🛠️ Atualizado em 2026-08-02 — a Missão 1.14 mediu a Sequência inteira e redigiu QUATRO
Frameworks fora do acervo.** Missão de **desenvolvimento**, aberta porque o Agente 1 estava sem
missão ativa e a *Sequência* estava integralmente `[ ]`. **A descoberta que reparte a Sequência:**
`ADR-0021` instituiu as **32** regras do Framework de Specifications inteiro em
**`C2 · Tipo 2`, `aprovador: DEP-EXE`, `ratificacao: nao-exigida`** — medido no frontmatter, não
inferido. **Escrever a norma de um componente não custa ato; promovê-la a `FND` custa** *(foi o
que `ADR-0022` pagou)*, **e adotar cada instância custa conforme `FND-04 §6`.** Com isso, **4 dos
6 Goals da Sequência têm a norma redigível sem ato**, e os quatro candidatos foram escritos:
**1.14** *(`TF-01`–`TF-32`, 347 linhas)*, **1.16** *(`WF-01`–`WF-30`, 249)*, **1.17**
*(`AR-01`–`AR-30`, 270)* e **1.18** *(`EA-01`–`EA-28`, 259)*, todos em
`_missao-1-14-2026-08-02/`, **fora do acervo**, na mesma forma dos candidatos de `PS-2026-017` e
da 1.13.6. **`0` entidades · `0` tipos · `0` templates · `0` diretórios · `0` papéis · `0`
portões · `0` verbos de autoridade criados** — os quatro **recebem** `TOL`, `WFL`, `AGT`/`SUB` e
`FIT`, que já existem. **9 achados novos** *(`AF-1`–`AF-3`, `AW-1`–`AW-3`, `AA-1`–`AA-3`,
`AE-1`–`AE-3`)*, **nenhum registrado no catálogo e nenhum corrigido** — congelamento em vigor.
**`0` artefatos criados · `0` fontes emendadas · `0` atos · `0` baselines · `0` missões abertas.**
Escrito sob **`fencing_token` 21**, adquirido antes da primeira escrita sob o fence, com cópia
datada **provada fiel** *(649 arquivos; reproduz `230 / 68.893 / 45dc449f…c4b0`)*. Medição
completa na **QUINTA DIVERGÊNCIA**.

### Sequência

> **⚖️ ORDEM DECIDIDA PELO FUNDADOR em 2026-08-03, e ela não é a de antes.** A cadeia passa a
> ser **Skills → 1.16 Workflows → 1.14 Ferramentas e Modelos → 1.17 Agentes → 1.18 Execução**.
> **Os números são IDENTIFICADORES, não posições** — o próprio despacho os manteve estáveis ao
> reordenar, e renumerar quebraria referências em artefatos ratificados. **A ordem é a da lista.**
>
> **`1.15` e `1.19` ficam FORA da cadeia ordenada, e por razão distinta de todas as outras: eles
> não esperam vez, esperam FATO.** *"1.15 fica onde está: não espera decisão, espera sinal
> observado (`TE-7`)"* — palavras do despacho.

- [x] **Skills Framework — o portão `GO-TO-SKILLS`.** Capacidade reutilizável e
      nomeada, invocável por mais de um papel. Contrato, gatilho, procedimento,
      entradas e saídas, modos de falha, reuso e não-proliferação, custo de
      contexto, descontinuação. **Onde vive o acionamento** que `FND-10 §4.8`
      recusou como tipo `Command`.
      → saída: **`GO-TO-SKILLS` exercido**, e com ele o **sinal observado** que
      `1.15` espera
      **⚖️ POSTO AQUI POR DECISÃO DO FUNDADOR de 2026-08-03**, que resolveu a
      **primeira DIVERGÊNCIA REGISTRADA**. Fundamento citado por ele: `Ferramenta`
      é o único `Tipo 1` **com ato sempre**; `Skill` e `Workflow` **nunca
      ratificam** — *"começar pelo mais caro invertia custo e sinal"* —, e o acervo
      **já nomeava `GO-TO-SKILLS` quatro vezes como o portão seguinte**, em
      `FIT-2026-015`, `FIT-2026-016`, `PT-2026-007` e `PT-2026-008`. **A conclusão
      dele, e ela é a que reposiciona a culpa:** *"a sequência canônica é que estava
      desalinhada, não os quatro artefatos"*.
      **✍️ CANDIDATO REDIGIDO** — `SK-01` a `SK-26`, **253** linhas, em
      `_missao-1-14-2026-08-02/`, fora do acervo. **Espera admissão.**
      **✅ ADMITIDO em 2026-08-03 pela Missão 1.13.10 —** [`ADR-0033`](../decisions/ADR-0033-framework-de-skills.md),
      `ativo`, **`C2 · Tipo 2`, `ratificação: não-exigida`, `0` atos**. Classe **medida, não
      presumida**: `FND-04 §2.1`/`§2.2`, `FND-09 §8.2` linha `ADR`, `AL-01`, com o precedente
      `ADR-0021` **lido no frontmatter**. **`0` entidades · `0` tipos · `0` templates · `0`
      portões · `0` campos novos** — `gatilho` **já era atributo mínimo** de `FND-09 §E-13`.
      **4 artefatos:** `RFC-0028` · `ADR-0033` · `FIT-2026-026` · `PT-2026-020`. **A sede é
      `ADR`, logo `M1`:** corrigir uma vírgula exigirá **ADR sucessor** — a promoção a `FND`
      é `C3 · Tipo 1` **com ato**, e **não foi feita**, por a norma ainda não ter sido exercida.
      ⚖️ **ATUALIZADO em 2026-08-03: o enunciado acima estava CERTO e CADUCOU em parte —
      preservado, não apagado.** *"Corrigir uma vírgula exigirá `ADR` sucessor"* **cumpriu-se**:
      `ADR-0037` existe. E **a razão da não-promoção MUDOU**: já não é *"a norma ainda não ter
      sido exercida"* — **ela foi, três vezes** —, e sim que **três instâncias são EXERCÍCIO e
      não POPULAÇÃO** (`FND-01 §6.2`; decisão do Fundador de 2026-08-03). **O que não mudou:**
      a promoção continua `C3 · Tipo 1` **com ato**, e **continua não feita**.
      ⚠️ **`GO-TO-SKILLS` NÃO foi liberado** — liberar portão é ato de autoridade (`FND-01 §6.2`).
      Ele passa a **EXERCÍVEL**, e **exercê-lo é criar a primeira `Skill`**.
      ⚠️ **Sem número de Goal, e a omissão é deliberada:** o Fundador escreveu
      número para `1.16`, `1.14`, `1.17` e `1.18` e **não** para Skills. **Atribuir
      um seria decidir o que ele não decidiu** — `RD-124` fecha quanto à **posição**
      e ao **portão**, e permanece **aberto quanto ao número**.
      **✅ EXERCIDO em 2026-08-03 pela Missão 1.13.11 —** [`SKL-custodia-criar-copia-datada`](../skills/SKL-custodia-criar-copia-datada.md),
      a primeira `Skill`. **`5` artefatos**, `C2 · Tipo 2`, `0` atos. As **26** regras saem de
      *determinadas* para **observadas**, com **`19`/`4`/`2`/`1`**.
      **✅ EXERCIDO PELA SEGUNDA VEZ em 2026-08-03 pela Missão 1.13.12 —** [`SKL-seguranca-varrer-credencial`](../skills/SKL-seguranca-varrer-credencial.md).
      **`5` artefatos de novo — o custo é do RITO, não da novidade.** As 26 passam de *observadas*
      a **observadas em série**, com **`22`/`1`/`2`/`1`** e **`0` defeitos novos**. **`SK-24`
      calculada e provada VAZIA até `n = 3`; `SK-09` e `SK-10` confirmadas como defeito do
      FRAMEWORK; `SK-05`, `SK-12` e `SK-22` exercidas pela primeira vez.** Cobertura acumulada
      **`25` de `26`** — **a única jamais exercida é `SK-21`**, sem caso porque `0` agentes existem.
      ⚠️ **`GO-TO-SKILLS` continua NÃO liberado: exercer duas vezes não é liberar.**
      ⚠️ **`ADR` sucessor NÃO aberto** — recomendação de `FIT-2026-028`: **esperar a terceira
      `Skill`**, porque `SK-24` é a única cujo sinal ainda é cego, e o sucessor também será `M1`.
      **✅ EXERCIDO PELA TERCEIRA VEZ em 2026-08-03 pela Missão 1.13.13 —** [`SKL-custodia-provar-restauracao-de-backup`](../skills/SKL-custodia-provar-restauracao-de-backup.md).
      **`5` artefatos pela terceira vez — `0` redução.** **`SK-24` calculada com `n = 3`: o piso é
      `3`, PROVADO** — a regra deixa de ser estruturalmente vazia. **`SK-09` e `SK-10` reprovam pela
      terceira vez COM O AUTOR CIENTE**, o que **elimina a hipótese de defeito de leitor**.
      Cobertura acumulada **`25` de `26`**. *(Assinalado na sessão da própria 1.13.13; esta linha
      **reconcilia o corpo do item**, que registrava até a segunda — o registro da terceira vivia só
      no bloco de conferência do topo.)*
      **✅ SUCESSOR ABERTO em 2026-08-03 pela Missão 1.13.14 —** [`ADR-0037`](../decisions/ADR-0037-sucessor-parcial-do-framework-de-skills.md),
      `ativo`, **`C2 · Tipo 2`, `0` atos**, **`4` artefatos** *(`5 − 1`: **não há ficha**, porque
      nenhuma `Skill` foi criada — e o rito da classe veio **inteiro** assim mesmo)*.
      **Supera `ADR-0033` PARCIALMENTE**, pelo resultado **`AJUSTAR`** de `FND-07 §8.1` — a norma
      **nomeia** o instrumento —, e institui **`SK-27` a `SK-30`**. **`ADR-0033` fica `ativo`, com
      `0` bytes e `22` regras vigentes**; o Framework passa a **`30` regras em DUAS sedes**.
      ⚠️ **Quem ler `ADR-0033` isolado lê `SK-09` defeituosa SEM AVISO** — `CC-01` proíbe o aviso
      dentro dele, e **o lugar canônico do aviso é o catálogo mestre**, por ser `M3`.
      ⭐ **A regra geral foi VARRIDA, não amostrada: a classe tem `5` membros, não `2`** — e a
      varredura produziu **três achados que exercício nenhum produziria**: **`SK-25` jamais fora
      medida por missão alguma**, **`SK-21 (a)` estava classificada errado** *(de *"vacuamente
      satisfeita"* para **`INAPLICÁVEL`**)*, e **`SK-22` foi exercida exatamente no seu piso
      `n = 2`** — corroboração encontrada **ao varrer**, nunca procurada.
      ⚠️ **`GO-TO-SKILLS` continua NÃO liberado, e desta vez nem foi exercido: `0` `Skill`s criadas.**
      ⚖️ **DECIDIDO pelo Fundador em 2026-08-03, e a pergunta `Q1` FECHA:** ⛔ **NÃO promover
      `ADR-0033` e `ADR-0037` a `FND` agora** — as `30` regras seguem **determinadas e não
      observadas**, e são **três instâncias, não população**. Canonizar antes do exercício
      repetiria **`L1` de `FND-11 §14`** e **`RD-107`** — **o que `ADR-0021` pagou e `ADR-0022`
      desfez, a `1.328` linhas de retrabalho**. **Custo aceito e registrado:** o Framework fica
      em **duas sedes**, e o aviso sobre `SK-09` vive **no catálogo mestre**, suficiente
      **enquanto os consumidores forem o Fundador e as sessões, que leem o catálogo**.
      **Gatilho de reavaliação, escrito:** promover quando **(a)** as `30` regras tiverem
      **população e não amostra** *(piso a definir na própria promoção, coerente com `SK-27`)*,
      **ou (b)** surgir **consumidor que leia `ADR-0033` isolado, sem passar pelo catálogo** —
      **o que ocorrer primeiro**. **Decisão de registro: `0` `ADR`, `0` atos, `0` baseline nova.**

- [!] **1.16 — Workflow Framework.** Estados, transições, papéis, handoffs,
      Pacotes de Contexto, portões, impedimentos, retries, timeouts,
      compensação, rollback, escalonamento, incidentes, intervenção humana,
      ato soberano, evidências, memória, retomada. Depende de papéis, nunca de
      nome de agente.
      → saída: GO-TO-AGENTS
      **✍️ CANDIDATO REDIGIDO em 2026-08-02 — `WF-01` a `WF-30`, 249 linhas, fora do
      acervo.** Entidade `WFL`, template de 11 seções, `Pacote de Contexto` (`FND-05 §5`)
      e `HO-01`–`HO-05` **já existem e são recebidos**. **Lacuna medida com controle
      positivo:** `retry`, `timeout`, `compensação` e `retomada` têm **`0`** ocorrências no
      acervo normativo, contra **9** de `escalonamento` e `impedimento` na mesma execução.
      **23 de 30 regras são recepção; 7 são contribuição própria.** ✅ **`WFL` nunca exige
      ratificação** (`FND-09 §8.2`, coluna *Ratifica* = `—`) e `FND-04 §6` **não exige
      "Produto existe"**: junto com `SKL`, **é o componente mais barato do acervo**.
      **Espera decisão do Fundador.**

- [!] **1.14 — Tool & Model Framework.** Model é classe de Ferramenta; sem
      framework paralelo. Registro, permissões, classificação de dados,
      isolamento, sandbox, credenciais, custo, limites de contexto, seleção,
      fallback, roteamento, falhas, observabilidade, versionamento,
      autorização por Departamento/Skill/Agente. Sem integrar provedor ainda.
      → saída: GO-TO-COMMANDS
      **✍️ CANDIDATO REDIGIDO em 2026-08-02 — `TF-01` a `TF-32`, 347 linhas, fora do
      acervo.** A premissa do Goal **já era norma vigente**: `FND-03 §3.12` traz a classe
      `modelo` desde `ADR-0003`, que **absorveu a candidata `Model`**. Entidade `TOL`,
      template e local canônico **já existem** — o candidato **recebe** os três e cria
      `0` entidades. **Rito projetado `C2 · Tipo 2`, sem ato.** ⚠️ **Mas a ADOÇÃO de cada
      Ferramenta é `C2 · Tipo 1` com ratificação do SOBERANO** (`FND-04 §6`,
      `FND-09 §8.2`): **a norma não custa ato; toda Ferramenta que ela governar custará.**
      **Espera decisão do Fundador** — admitir, manter fora do acervo, ou descartar.

- [!] **1.17 — Agent Framework.** Identidade, Departamento, Capabilities,
      missão, autoridade, limites, Specs, Skills, ferramentas, Commands,
      Workflows, memória, contexto, autonomia, escalonamento, avaliação, custo,
      ciclo de vida. Regra central: um agente não escreve, revisa e aprova
      sozinho o mesmo objeto com impacto relevante. **Somente dois pilotos
      contrastantes** — um de execução, um de guarda.
      → saída: GO-TO-EVALUATION
      **✍️ CANDIDATO REDIGIDO em 2026-08-02 — `AR-01` a `AR-30`, 270 linhas, fora do
      acervo.** **26 de 30 regras são recepção — a maior proporção dos quatro**, porque o
      Agente é o componente **mais especificado e menos existente** do acervo: tem ficha
      completa (`FND-09 §E-11`/`E-12`), 4 níveis de autonomia, template de 14 seções e
      relações permitidas — e **`0` instâncias**. **A regra que o roadmap chama de central
      NÃO existia enunciada para o agente:** vivia espalhada em `LV-03`, `AC-03`, `PI-05` e
      `RM-06b`, *"sempre para artefato, aprovação, instrumento ou DEP-GOV — nunca para o
      agente"*. O candidato a enuncia em `AR-16`–`AR-19`, **derivando-a, sem criar norma**.
      ⚠️ **Achado `AA-1`, medido:** `A1`/`A2`/`A3` designam **níveis de autonomia** em
      `FND-01 §7.2` e **arquétipos** em `FND-09 §4` — e **as duas famílias aparecem no
      mesmo cartão `E-11`**. **Espera decisão do Fundador.**

- [!] **1.18 — Execution & Evaluation Framework.** Distingue atividade
      executada ≠ resultado declarado ≠ comprovado ≠ aprovado. Registro,
      evidência, testes, avaliações determinísticas e por modelo, revisão
      humana, independência, scorecards, aceite, erro, incidente, custo,
      tokens, tempo, reprodução, rollback, aprendizado, promoção para memória.
      Primeiro conjunto de Golden Tests.
      → saída: GO-TO-VERTICAL-PROOF
      **✍️ CANDIDATO REDIGIDO em 2026-08-02 — `EA-01` a `EA-28`, 259 linhas, fora do
      acervo — sob RESSALVA ESTREITA e cumprida.** `FND-10 §4.8` **recusa `Evaluation`**
      *("nome guarda-chuva de Fitness Check e Revisão Arquitetural")*, e é **a única das 6
      recusas cujo gatilho de reabertura é `—`** *(achado `AE-1`)*. O candidato **não cria
      o tipo**: escreve sobre `FIT`, `QG-3`, o `DoD` e os critérios de aceite da `Spec`,
      todos vigentes. **Medido antes de escrever:** `declarado` **27** × `comprovado`
      **5** — *a norma fala cinco vezes mais em declarar do que em comprovar* —, contra
      **`0`** de `scorecard`, `determinístic*` e `golden`, este com sua **única** ocorrência
      no **próprio roadmap**. **Eleva a regra três práticas que as missões já exercem e
      nenhuma norma escreve:** controle positivo antes de crer no zero, prova por reversão,
      e reprodução em duas execuções. **21 de 28 são recepção.** **Espera decisão do
      Fundador.**

> **Abaixo, o que NÃO está na cadeia ordenada.** Não é atraso nem despriorização: é
> **dependência de fato**. Nenhum ato do Fundador os move.

- [ ] **1.15 — Command Framework.** Identidade, propósito, parâmetros,
      autorizações, pré-condições, validação, comportamento, resultado, falhas,
      idempotência, dry-run, impacto, auditoria, workflow acionado, evidências,
      interrupção. **Apenas comandos-piloto.**
      → saída: GO-TO-WORKFLOWS
      **⛔ BLOQUEADO por norma vigente, e não por falta de decisão.** `FND-10 §4.8` —
      *"Tipos recusados"* — **recusa `Command`**: *"forma de acionamento, não artefato"*,
      que **vive no atributo `gatilho` de `SKL`/`WFL`*. `CMD` **não consta** das 21 linhas
      de `FND-09 §8.2`. Criar a entidade é **degrau 4**, `C3 · Tipo 1`, com os **7 testes
      `TE`** e rito de **9 passos**. **E `TE-7` exige *"sinal já observado"***, enquanto o
      gatilho de reabertura pede *"superfície com ciclo de vida independente do
      procedimento"* — **não há `SKL` nem `WFL` no acervo**, logo **não pode haver sinal**,
      e antecipar é recusado por `FND-08 §7.1`. **Nenhum ato do Fundador o destrava hoje:
      falta FATO, não autorização.** O caminho é Skills/Workflows primeiro.
      ⚠️ **DESATUALIZADO PELA MISSÃO 1.13.12, e corrigido aqui — a premissa caiu, a conclusão
      não.** Há **`2` `SKL`** no acervo desde 2026-08-03, de modo que *"não pode haver sinal"*
      **deixou de valer**: `SK-12` ganhou sua **primeira observação empírica**. **E ela mantém
      `Command` fechado, agora por observação e não por ausência:** existe consumidor com **`3`
      dos `4`** marcadores — identificador, citabilidade e autoridade próprios —, **e a superfície
      vive NO CONSUMIDOR, fora do acervo**, que é onde `SK-12` manda que fique. **O gatilho de
      reabertura de `FND-10 §4.8` segue NÃO satisfeito**, e o que mudou é a qualidade da prova.
      **✍️ O evento que o destrava está NOMEADO** — `SK-12` do candidato de Skills: o dia em
      que um `gatilho` precisar de versão, identificador, citação externa ou autoridade
      próprios, **a "superfície com ciclo de vida independente" existirá** e o gatilho de
      reabertura estará satisfeito. ⚠️ **Mas hoje o `gatilho` nem sequer cabe em `SKL`:**
      `TPL-skill` tem **`0`** ocorrências dele contra **1** de `TPL-workflow` — achado
      **`AS-1`**, e é `C2` de `TPL`, **sem ato**.

- [ ] **1.19 — Primeira Prova Vertical Organizacional.** Missão real, pequena,
      controlada, atravessando: Objetivo → Capability → Departamento → Spec →
      Skill → Ferramenta → Command → Workflow → Agentes → Execução → Avaliação
      → Evidência → Decisão → Memória → Aprendizado.
      **Organizacional e rastreável, ainda sem plataforma técnica completa.**
      Saídas: PASS · ADJUST · FAIL · STOP. Só PASS encerra o Épico 1.
      **⛔ BLOQUEADO por dependência de FATO, medido em 2026-08-02.** A cadeia atravessa
      dez elos, e **cinco não têm instância nenhuma**: `0` Skills · `0` Ferramentas ·
      `0` Workflows · `0` Agentes · `0` Commands. **Não é redigível como candidato** — é
      execução, e execução exige os componentes existindo. **Nenhum candidato a substitui,
      e escrever um seria confundir norma com prova.**
      ⚠️ **REMEDIDO em 2026-08-03, ao fechar a Missão 1.13.12: são QUATRO elos sem instância,
      não cinco.** **`2` Skills existem** — `SKL-custodia-criar-copia-datada` e
      `SKL-seguranca-varrer-credencial` —, e seguem em `0` **Ferramentas · Workflows · Agentes ·
      Commands**. **O item continua ⛔ BLOQUEADO**: um elo instanciado de dez não é a cadeia, e
      `PASS` exige atravessá-la inteira.
      ⚠️ **REMEDIDO de novo em 2026-08-03, ao fechar a Missão 1.13.13: são `3` Skills**, com
      [`SKL-custodia-provar-restauracao-de-backup`](../skills/SKL-custodia-provar-restauracao-de-backup.md).
      **Os quatro elos sem instância NÃO mudam**, e o item **continua ⛔ BLOQUEADO** — **contar
      Skills não move o bloqueio, e é por isso que o número é remedido em vez de comemorado.**
      **Medição que o item ganha e que vale mais que a contagem:** as três Skills são
      **mutuamente independentes** — `0` dependências entre componentes —, de modo que **a
      cadeia de `1.19` não tem nem sequer a primeira aresta**, e é isso, não o número de elos,
      que a torna não atravessável. É o mesmo achado que deixa **`SK-21` (b) não exercida**.

---

## ÉPICO 2 — Kernel Técnico
*Numeração provisória: deve incorporar o que a prova vertical demonstrar.*

- [ ] 2.1 Arquitetura Técnica de Referência
- [ ] 2.2 Fundação de Engenharia e Repositório
- [ ] 2.3 Identity, Registry e Policy Engine
- [ ] 2.4 Runtime de Memória e Conhecimento
- [ ] 2.5 Tool Gateway e Model Router
- [ ] 2.6 Command Bus e Workflow Runtime
- [ ] 2.7 Agent Runtime e Isolamento
- [ ] 2.8 Evidence, Evaluation e Audit Ledger
- [ ] 2.9 Observabilidade, Segurança e Custos
- [ ] 2.10 Confiabilidade, Backup, Restore e Release
- [ ] 2.11 Prova Vertical Automatizada do Kernel v0.1

---

## ÉPICO 3 — Empresa Virtualizada v0.1
*Sem Goals recortados.*

- [ ] Entrada e classificação de missões
- [ ] Controle executivo
- [ ] Portfólio e priorização
- [ ] Conjunto mínimo de agentes reais
- [ ] Operação dos Departamentos
- [ ] Planejamento e handoffs
- [ ] Gates de qualidade e segurança
- [ ] Memória e aprendizado organizacional
- [ ] Prova operacional da empresa

---

## ÉPICO 4 — Fábrica de Produtos Premium
*Sem Goals recortados.*

- [ ] Product Operating Model
- [ ] Discovery
- [ ] Pesquisa e evidências
- [ ] PRD e roadmap
- [ ] UX e Design System
- [ ] Engenharia
- [ ] Qualidade
- [ ] Segurança
- [ ] Release
- [ ] Growth
- [ ] Customer feedback
- [ ] Workspace canônico de produto
- [ ] Primeiro produto greenfield completo

---

## ÉPICO 5 — Engenharia Reversa do LucaX Legacy
*Somente depois de o novo sistema provar que funciona.*

- [ ] Firewall de migração
- [ ] Inventário somente leitura
- [ ] Proveniência
- [ ] Mapeamento para Capabilities
- [ ] Extração de agentes, skills, specs e conhecimento
- [ ] Fit-gap
- [ ] ADOPT / ADAPT / REWRITE / RETIRE
- [ ] Migração-piloto
- [ ] Validação
- [ ] Migração por ondas
- [ ] Cutover
- [ ] Retirada do legado

---

## TRILHA PARALELA — SSC+
*Laboratório experimental. Sem autoridade normativa. Não bloqueia o Épico 1.*

> **Não conferido contra o catálogo em 2026-08-01, e a razão foi medida.** O SSC+
> vive fora do acervo *(`E:/LucasIA/Projetos/SSC-Plus`)*, e o acervo o menciona
> **23 vezes em 8 artefatos — sempre para declarar que ele NÃO é acervo**
> *(`ADR-0029`, `RFC-0024`, `FIT-2026-016`, `FIT-2026-022`, `atos-superados`,
> `PT-2026-008`, `PT-2026-012`, `MSG-2026-0008`)*. **Nenhum registra os itens
> desta trilha:** `P0`, `P1-A`, `P1-B` e `SHADOW_ELIGIBLE` têm **`0`** ocorrências.
> *(Cuidado com homônimo: `D8` aparece **12 vezes em 4 artefatos**, mas como
> **identificador de achado** de revisão arquitetural — não é o `D8` desta
> trilha.)* As marcações abaixo valem pela fonte da própria trilha, não por este
> catálogo. A integração só entra por **Goal canônico**.

- [x] P0, P1-A, P1-A.3 — kernel provado em bancada
- [x] P1-A.3.2 — seis MAJOR corrigidos
- [x] P1-A.3.5 — varredura de 86 guardas; EXERCE 49→64, SEM-TESTE 26→13
- [x] P1-B.01 — runner entra pela cápsula ratificada
- [x] P1-B.02 — codex e kimi em SHADOW_ELIGIBLE
- [ ] P1-A.3.6 — revisão dupla independente (janela de tier: 2026-08-02T23:54:41Z)
- [ ] P1-B-02 — shadow observacional, sem invocação
- [ ] Relatório de qualidade do roteador · P2-DESIGN
- [ ] P2 read-only · P3-DESIGN · P3 worktree
- [ ] D8 — integração ao canônico por Goal canônico

---

## ACHADO REGISTRADO — sem número `RD`, e a razão está escrita

> **Registrado por determinação do Fundador em 2026-08-03, ao decidir sobre a promoção a `FND`.**
> **Não gera missão. Deliberadamente NÃO corrigido.**

### `superado_por` é `null` em `37` de `37`, e os superados seguem `ativo`

| O que foi medido, na varredura da Missão 1.13.14 | Resultado |
|---|---|
| Campo `supera:` nos `37` `ADR` | **`28` vazios · `6` `null` · `3` preenchidos** |
| Quais | `ADR-0022` supera `ADR-0021` · `ADR-0027` supera `ADR-0007` · `ADR-0037` supera `ADR-0033` |
| Campo `superado_por:` preenchido | **`0` de `37`** |
| `status` dos três superados | ⭐ **`ativo` nos três** |

**O achado não é o valor: é que ninguém o tinha medido.** **O acervo já praticava a construção de
superação PARCIAL — superado vigente, espelho fora do frontmatter — DUAS vezes antes de
`ADR-0037`**, e as duas passaram sem que qualquer missão registrasse que era isso que estava
acontecendo. **`ADR-0037` foi construído por leitura de norma** *(`FND-07 §8.1` `AJUSTAR` +
`LN-01`)*, **e só depois descobriu que repetia prática já existente.**

⛔ **NÃO CORRIGIR, por determinação expressa:** **a construção FUNCIONA** — é o que `LN-01`
prescreve, porque `supera` é `R-08` e **não** está na exceção de `LN-02` —, e **alterar `37`
frontmatters sem necessidade é custo sem ganho**.

| Campo | Conteúdo |
|---|---|
| **Dono** | **DEP-GOV** |
| **Gatilho** | O **primeiro** consumidor que precise ler a sucessão **a partir do artefato superado**, sem passar pelo catálogo mestre — **o mesmo gatilho `(b)` da decisão sobre a promoção**, e não por coincidência: as duas dependem de haver leitor que não passe pelo `M3` |
| **Se o gatilho disparar** | Aí `LN-08` incide *(*"artefato cuja substituição não seja legível a partir dele próprio quebra a cadeia"*)*, e o remédio passa a ter ganho. **Antes disso, não tem** |
| ⚠️ **Por que SEM número `RD`** | Inscrever no catálogo mestre exigiria **escrever em artefato**, e a seção `§10.29` — que publica os quatro valores de `BL-2026-08-03-06` — **mora dentro do próprio `artifact-registry.md`**. Editá-lo depois da medição deixaria o artefato **afirmando valores que já não reproduzem**: família de **`RD-101`**. **O despacho é de registro, sem baseline nova**, e por isso o achado vive aqui. **Precedente:** `fencing_token` 14 registrou dois candidatos no roadmap, **não** no catálogo |

---

## DIVERGÊNCIA REGISTRADA

**Conferida contra o acervo em 2026-08-01. O enunciado anterior não se
sustentou, e está corrigido abaixo com o que foi medido.**

**Enunciado anterior:** *"A tabela de roteamento do relatório de transição põe
Skills em 1.14. Um dos dois documentos está desatualizado."*

**O que a varredura encontrou:**

| Verificação | Resultado |
|---|---|
| Tabela de roteamento que numere Goals nos relatórios de transição | **Não existe.** `0` ocorrências em `governance/relatorio-transicao-*.md` |
| Artefato que atribua **1.14** a Skills | **Nenhum.** As ocorrências de `1.14` no acervo são de **versão de documento**, não de Goal |
| Numeração de Goals acima de 1.13.5 em qualquer artefato | **`0`.** A sequência 1.14–1.19 existe **só aqui** |
| `GO-TO-SKILLS` no acervo | **4 artefatos** — `FIT-2026-015`, `FIT-2026-016`, `PT-2026-007` e `PT-2026-008` — nomeiam-no **"o portão seguinte"** a `GO-TO-SPECS`, **sem lhe atribuir número de Goal** |

**A divergência real, e é outra.** Não há dois documentos discordando sobre um
número: há **um portão nomeado quatro vezes no acervo — `GO-TO-SKILLS` — que
esta sequência não menciona**. Ela vai de 1.13.5 direto a Tool & Model.

**⚠️ ATUALIZADO em 2026-08-02: o fundamento CAIU, e a divergência continua.** DEP-QAR recusou
`GO-TO-SKILLS` com fundamento que **valia até 2026-08-01**: *"o portão anterior foi liberado e
não pode ser exercido"* (`FIT-2026-015`). **Agora ele foi exercido** — `SPC-001` existe, e
`GO-TO-SPECS` está `EXERCIDO`. **Isso não libera `GO-TO-SKILLS` por consequência:** cair o
fundamento de uma recusa não é o mesmo que haver fundamento para liberar, e liberar portão não
é ato deste registro. **Enunciado histórico, preservado:** *"ele segue sem ser exercido, porque
`0` Specs existem"* — verdadeiro até 2026-08-01, falso a partir de 2026-08-02. Se Skills entra como Goal próprio, onde, e se `GO-TO-SKILLS` foi
absorvido por 1.14 ou apenas não escrito: **é decisão do Fundador, não
reconciliação de registro.** Fica aqui, sem gerar missão.

**✅ RESOLVIDA em 2026-08-03 — o Fundador DECIDIU, e a decisão é dupla.** *(Enunciado anterior,
preservado acima: "é decisão do Fundador, não reconciliação de registro".)*

**(a) `Skills` entra como Goal próprio, ANTES de 1.14.** **(b) `GO-TO-SKILLS` é o portão
seguinte a `GO-TO-SPECS`.** A cadeia passa a ser **Skills → 1.16 → 1.14 → 1.17 → 1.18**, e a
*Sequência* já está assinalada nela.

**O fundamento é do Fundador e fica escrito, com os números que o sustentam:**

| O que fundamenta | Medida |
|---|---|
| **Custo** — a ordem anterior começava pelo mais caro | `Ferramenta` é o **único** dos quatro que é **`C2 · Tipo 1`** e **ratifica SEMPRE**; `Skill` e `Workflow` têm *Ratifica* = **`—`** em `FND-09 §8.2`, **nunca** exigem ato. *"Começar pelo mais caro invertia custo e sinal"* |
| **Sinal** — a ordem anterior não destravava `1.15` | `TE-7` exige **sinal observado**, e o gatilho de `FND-10 §4.8` pede *"superfície com ciclo de vida independente do procedimento"*. **Só `SKL`/`WFL` existindo o produzem.** Fazer Tool primeiro **não** gera o sinal |
| **Precedente** — o acervo já dizia isso | **4 artefatos** nomeiam `GO-TO-SKILLS` *"o portão seguinte"*: `FIT-2026-015`, `FIT-2026-016`, **`PT-2026-007`** e **`PT-2026-008`** — os dois últimos conferidos por `id` no frontmatter |

**A frase que reposiciona a culpa, e é do Fundador:** ***"a sequência canônica é que estava
desalinhada, não os quatro artefatos"***. O que `FIT-2026-015` recusou em 2026-07-29 foi
**prematuridade** — *"o portão anterior foi liberado e não pode ser exercido"* —, e essa
condição **caiu em 2026-08-02**, quando `SPC-001` passou `GO-TO-SPECS` a `EXERCIDO`.

**O que esta resolução NÃO faz.** Não atribui **número de Goal** a Skills — o despacho escreveu
número para `1.16`, `1.14`, `1.17` e `1.18` e **não** para Skills, e atribuir um seria decidir o
que ele não decidiu; **`RD-124` fecha quanto à posição e ao portão e segue ABERTO quanto ao
número**. Não **exerce** nem **libera** `GO-TO-SKILLS` — liberar portão é ato de autoridade
(`FND-01 §6.2`), e este registro não é autoridade. Não **admite** o candidato de Skills, que
segue fora do acervo. Não move `1.15`: *"não espera decisão, espera sinal observado"*. **`0`
missões abertas.**

### ⚖️ DECISÃO REGISTRADA — `RD-116` fica ABERTO por determinação, e o adiamento tem motivo

**`RD-116`** *(catálogo §7, item 139)* é a colisão de notação `A1`/`A2`/`A3` — **níveis de
autonomia** em `FND-01 §7.2` × **arquétipos** em `FND-09 §4` —, agravada por as duas famílias
aparecerem **no mesmo cartão `E-11`**. É o único dos 17 achados inscritos cujo risco de leitura
recai sobre **autonomia**, e a leitura errada cairia em `LV-07`.

**O Fundador decidiu NÃO sanar agora, em 2026-08-03, e o fundamento é de economia de ato:**
sanar exige emendar `FND-01` ou `FND-09` — **Fundacional, `C3` COM ATO** —, e *"o ato deve
carregar também o que a admissão dos candidatos revelar"*. **Gastar um ato `C3` numa correção
isolada, sabendo que outras virão da admissão, seria pagar duas vezes o mesmo rito.**

**Estado: ABERTO, dono `DEP-GOV`, gatilho — *"próxima emenda Fundacional"*.** **`0` bytes em
`FND-01` e `FND-09`. Sem missão.** A mitigação vigente continua sendo a linha *Autoridade* do
próprio cartão `E-11`, que declara o teto *"igual ou inferior à do departamento, nunca
superior"* — **mitiga, não sana**, e é por isso que o achado permanece.

### ⚠️ SEGUNDA DIVERGÊNCIA — medida em 2026-08-02 pela 1.13.5.1: o alcance de `RD-91`

**`RD-91` foi registrado como defeito de `FND-11 §5`. A medição do Item 0 mostra que a
sede é outra, e que o defeito não é só de `SPC`.** Nada disso reabre `SPC-001`, que nasceu
em `C2` validamente.

| O que se mediu | Resultado |
|---|---|
| Células de `SF-10` em que *Proposta* e *Aprovação* colapsam | **2 de 50** — `Aprovação` em `C0 · T2` e em `C1 · T2`. As colunas `C2 · T2`, `C2 · T1` e `C3` **não colapsam** |
| A célula `C1 · T2` tem mérito próprio? | **Não.** *Proposta* = `DEP-PRD` reproduz `FND-09 §8.2` linha `SPC`; *Aprovação* = `proprietário + revisor` reproduz `FND-04 §2.1` linha `C1`. **Reprodução literal nas duas** |
| Emenda confinada a `FND-11 §5` sana? | **Não.** `FND-11 §5` é projeção declarada (`PJ-02`); `PJ-03` — *"em divergência, a fonte prevalece, e o defeito é da projeção"* — e `FND-01 §10` — *"sobre autoridade, prevalece sempre o documento de origem"* |
| O mesmo defeito além de `SPC`, em `FND-09 §8.2` | **2 linhas.** **`PRJ`**: propõe `DEP-EXE`, aprova `DEP-EXE` — incondicional, sem leitura salvadora. **`TPL`**: propõe `DEP-GOV + dono do tipo`, revisa `DEP-GOV`, aprova `DEP-GOV` — três papéis, e fere também `ES-02` |
| Precedente que já decidiu parte disto | **`ADR-0019`, `C3`, `ratificada`.** §4 recusou a **Alternativa B** *(fixar `DEP-EXE` na célula Aprova)* por `K3`, e a **Alternativa D** *(emendar `FND-04 §6`)* por `K6` — esta virou **`RD-18`, aberto**, gatilho *"próxima emenda a `FND-04`"*. §4 **Alternativa E** fixou que fonte e projeções mudam **na mesma mudança** |

**Os três números de custo, medidos:** a primeira `Spec` em `C2` custou **5** artefatos
*(1.580 linhas)*; a segunda em `C1`, depois de sanado, custa **2** instrumentos de rito —
**3** contando o registro de missão, que é a regra pela qual os 5 foram contados; e **a
própria emenda custa 7** *(RFC, ADR, FIT, PS, PT do instrumento, MSG do ato, PT da aplicação)*,
medidos na cadeia idêntica de `PS-2026-008` → `MSG-2026-0006`, mais **2 fundacionais e
2 Cartas** emendadas.

**✅ RESOLVIDA quanto ao instrumento em 2026-08-02, pela 1.13.5.1.** O Fundador reafirmou o
menu depois de ler este relatório de alcance, e a emenda foi redigida **no escopo mais estreito
que a medição prova eficaz**: `FND-09 §8.2` linha `SPC` + cascata em `FND-11 §5` + as 2 Cartas.
`ADR-0032`, **`ativo` · `ratificada` desde 2026-08-02** — ratificado pelo item I do décimo ato e
**APLICADO pela 1.13.5.2**. *(Enunciado anterior, preservado: `aprovado` · `ratificação:
pendente`.)*

**✅ AS TRÊS QUESTÕES DE `PS-2026-017 §7`, RESPONDIDAS por despacho de 2026-08-02.** A
**largura** (`Q1`): **ESTENDE** a `PRJ` e `TPL`, com **missão própria ordenada** para redigir —
o Fundador acolheu a ressalva `S2` de `FIT-2026-025` **contra** a recomendação de `RFC-0027 §6`,
porque `TPL` tem **19** objetos vivos contra **1** de `SPC`, e o defeito com mais efeito real
ficaria de fora. `RD-96` e `RD-97` seguem **abertos** até o ato próprio que os sanar; a missão
ordenada **redige, não aplica**, e **não começa antes** de concluída a aplicação ministerial.
**`C0 · T2`** (`Q2`): **permanece declarado** em `RD-91`, sem emenda — `RD-91` fecha **só quanto
a `C1`**. O conflito *versão MAIOR × `AL-01`* (`Q3`, `RD-99`): prevalece a **prática exercida**,
incremento **MENOR**, decidido em **item VIII** criado para o ato não decidir `Q3` pelos números
de versão, calando — `RD-99` segue **aberto**, com o gatilho de `RD-18`. **A minuta foi
reemitida em `PS-2026-017` `1.2.0` — segundo despacho, 4 determinações, 3 cumpridas e 1
impossível — e continua NÃO assinada.** Fica aqui, **sem gerar missão**.

**⚖️ SUPERADO EM 2026-08-02: a minuta FOI assinada, e o ato FOI aplicado.** A reemissão `1.3.0`
recebeu o **ato dos itens I a VIII**, registrado em `MSG-2026-0010`, e a **Missão 1.13.5.2**
executou o **item VI**. O que muda nesta divergência, e só isto: **`RD-91` FECHA quanto a
`C1`** — a coluna que produzia aprovação nula está sanada e o piso de `FND-04 §6` voltou a ser
utilizável —, e **`C0 · T2` PERMANECE ABERTA**, exatamente como `Q2` decidiu. **`Q1` e `Q3` não
se movem:** `RD-96`, `RD-97` e `RD-99` seguem **abertos**, e a missão ordenada pelo item IV
**continua não aberta** — o item VII a condiciona à conferência desta aplicação, que é do
Fundador.

### ⚠️ TERCEIRA DIVERGÊNCIA — medida em 2026-08-02 pela missão A3.03 (Agente 3): a **sede** do `PRO-nxtrack` mudou

**O `nXtrack` passou a ter repositório git próprio.** Foi extraído de
`lucaX/My_WorkSpace/Meus_projetos/nxtrack` para `E:/LucasIA/Projetos/nxtrack` por
`git subtree split`, com histórico preservado, em 2026-08-02. **Nada foi removido do `lucaX`**, e
**nada do acervo foi corrigido pela missão que mediu** — a reconciliação é do Agente 1, e o
registro de proveniência completo está em `nxtrack/docs/separacao-do-lucax-2026-08-02.md`,
fora do acervo.

| O que se mediu | Resultado |
|---|---|
| Âncora de **conteúdo** do ato `MSG-2026-0009` | ✅ **Intacta.** `tree` = `b9b36be9…fb4b` no destino, **idêntico** ao publicado em `PRO-nxtrack §14`, `PT-2026-014 §5` e `CD-3`. E **idêntico ao `tree` no próprio commit do ato** (`b9fbccd…3bcb`): `0` commits tocaram o candidato entre o ato e a extração |
| Equivalência byte a byte | ✅ **183 de 183** arquivos com `sha256` idêntico ao blob de origem lido do commit, mais lista de nomes conferida nos dois sentidos. **Controle positivo rodado** — o mesmo comparador acusou divergência num arquivo com 1 byte alterado |
| Âncora de **caminho** | ⚠️ **Deixou de localizar a sede.** `3` ocorrências textuais de `My_WorkSpace/Meus_projetos/<produto>` no acervo — `artifact-registry.md` linhas **1002** e **1991**, e `relatorio-transicao-2026-08-02-primeira-spec.md` linha **265**. Hoje apontam para pasta que existe mas **não é mais a sede**; depois da remoção, para caminho inexistente |
| Fundamento de fato de **`RD-71`** | ⚠️ **Mudou.** `RD-71` (Alta, **ABERTO**, dono `DEP-PRD`) enuncia *"NÃO tem repositório próprio: é subárvore de `lucaX`"* e funda-se em *"não há fronteira de custódia separável do hospedeiro"*. **A fronteira separável agora existe.** O achado **não é fechado aqui** — tem dono e gatilho próprios, e fechá-lo é ato de quem o abriu |
| Hospedeiro `lucaX` | `444` caminhos sem commit, medidos em 2026-08-02 — contra os **`758`** que `RD-71` registrou em 2026-08-01. Número diferente, **sem conclusão tirada aqui** |

**O que a distinção acima protege.** A leitura apressada é *"a sede mudou, logo a admissão
precisa ser refeita"*. A medição diz o contrário: **o conteúdo admitido é bit a bit o conteúdo
extraído**, e a âncora que o acervo publicou **11 vezes** — o `tree` — sobrevive intacta à
mudança de sede, porque `tree` não codifica caminho de hospedeiro. O que caducou é só a
**localização**. Tratar tudo como invalidado jogaria fora uma âncora que continua reproduzindo.

**⚠️ O que a extração NÃO levou, e é a parte que pode custar caro.** Extração move histórico
git; o que o git não rastreia, ela não leva. Os **13.182 ignorados** que `artifact-registry.md`
§1002 registra como *"não consumidos"* **continuam existindo em um lugar só**: a pasta antiga.
Entre eles, `nxtrack.db` *(4,7 MB)*, `data/` com o snapshot de 420 sets *(9,2 MB)* e `backups/`
*(88 MB)*. **A separação está provada para o código; para o dado, não foi tentada.** Removida a
pasta antiga sem decidir o destino desses arquivos, apaga-se o exemplar único.

**Pendências nomeadas, ambas ato do Fundador:** `PENDÊNCIA-A3.03-nxtrack` e
`PENDÊNCIA-A3.03-consult` — remoção das pastas antigas, **depois** de resolvido o dado acima.
Fica aqui, **sem gerar missão**.

### ⚠️ QUARTA DIVERGÊNCIA — medida em 2026-08-02 pela 1.13.6: o piso `C1` está utilizável e **não pode ser exercido** sobre o `consult`

**O rito barateado funciona. A primeira matéria que ele encontrou não é elegível.** As duas
afirmações são independentes, e confundi-las leria como falha da emenda o que é pré-condição
anterior a ela.

| O que se mediu | Resultado |
|---|---|
| O rito exigido para esta `Spec` sai em `C1`? | **Sim.** `FND-09 §8.2` **1.6.0** linha `SPC`: aprova **DEP-EXE** em `C1`; ratifica só se `C3` ou `Tipo 1`. `FND-11 §5` **1.1.0**, célula `C1 · T2`: `DEP-EXE + revisor`, ratificação *não exigida*. `FND-04 §2`: **Nota de Decisão**, sem `RFC` e sem `ADR`. `FND-09 §10.2`: `FIT` **opcional** em `C1` |
| A classe do efeito é mesmo `C1 · Tipo 2`? | **Sim.** Fixa **como** se julga uma rodada, dentro de escopo já aprovado; não cria componente, não move fronteira, não altera direito de decisão (`AL-01`, piso `C1` de `FND-04 §6`). Nada executado — `Tipo 2`, e `FND-04 §2.2` não escala |
| Então por que a `Spec` não nasceu? | **`PRO-consult` não existe.** `FND-04 §6` linha *Spec* exige ***"Produto existe"*** e diz *"todas precisam ser verdadeiras para a criação ser aprovada"*; `SF-23` item **(9)** repete a exigência no `DoR`; `FND-03 §3.6` e `FND-10 §4.4` alojam a `Spec` em `products/<slug>/specs/`, e **não há caminho canônico** fora de um produto |
| Isto é `RD-88`? | **Não.** `RD-88` é a `Spec` sobre matéria **não-produto**, que só `S2` cria. A matéria do `consult` **é de produto** — falta o **Produto**. O caminho é `S1` exercida uma segunda vez |
| As saídas fáceis foram tentadas? | **Não, e o motivo é anterior a esta missão:** `ADR-0021 §7.3` já as recusou com norma citada — escrever em outro diretório (`MT-01`, `FND-03 §7.1`) ou criar `products/` (`LV-06`, `LV-07`) |
| Quanto custa desbloquear | **8 artefatos · 2.338 linhas**, medidos por `wc -l` no precedente `nxtrack`: `RFC-0025` 187 · `ADR-0030` 340 · `FIT-2026-023` 151 · `PS-2026-016` 359 · `MSG-2026-0009` 375 · `PT-2026-014` 292 · `PT-2026-015` 371 · Carta `PRO` 263. Admitir Produto é **`C2 · Tipo 1` do SOBERANO**, com o portão de `ADR-0007 §5.3` |
| Condição de fato que mudou a favor | O `consult` **foi separado do `lucaX` em 2026-08-02** e tem repositório próprio. A **fronteira de custódia separável existe** — o mesmo fato que a TERCEIRA DIVERGÊNCIA registrou para o `nxtrack` |

**Os números de custo, medidos:** `SPC-001` custou **5** artefatos *(1.580 linhas)*; esta `Spec`
custou ao acervo **`0`** — nada foi criado, porque nada podia ser aprovado; se pudesse nascer,
custaria **2** *(a `Spec` + a Nota de Decisão)*. **O `2` contra `5` continua derivado da norma
e não observado**, exatamente como `ADR-0032 §8` já declarava. **O que esta missão mediu é
outra coisa:** o piso barato só entrega economia **onde já há Produto** — para matéria de
produto não admitido, a entrada custa **8** e a `Spec` de **2** vem depois.

**A matéria não se perdeu.** O critério da primeira rodada real do `consult` — o que a saída
deve conter, o que a invalida, tetos de custo, de tempo e de número de rodadas, e o que a
rodada **não** estabelece — foi redigido integralmente e vive **fora do acervo**, como
candidato de **348 linhas** e **13 requisitos**, em
`_missao-1-13-6-2026-08-02/CANDIDATO-spec-primeira-rodada-real-do-consult.md`, com o registro
em `RELATORIO-1.13.6.md`. **Mesma forma dos quatro candidatos de `PS-2026-017`**, que também
viveram fora do acervo até o ato. **`0` bytes em `products/`, `foundation/`, `decisions/` ou
`rfcs/`; `0` bytes no `consult`, lido em somente leitura; portão de chamada real **intocado**.

**Espera decisão do Fundador**, e são três: **(a)** admitir o `consult` como Produto, exercendo
`S1` uma segunda vez — **8** artefatos, e só depois a `Spec` de **2**; **(b)** manter o critério
fora do acervo, útil para operar a rodada, sem custo de rito e sem autoridade; **(c)** se `(b)`,
declarar se o candidato basta como critério operacional. Fica aqui, **sem gerar missão**.

**✅ RESOLVIDA em 2026-08-02 — o Fundador decidiu `(b)`, e o candidato BASTA.** `PRO-consult`
**não é admitido agora**. O critério de **348 linhas** fica **fora do acervo**, como **critério
operacional da primeira rodada real**, **sem autoridade canônica e sem custo de rito**.

**O fundamento é do Fundador e fica escrito:** *admitir custa 8 artefatos e 2.338 linhas para
governar um produto que ainda não provou que executa. Se a rodada reprovar, o acervo terá
admitido algo que não entrega. A ordem correta é rodar, medir, e só então decidir se vale
admitir.* **É a ordem inversa da que o acervo vinha praticando** — `PRO-nxtrack` e `PRO-medally`
foram admitidos **antes** de qualquer execução medida.

**`GATILHO-1.13.6-PRO-consult` — dois, alternativos, e nenhum admite sozinho:**
**(i)** a **primeira rodada real passar**; **(ii)** o `consult` passar a **exigir mais de uma
`Spec`**. Ocorrendo qualquer um, **reavaliar** a admissão. **Reavaliar não é admitir**, e a
admissão continua sendo **`C2 · Tipo 1` do SOBERANO**, com o portão de `ADR-0007 §5.3`.

**Registrado no catálogo, com dono e gatilho, e sem missão:** a decisão em **§2**, linha
`PRO-consult`; a medição em **§7, item 130 — `RD-107`** *(Média, dono **SOBERANO**, instrui
**DEP-PRD**, gatilho "toda decisão futura de admitir Produto")*: **o piso barateado só entrega
economia onde já há Produto — `8` de entrada `+ 2` por `Spec` = `10` na primeira, contra os `5`
de `SPC-001` com Produto já existente**, e o **`2` contra `5` segue derivado da norma e não
observado** (`ADR-0032 §8`). **A emenda não está em causa e não falhou:** ela barateou o
**segundo** passo; o **primeiro** continua caro e continua sendo do Soberano.

**O que esta decisão NÃO faz:** não reabre `RD-88`, não decide `C0`, não abre a missão do item
IV *(segue dependendo da conferência do Fundador — item VII de `MSG-2026-0010`)*, não levanta o
congelamento e **não abre missão nenhuma**. Escrito sob **`fencing_token` 20**.

### ⚠️ QUINTA DIVERGÊNCIA — medida em 2026-08-02 pela 1.14: **a ordem da Sequência custa mais do que a ordem que o próprio acervo nomeia**

**Não é divergência de opinião: é de preço, e o preço está em `FND-04 §6` e `FND-09 §8.2`.**
A **primeira DIVERGÊNCIA REGISTRADA** já reservou ao Fundador *"se Skills entra como Goal
próprio e onde"*. **Esta não a reabre — acrescenta o número que faltava para decidir.**

| Componente | Pré-condição de criação (`FND-04 §6`) | Classe | Ratificação (`FND-09 §8.2`) | Depende de Produto? |
|---|---|---|---|---|
| **Skill** | procedimento se repete; resultado verificável; usável por +1 papel | `C2` | **— (nunca)** | **não** |
| **Workflow** | gatilho, entradas, saídas, responsável por etapa, portões, critério de falha | `C2` | **— (nunca)** | **não** |
| **Agente** | departamento existe; escopo e não-escopo; autonomia ≤ do dep.; Capabilities; Carta | `C2` | SOBERANO **se `Tipo 1`** | **não** |
| **Ferramenta** | 7 pré-condições, incl. *Capabilities habilitadas* | **`C2 · Tipo 1`** | **SOBERANO — sempre** | **não** |
| **Spec** | ***Produto existe***; problema; critérios; escopo negativo | `C1` | — | **SIM** |

**Dois fatos que esta tabela torna visíveis, e nenhum estava escrito em lugar nenhum:**

**(a) A linha *Spec* é a ÚNICA com *"Produto existe"*.** O bloqueio que travou a 1.13.6 e
manteve `RD-33` aberto **não alcança Skill, Workflow, Agente nem Ferramenta.** Nenhum dos
quatro depende de Produto.

**(b) A Sequência ataca primeiro o componente mais caro.** `Ferramenta` é o único dos quatro
que é **`Tipo 1` e exige ato do Soberano sempre**; `Skill` e `Workflow` **nunca** exigem
ratificação.

| Ordem | Sequência | Componentes que exigem ato na 1ª instância |
|---|---|---|
| **Do roadmap** | Tool → Command → Workflow → Agent | **1.14 SIM** · 1.15 sim · 1.16 não · 1.17 talvez |
| **Do acervo** *(`GO-TO-SKILLS`, nomeado 4×)* | Skill → Workflow → Agent → Tool | **não** · não · talvez · sim |

**E a ordem do acervo destrava 1.15, que a do roadmap não destrava.** `1.15` exige **sinal
observado** (`TE-7`) de *"superfície com ciclo de vida independente do procedimento"* — sinal
que **só `SKL`/`WFL` existindo pode produzir**. **Fazer Tool primeiro não gera esse sinal;
fazer Skill primeiro gera.**

**Medição colateral, e ela reforça a primeira divergência:** dos **7** portões `GO-TO-*` que
esta Sequência nomeia, **apenas 2 existem no acervo normativo** — `GO-TO-SPECS` e
`GO-TO-SKILLS`. `GO-TO-COMMANDS`, `GO-TO-WORKFLOWS`, `GO-TO-AGENTS`, `GO-TO-EVALUATION` e
`GO-TO-VERTICAL-PROOF` têm **`0`** ocorrências fora deste arquivo. *(Cuidado com homônimo: os
**7** portões de `FND-01 §6.2` são `QG-0` a `QG-6` — família distinta, que não se soma a esta.)*

**Espera decisão do Fundador, e são quatro:** **(a)** a ordem da Sequência; **(b)** o destino
dos **5** candidatos — admitir por missão própria, manter fora do acervo como o critério da
1.13.6, ou descartar; **(c)** os **12** achados, dos quais **`AF-1`, `AF-2`, `AW-2` e `AS-1` são
`C2` de `TPL`, sem ato**, e corrigem defeito **medido** em template; **(d)** se `GO-TO-SKILLS`
vira Goal. Fica aqui, **sem gerar missão**.

**➕ QUINTO CANDIDATO, redigido depois dos quatro e por consequência da própria medição:
`CANDIDATO-framework-de-skills.md` — `SK-01` a `SK-26`, **253** linhas, fora do acervo.**
**Skills não é Goal da Sequência, e este candidato não a torna Goal** — a reserva da primeira
DIVERGÊNCIA fica intacta. Ele foi escrito porque **`1.15` está bloqueado por FATO**, e a Skill
é o caminho **mais curto e mais barato** para o sinal que `TE-7` exige: `C2`, **sem ratificação
nunca**, sem dependência de Produto, proposição aberta a **qualquer DEP** *(o único componente
assim)*.

**E ele achou o defeito que mais importa desta missão — `AS-1`.** `FND-10 §4.8` justifica **não
criar o tipo `Command`** dizendo que ele já tem onde morar: *"no atributo `gatilho` de
`SKL`/`WFL`"*, **nomeando os dois**. **Medido nos dois templates:** `TPL-workflow` tem **1**
ocorrência de `gatilho`; **`TPL-skill` tem `0`**. **A justificativa é verdadeira em `WFL` e
FALSA em `SKL`** — o lugar que a norma indica **não existe** no template de Skill, e uma Skill
criada hoje não tem onde declarar o que a aciona. `SK-11`–`SK-13` dão contrato ao gatilho
**dentro** da Skill, e **`SK-12` declara expressamente a linha que ele não cruza**: no dia em que
um acionamento precisar de versão, identificador, citação externa ou autoridade próprios, **o
gatilho de reabertura de `FND-10 §4.8` estará satisfeito** — e o caminho passa a ser o rito de
entidade de `FND-09 §11.1`, **jamais esticar o atributo**. **É o mesmo evento que destrava
`1.15`.**

**🔬 ACHADO DE INSTRUMENTO — `AI-1`, e ele alcança toda baseline já emitida.**

> **⚠️ CORRIGIDO em 2026-08-03 pela Missão 1.13.9, e a correção é de premissa.** *(Enunciado
> anterior, preservado: **"descoberto por EXERCÍCIO e não por leitura"**.)* **O limite não foi
> descoberto aqui: ele já estava declarado no acervo 10 vezes, em 4 artefatos, desde 2026-07-28**
> — `revisao-estrutural-01`, **`MSG-2026-0001`** *(o primeiro ato soberano)*, `MSG-2026-0002` e
> múltiplas seções `§10.x` do catálogo —, com o texto *"ela cobre caminhos e contagens de linha, e
> não detecta edição que preserve o número de linhas (`PI-10`, `LV-12`)"*. **O que o exercício
> produziu foi a PROVA e a medida do alcance, não a notícia.** Inscrito como **`RD-108`** e
> **CORRIGIDO** por `IR-BL/4`. O que segue abaixo continua exato quanto ao fato medido. `IR-BL/3` calcula a impressão digital como
`sha256` **da saída de `wc -l`** — isto é, do manifesto `(linhas, caminho)` — e **nunca do
conteúdo dos arquivos**. **Provado nos dois sentidos, na mesma sessão, com controle positivo:**

| Mudança aplicada | `sha256` do arquivo | Impressão digital |
|---|---|---|
| **+26 linhas** no roadmap | mudou | **MUDOU** — `771bbef4…` → `22771cbd…` ✅ *(controle positivo)* |
| **edição in-place** *(`265`→`253`)*, mesma contagem | `4d0552af…` → `75f38927…` | **NÃO MUDOU** — segue `22771cbd…` ❌ |

**Consequência, e ela é de leitura, não de dano:** *"a baseline reproduz em 2 execuções"* prova
que **a lista de artefatos e as contagens de linha** batem — **não** prova que o acervo está
byte-idêntico. **Uma troca de palavra, um número alterado, um `MUST` virado `MAY`, um hash
publicado editado: nada disso move a impressão digital**, desde que a contagem de linhas se
mantenha.

**A prática JÁ COBRE a lacuna, e é por isso que a severidade não é Alta:** toda missão desde o
token 4 confere o conjunto de mudança **arquivo a arquivo** por `sha256`/`cmp`/`diff -rq` contra
a cópia datada — **e é essa conferência, não a digital, que garante byte.** O risco real é de
**linguagem**: quem lê *"baseline reproduzida"* como *"acervo intacto"* está lendo mais do que o
instrumento mede. **Não corrigido** — emendar medidor exige as **quatro provas de inércia**, e
fazê-lo dentro de missão não aberta para isso seria mover o medidor. **Dono: `DEP-GOV`. Gatilho:
próxima emenda de `IR-BL`.**

---

## ONDE O SISTEMA COMEÇA A DEVOLVER

**1.13.5.** A partir dela, cada Spec resolve um problema real. Não é preciso
esperar 1.19, nem o Épico 2, nem o 3.

**Critério de medição, ao fechar a 1.13.5:** comparar o esforço que ela custou
com o valor da Spec produzida. Se resolver LM-6(a) de forma utilizável, o OS
se pagou uma vez e a sequência continua. Se não, a pergunta deixa de ser
quando fica pronto e passa a ser se vale continuar.

**✅ MEDIDO em 2026-08-02, e os dois lados ficam escritos — o julgamento é do Fundador.**

| Lado | O que se mediu |
|---|---|
| **O que custou** | **5 artefatos criados** *(`RFC-0026`, `ADR-0031`, `SPC-001`, `FIT-2026-024`, `PT-2026-017`)* e **7 editados**. **3 dos 5 são consequência da classe `C2`**, e a classe foi imposta por um defeito da própria norma (`RD-91`) — não pela matéria. **A segunda Spec, sem colisão própria, custa 2** ⟵ **⚠️ CORRIGIDO em 2026-08-02 pela 1.13.5.1: o enunciado está certo na aritmética e errado na condição.** A colisão de `RD-91` **não é própria de `SPC-001`** — é estrutural da norma e alcança **todo** `SPC`. Enquanto `RD-91` não for sanado, a segunda `Spec` **também custa 5**, porque também terá de nascer em `C2` para não nascer nula. Os **2** valem **depois** da emenda, que foi **redigida em 2026-08-02** pela 1.13.5.1 e **custa 7** — `ADR-0032`, `aprovado` · `ratificação: pendente`, espera ato *(ver a segunda divergência registrada)* |
| **O que entregou** | Onde havia **0** exigências verificáveis sobre dado pessoal, há **10**, com critério de aceite verificável por terceiro **sem consultar o autor**. Consultar um requisito custa **13 linhas**, contra **555** que antes **não respondiam** |
| **O que só a primeira Spec podia dar** | O Framework saiu de **determinado** para **observado**: **22** regras exercidas sem ressalva, **4** insuficientes, **1** defeituosa, **5** não exercidas — e **4 defeitos que nenhuma leitura tinha achado** (`RD-91` a `RD-94`), mais **1** achado por exercer o contador (`RD-95`) |
| **O que NÃO entregou** | **Nada implementado**: `LM-6(a)` continua com `0` ocorrências no nXtrack. A Spec **descreve o que precisa existir**; construir é outra missão. E **não substitui advogado** |
