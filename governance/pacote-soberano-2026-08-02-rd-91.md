---
id: PS-2026-017
titulo: Pacote de decisao soberana — emenda C3 que sana RD-91, separando proponente de aprovador na Spec C1
tipo: relatorio
versao: 1.3.0
status: ativo
camada_memoria: operacional
autor: DEP-GOV
proprietario: DEP-GOV
aprovador: DEP-EXE
criado_em: 2026-08-02
atualizado_em: 2026-08-02
revisao_prevista: null
decisoes_relacionadas: [ADR-0019, ADR-0021, ADR-0022, ADR-0031, ADR-0032]
substitui: []
substituido_por: null
canal: REPORTE
emissor: DEP-GOV
destinatario: SOBERANO
ttl: ate decisao do Soberano
resumo: Submete ao Soberano a emenda C3 que faz DEP-EXE aprovar Spec C1, com diff literal, hashes integrais dos quatro candidatos, os tres numeros de custo e a minuta do ato. Em 1.3.0 o terceiro despacho esta cumprido na letra - copia parcial apagada com prova de zero bytes unicos, baseline.sh em IR-BL/3 calibrado com reversao e medindo 228/67.771/1513fd45 pelo instrumento vigente sem variante, lease token 16 adquirido ANTES da primeira escrita e a regra gravada no CLAUDE.md, RD-103 registrado em Alta com a premissa do despacho corrigida por medicao no script. Os quatro H-P reconferidos sobre esta versao: 20 de 20. Segue NAO emitida, NAO assinada e NAO aplicada.
perfil_contexto: missao
confidencialidade: interno
revisor: DEP-QAR
---

# PS-2026-017 — Emenda **C3** que sana `RD-91`

> ## Este pacote **informa**. Nao decide, nao aprova, nao promulga e nao edita nada.
>
> **`FND-09` permanece em `1.5.0`, `FND-11` em `1.0.0`, as Cartas de `DEP-PRD` e `DEP-EXE` em
> `1.1.0`.** Os candidatos `1.6.0`, `1.1.0`, `1.2.0` e `1.2.0` existem como **diff literal +
> hash**, **fora do acervo**, em
> `_candidatos-LucaX-Enterprise-OS-2026-08-02-M1.13.5.1/`.
>
> **`0` bytes foram escritos nos quatro arquivos vivos.** Conferido por `sha256` arquivo a
> arquivo em §3, e **reconferido a cada despacho** — §3.2, §3.3 e **§3.4**, esta ultima sobre
> a versao final `1.3.0`: **`20` de `20` reproduzem**.
>
> **Caminho exato:** `governance/pacote-soberano-2026-08-02-rd-91.md` *(RE-01)*.

> ## `1.1.0` — despacho do Soberano de 2026-08-02: as tres questoes de §7, RESPONDIDAS
>
> **`Q1`** ESTENDE a `PRJ` e `TPL`, com missao propria ordenada · **`Q2`** `C0` PERMANECE
> declarado em `RD-91` · **`Q3`** prevalece a PRATICA EXERCIDA, incremento **MENOR**.
>
> A minuta de §6 foi **reemitida** com **as tres caixas marcadas** e com o **item VIII**, que
> da a `Q3` casa de decisao propria — sem ele, o ato decidiria `Q3` **em silencio**, pelos
> numeros de versao do item I, que e exatamente o que §7 recusou fazer.
>
> **A minuta continua NAO EMITIDA, NAO assinada e NAO aplicada. Congelamento em vigor.** O
> que muda de `1.0.0` para `1.1.0` esta listado, linha a linha, em **§9.1**.

> ## `1.3.0` — terceiro despacho de 2026-08-02: **`4` de `4` cumpridas, nenhuma impossivel**
>
> **`(c)` PRIMEIRO, porque autoriza as outras:** `fencing_token` **16** adquirido as
> **`13:24:25`**, **`0` bytes no acervo antes dele** — **a primeira emissao do acervo sob
> lease adquirido** —, e a regra permanente gravada no `CLAUDE.md` da raiz.
> **`(b)`** `baseline.sh` vai a **`IR-BL/3`**, calibrado com **`4` provas** incluindo
> **reversao**; a baseline e medida **pelo instrumento vigente, sem variante**:
> **`228 · 67.771 · 1513fd45…eb5f`**, `2` execucoes — **§10**.
> **`(a)`** a copia parcial de `12:04` **apagada**, com prova previa e posterior de que
> **nenhum byte unico se perde**.
> **Inscricao:** **`RD-103` REGISTRADO** em **Alta**, item **126** — **com a premissa do
> despacho corrigida por medicao no proprio script**: ele **pede** confirmacao; o defeito e
> que a listagem era de primeiro nivel e o operador **confirmou sem poder ver** o que
> destruia.
>
> **`20` de `20`** em §3.4 · **`0` bytes** nos objetos do ato · **`0` baselines emitidas**.
> **A minuta continua NAO EMITIDA, NAO assinada e NAO aplicada.** O que muda de `1.2.0` para
> `1.3.0` esta em **§9.3**, com os **`3` acrescimos sem ordem** separados para serem riscados.

> ## `1.2.0` — segundo despacho do Soberano de 2026-08-02: **tres determinacoes cumpridas, uma IMPOSSIVEL**
>
> **`1`** item **VII** MANTIDO como reconciliado, com os outros tres acrescimos de §9.1 · **`3`**
> **`RD-101` REGISTRADO** sem correcao, no catalogo, item **124** · **`4`** a edicao sem copia
> datada REGISTRADA como **`RD-102`**, item **125**.
>
> ### ⛔ **`2` — o rollback NAO pode ser movido para `_backups/`: a copia NAO EXISTE MAIS**
>
> `_to_delete/` foi **apagado** — `rd /s /q`, sem lixeira — pelo script `_FAXINA-2-apagar.bat`,
> **entre a emissao de `1.1.0` e a chegada do despacho**. Com ele foi a copia datada
> `LucaX-Enterprise-OS_2026-08-02_pre-instrumentos-1-13-5-1`, que §4 de `1.1.0` declarava como
> **ponto de retorno do acervo inteiro**. **Busca em toda a arvore `E:\LucasIA`, ate 6 niveis:
> `0` ocorrencias de `_to_delete` e `0` de `*pre-instrumentos-1-13-5-1*`.**
>
> **O aviso escrito em `1.1.0` foi cumprido pelos fatos, na mesma hora em que foi escrito:**
> *"basta que alguem o esvazie para que a linha acima passe a apontar para o vazio, e ninguem
> seria avisado"*. **Alguem esvaziou.** Este paragrafo e o aviso que aquele paragrafo previu.
>
> **§4 foi reescrito com o que sobrou, medido — nao com o que se gostaria que houvesse.** O
> rollback dos **quatro objetos do item I permanece INTACTO e trivial**; o rollback do **acervo
> inteiro** ficou **PARCIAL**, e o §4 diz exatamente onde. **Nova copia datada tomada nesta
> sessao, antes de escrever.**
>
> **A minuta continua NAO EMITIDA, NAO assinada e NAO aplicada. Congelamento em vigor.** O que
> muda de `1.1.0` para `1.2.0` esta listado, linha a linha, em **§9.2**.

## Proposito

Levar ao Soberano a emenda que sana **`RD-91`** — a colisao que torna **nula** (`LV-03`) a
aprovacao de **toda** `Spec` de classe `C1` e, com isso, **inutilizavel** o piso que
`FND-04 §6` fixa. A decisao e dele por construcao: **`FND` nao vigora sem ratificacao**
(`LM-02`; `FND-09 §8.2`, linha `FND`; `SF-32`), e **duas Cartas ratificadas** so se emendam
por ato.

## 1. O que se pede, em uma frase

> **Aprovar e ratificar [ADR-0032](../decisions/ADR-0032-separacao-de-proponente-e-aprovador-na-spec-c1.md),
> promulgando quatro versoes: `FND-09` 1.6.0, `FND-11` 1.1.0, Carta `DEP-PRD` 1.2.0 e Carta
> `DEP-EXE` 1.2.0** — de modo que a aprovacao de `Spec` `C1` passe do **proprietario**, que e
> quem a propoe, para **DEP-EXE**, que nao a propoe.

## 1.1 Os tres numeros de custo — medidos, em destaque

| # | Pergunta | Resposta | Como se apurou |
|---|---|---|---|
| **1** | Quanto custou a **primeira** `Spec`, em `C2` | ## **5 artefatos** *(1.580 linhas)* | `wc -l` em 2026-08-02: `RFC-0026` **197** · `ADR-0031` **271** · `SPC-001` **603** · `FIT-2026-024` **211** · `PT-2026-017` **298** |
| **2** | Quanto custara a **segunda**, em `C1`, **depois** desta emenda | ## **2 artefatos** *(Nota de Decisao + a `Spec`)* — **3** contando o registro de missao | **Derivado de norma, nao medido:** `FND-07 §2.3` da `C1 · Tipo 2` = **Nota de Decisao**; `SF-24` item (9) so exige `FIT` em `C2`/`C3`; `FND-04 §2` nao exige `RFC` nem `ADR` em `C1` |
| **3** | Quanto custa **esta emenda** | ## **7 artefatos** *(+ 4 documentos emendados)* | `RFC-0027` · `ADR-0032` · `FIT-2026-025` · **este pacote** · `PT-2026-018` · **o ato (`MSG`)** · o `PT` da aplicacao. Medido na cadeia identica de `PS-2026-008` → `MSG-2026-0006` |

> **A economia se paga na segunda `Spec` e sobra a partir da terceira.** Emenda: **7**,
> uma vez. Cada `Spec` que deixa de subir a `C2`: **3 a menos**. **`CE-04` respeitado:** o
> numero **1** e medicao; os numeros **2** e **3** sao **derivacao de norma e de precedente**,
> e estao declarados como tal — nenhuma linha foi estimada.

## 2. Diff literal

### 2.1 `FND-09` — §8.2, linha `SPC`, coluna *Aprova* *(a fonte)*

**Antes** *(linha 900)*:
```
| SPC | DEP-PRD | DEP-ENG + DEP-QAR | conforme classe (FND-04 §2) | SOBERANO se C3 ou Tipo 1 | DEP-PRD |
```

**Depois**:
```
| SPC | DEP-PRD | DEP-ENG + DEP-QAR | conforme classe (FND-04 §2); em **C1**, **DEP-EXE** | SOBERANO se C3 ou Tipo 1 | DEP-PRD |
```

**Nota acrescentada** *(apos as notas existentes de §8.2, antes de §8.3 — 14 linhas)*:
```
> **Sobre a linha `SPC`, coluna *Aprova*, em `C1`.** Esta celula **nao redefine FND-04 §2**:
> ela **aplica FND-04 §3.1** ao unico caso em que a propria matriz torna o default de §2
> impossivel. Para `SPC`, esta tabela poe **DEP-PRD** como quem **propoe/cria** e como quem
> **aposenta** — logo, proprietario —, e `FND-04 §2` atribui a aprovacao `C1` ao
> **proprietario**. As duas leituras juntas produzem `Proponente = Aprovador`, que
> `FND-04 §3.1` declara **nula** por **`LV-03`**, Linha Vermelha de `FND-01`, **nivel 1** da
> hierarquia normativa. **Entre um default de §2 e uma incompatibilidade absoluta de §3.1,
> prevalece a incompatibilidade** — e o aprovador passa a ser o titular que `FND-04 §2` ja
> nomeia no degrau seguinte: **DEP-EXE**. **Nenhum titular foi ampliado:** `DEP-EXE` ja
> aprova `Spec` `C2` na mesma linha desta tabela. **A classe NAO muda:** `C1` continua `C1`,
> com **Nota de Decisao** como instrumento (`FND-04 §2`, `FND-07 §2.3`), **sem** `RFC`,
> **sem** `ADR`, **sem** `FIT` e **sem** ratificacao. **`C0` NAO e alcancado por esta emenda,
> e o colapso de `C0` permanece declarado** em `RD-91`.
```

**Frontmatter:** `versao` 1.5.0 → **1.6.0** · `atualizado_em` → **2026-08-02** ·
`decisoes_relacionadas` **+ ADR-0032** · `status` → `aprovado` · `ratificacao` → `pendente`
*(os dois ultimos voltam a `ativo`/`ratificada` **pelo ato** — e o que `H-P` projeta em §3)*.
**Mais 1 linha de historico.**

**Total: 6 blocos de diff. `0` fora deles.**

### 2.2 `FND-11` — §5, matriz de `SF-10`, celula *Aprovacao* × `C1 · T2` *(cascata)*

**Antes** *(linha 260)*:
```
| **Aprovacao** | proprietario | proprietario **+ revisor** | **DEP-EXE** + parecer **DEP-GOV** | **DEP-EXE** propoe | **SOBERANO**, indelegavel |
```

**Depois**:
```
| **Aprovacao** | proprietario | **DEP-EXE** + revisor | **DEP-EXE** + parecer **DEP-GOV** | **DEP-EXE** propoe | **SOBERANO**, indelegavel |
```

**Declaracao `PJ-02` de §5, linha *Fonte*** — acrescenta **`§3.1`**:
```
> **Fonte:** `FND-04 §2`, `§2.1`, `§2.2`, **`§3.1`**, `§6` · `FND-07 §2.3`, `§2.4`, `§5` · `FND-09 §8.2`
```

**Linha de §2.2, regra `SF-10`** — passa a datar a equivalencia:
```
| **SF-10** | §5.3 | `T-IDENTICA` **na recepcao de `1.0.0`** — inclusive a matriz de **50 celulas** e a declaracao `PJ-02` que a governa. **`1.1.0` emendou 1 celula da matriz** *(linha Aprovacao × coluna `C1 · T2`)*, por `ADR-0032` | **Integral na recepcao.** A divergencia posterior e **declarada** em §2 e no historico, nunca silenciosa |
```

**Nota de alcance temporal em §2** *(11 linhas)* — declara que a matriz passa a diferir em
**1** celula da copia de `ADR-0021 §5.3`, artefato `M1` que nunca se emenda, e que **prevalece
`FND-11`** por `ADR-0022 §5.4`. Achado `RD-98`.

**Frontmatter:** `versao` 1.0.0 → **1.1.0**, mesmos cinco campos. **Mais 1 linha de historico.**

**Total: 8 blocos de diff. `0` celulas da matriz alteradas alem de uma.**

### 2.3 Carta de `DEP-PRD` — 4 linhas *(propagacao obrigatoria, `CV-04`)*

| § | Antes | Depois |
|---|---|---|
| **§4** *(o que NAO me compete)*, L126 | *"Aprovar a Spec de classe **`C2` ou `C3`** que eu escrevo"* | *"Aprovar a Spec de classe **`C1`, `C2` ou `C3`** que eu escrevo"* — e `DEP-EXE` cobre `C1` e `C2` |
| **§5** *(o que decido)*, L138 | *"**Aprovar Spec** (`SPC`) **de classe `C0` ou `C1`**, como proprietario"* · *"`C2` aprova DEP-EXE; `C3`, o SOBERANO"* | *"**Aprovar Spec** (`SPC`) **de classe `C0`**, como proprietario"* · *"**`C1` aprova DEP-EXE** (pos-`ADR-0032`: sou o proponente de toda `Spec`, e `FND-04 §3.1` veda `Proponente = Aprovador`); `C2` aprova DEP-EXE; `C3`, o SOBERANO"* |
| **§5.1** *(o que NAO decido)*, L153 | *"Aprovar Spec **`C2` ou `C3`**"* | *"Aprovar Spec **`C1`, `C2` ou `C3`**"* |
| **§7** *(artefatos mantidos)*, L224 | *"aprovador **apenas quando a classe do efeito for `C0` ou `C1`**"* | *"aprovador **apenas quando a classe do efeito for `C0`**"* |

### 2.4 Carta de `DEP-EXE` — 2 linhas

| § | Antes | Depois |
|---|---|---|
| **§5** *(o que decido)*, L158 | *"**Aprovar Spec** (`SPC`) **quando a classe do efeito for `C2`**"* · *"**`C0`/`C1`: o proprietario.** `C3`: SOBERANO"* | *"**Aprovar Spec** (`SPC`) **quando a classe do efeito for `C1` ou `C2`**"* · *"**`C0`: o proprietario.** `C3`: SOBERANO"* — parecer de DEP-GOV **so em `C2`** |
| **§7** *(artefatos mantidos)*, L260 | *"aprovador **quando a classe do efeito for `C2`**"* | *"aprovador **quando a classe do efeito for `C1` ou `C2`**"* |

> **`0` linhas de historico editadas nas duas Cartas.** A linha `1.1.0` de `DEP-PRD` conserva
> *"segue aprovando Spec `C0` e `C1`"* — **verdadeira quando escrita**, e agora **superada por
> versao**, nunca reescrita (`AC-10`, `LV-04`). Historico nao se corrige: se supera.

## 3. Identificadores de integridade

**Instrumento:** `hashes.sh` — `H-A` = `sha256` do arquivo submetido (`IR-07`); `H-N` =
`sha256` com as linhas de `IR-03` removidas **so dentro do frontmatter** (`IR-02`); `H-P` =
`sha256` projetado **apos** a transicao `O4` que o ato realiza (`status: aprovado → ativo`,
`ratificacao: pendente → ratificada`).

| Objeto | Estado | Linhas | `H-A` | `H-N` |
|---|---|---|---|---|
| **`FND-09`** | **VIGENTE 1.5.0** | 1.263 | `191ff367eead695b4a1c2622ea20dfb89d47c40bfe2d5945286bf99e7bbd1952` | `052a948a159babe6a83fbd44763ce13774ee3195792bf738352909762212edcf` |
| **`FND-09`** | **CANDIDATO 1.6.0** | 1.278 | `defdf5b8e815ce1e6d76ae453348726625e42b13b4a3c523ffce8eed247e6c6c` | `eee41dfcf2147ab81ab1a477d6bdf0e91bc67f3310a9ca59c7248597386dbaf3` |
| **`FND-11`** | **VIGENTE 1.0.0** | 399 | `383ee51df8ee8782a693897f07423529bc4dd5a7d866603ad31e61fe06ad7c20` | `90f4efb74e2d2573e9f4fdf69db43e3da863f3b1e09926febe22517f32ca79ee` |
| **`FND-11`** | **CANDIDATO 1.1.0** | 411 | `efa9e10959a095fc51c327bf553ee6171bff6b8ef7c94c8c92ce953599fce373` | `7b1ff33aeea2f0dc7a70247783d9290e86db7a1643971c1bdeebc590a3985f47` |
| **`DEP-PRD`** | **VIGENTE 1.1.0** | 445 | `0e98511630faf9d5bec4c7cb36d8a37fa0bf15506dfde36954f0419e2720fc15` | `ce3490049a57e6c141a40a07bcb7da1881b0389c4fe8134b359c7bf406d40279` |
| **`DEP-PRD`** | **CANDIDATO 1.2.0** | 446 | `abf2ddfd70692833b92ace28b3b0a64f6da8b853263fad07aa93f533cb50ea9e` | `10f2ae9fc8f261f697006bc6c9478964a96627e14a2174b37f21ca6ab7025237` |
| **`DEP-EXE`** | **VIGENTE 1.1.0** | 506 | `a75a1ffea140e1f962e97b5b8772df70cf28831df882a25cf804b4e931f17e12` | `537eb9f474dcc6e778c911d3abbe5cc9e4a84ec79914cf2aadb15d6aa929aab6` |
| **`DEP-EXE`** | **CANDIDATO 1.2.0** | 507 | `087fc63454e617c37c688f2d6391b880db3cdcd7538475c1a87afbe8c0c531b3` | `a191fb9e86bb6518f0935ec7203861ae61701d7f632632ed23a5edb40daaa192` |

### 3.1 `H-P` — o valor que o arquivo tera **depois** do ato

| Objeto | `H-P` *(projetado)* |
|---|---|
| **`FND-09` 1.6.0** | `ea5efd35249c12f9587b7ded68a72d165a14b4adf399fdb2f9dc09dad395a8db` |
| **`FND-11` 1.1.0** | `b2cff9f59e9f0d47034f02322d32438552d122c867ae0e954bb30fc42cd16e08` |
| **`DEP-PRD` 1.2.0** | `b9ac470c9227dd5bc4445d96faf1ff6b1d67c2d6f40ea6d0fd8a4f37180babdd` |
| **`DEP-EXE` 1.2.0** | `a07c765a9134b3f4caad76b2750c38beff53b6f7f2e8659717c42c881c9619e4` |

> **Para que serve o `H-P`.** A missao que aplicar o ato **tem de reproduzir estes quatro
> valores** depois de mover `status` e `ratificacao`. Se nao reproduzir, alguma coisa alem da
> transicao `O4` foi tocada, e a aplicacao **para**. **A transicao fica testavel antes de
> ocorrer.**

### 3.2 Reconferencia dos quatro `H-P` — ordenada pelo despacho, executada antes da devolucao

**Instante da medicao:** `2026-08-02 10:40:11 -03:00`. **Instante, e nao data** — os candidatos
vivem **fora do acervo**, em arvore que pode mudar dentro da propria missao, e a data sozinha
nao ancora nada.

**Instrumento:** `hashes.sh`, `sha256` = `729cafadee7fd10ec218216919e585dc2ac90315a2b5cb4fd9322a85fc8487ec`,
recuperado de `_arquivo/missoes-encerradas/_missao-1-13-5-2026-08-01/ferramentas/`. **Nao foi
reescrito nem reimplementado:** e o mesmo binario de texto que produziu os valores de §3.
**Controle positivo antes do uso** — o instrumento rodou sobre **si proprio** e devolveu o
proprio `sha256`, provando que `ha()` mede o que diz medir e que um `0` posterior nao seria
`0` de instrumento morto.

| Objeto | `H-P` **publicado em §3.1** | `H-P` **reconferido em 2026-08-02 10:40** | |
|---|---|---|---|
| **`FND-09` 1.6.0** | `ea5efd35…a8db` | `ea5efd35249c12f9587b7ded68a72d165a14b4adf399fdb2f9dc09dad395a8db` | ✅ **reproduz** |
| **`FND-11` 1.1.0** | `b2cff9f5…6e08` | `b2cff9f59e9f0d47034f02322d32438552d122c867ae0e954bb30fc42cd16e08` | ✅ **reproduz** |
| **`DEP-PRD` 1.2.0** | `b9ac470c…abdd` | `b9ac470c9227dd5bc4445d96faf1ff6b1d67c2d6f40ea6d0fd8a4f37180babdd` | ✅ **reproduz** |
| **`DEP-EXE` 1.2.0** | `a07c765a…19e4` | `a07c765a9134b3f4caad76b2750c38beff53b6f7f2e8659717c42c881c9619e4` | ✅ **reproduz** |

**`4` de `4` reproduzem.** Reconferidos tambem, no mesmo instante e pelo mesmo instrumento, os
**`8`** `H-A`/`H-N` de §3 e os **`4`** `H-A` dos arquivos vigentes: **`12` de `12` reproduzem**
— os quatro arquivos vivos **continuam intocados**, e o `H-P` continua sendo a projecao dos
candidatos que §3 publica.

**Prova de que a projecao `O4` alcanca exatamente dois campos, e nao mais.** O `diff` entre
cada candidato e a sua projecao foi tomado objeto a objeto: **`2` linhas alteradas em cada um
dos quatro** — `status: aprovado → ativo` e `ratificacao: pendente → ratificada`. **`0` linhas
de corpo, `0` linhas de historico, `atualizado_em` nao tocado.** Os quatro candidatos estao,
de fato, em `status: aprovado` **com** `ratificacao: pendente` — sem isso a projecao seria
identidade, e o `H-P` valeria `H-A` sem que nada avisasse.

> ### ⚠️ Divergencia encontrada na reconferencia — na EVIDENCIA, nunca no pacote
>
> `_missao-1-13-5-1-2026-08-02/evidencia/hashes-candidatos.txt`, citado por
> [`PT-2026-018`](relatorio-transicao-2026-08-02-emenda-sf-10.md) como a tabela de hashes da
> missao, **nao reproduz para as duas Cartas**: registra `H-A` `20fd1450…d2ed` / `H-P`
> `a0e23d60…9228` para `DEP-PRD` e `H-A` `7d00d46b…9bbe` / `H-P` `eb35d890…b9db` para
> `DEP-EXE` — **`6` valores divergentes de `24`**. `FND-09` e `FND-11` reproduzem nos dois
> lugares.
>
> **A causa esta medida, nao suposta.** `hashes-candidatos.txt` foi escrito as **01:31:14**;
> `DEP-PRD-carta-1.2.0.md` e `DEP-EXE-carta-1.2.0.md` foram modificados as **01:39:33** —
> **oito minutos depois**. `FND-09` *(01:28:04)* e `FND-11` *(01:29:16)* sao anteriores ao
> arquivo de evidencia, e por isso batem. **Divergem exatamente os dois arquivos tocados
> depois do instantaneo, e so eles.**
>
> **Quem esta certo e o pacote.** §3 e §3.1 carregam os valores dos arquivos que existem
> agora, e a reconferencia acima os reproduz um a um; o que ficou para tras foi a evidencia,
> que **nunca foi reemitida**. **`0` linhas de `PS-2026-017` estavam erradas** — e os seis
> valores das Cartas **so existem neste pacote**: nao aparecem em `ADR-0032`, `RFC-0027`,
> `FIT-2026-025`, `PT-2026-018` nem no catalogo mestre, de modo que **nenhum artefato do
> acervo propaga o valor superado**.
>
> **Efeito sobre o ato: nenhum.** O item VI manda reproduzir os `H-P` **do item I**, que sao
> os de §3.1. **Efeito sobre quem aplicar: existe, e e por isto que fica escrito** — quem
> conferir pela evidencia da missao, e nao pelo pacote, obtera `2` de `4` falhando e **parara
> uma aplicacao correta**. **Candidato a achado `RD-101`, severidade Baixa, dono DEP-GOV,
> gatilho *"aplicacao do ato"*. NAO registrado: registrar e ato de catalogo, e o congelamento
> esta em vigor** — entra por decisao do Soberano, junto com a aplicacao do item VI.

### 3.3 Reconferencia sobre a versao FINAL `1.2.0` — ordenada pelo segundo despacho

**Instante da medicao:** `2026-08-02 11:41:54 -03:00`. **Instrumento:** o mesmo `hashes.sh`,
`sha256` = `729cafadee7fd10ec218216919e585dc2ac90315a2b5cb4fd9322a85fc8487ec` — **nao
reescrito, nao reimplementado**, o mesmo binario de texto de §3 e §3.2.

**Controle positivo antes do uso, e desta vez ele era obrigatorio.** O instrumento rodou sobre
**si proprio** e devolveu o proprio `sha256`. **A razao de nao dispensar o controle esta no
proprio arquivo:** o docstring de `hashes.sh` afirma que *"a calibracao contra os 8 controles
publicados prova isso, **e roda antes do uso**"* — e o arquivo **nao implementa calibracao
alguma**: tem `ha()`, `hn()`, `hp()` e um `case`, mais nada. **O instrumento afirma uma
propriedade que nao tem.** Nao e defeito de medicao — os tres calculos estao corretos —, mas e
**a mesma familia de `RD-101`**, e por isso a calibracao foi **exercida a mao**, nunca assumida.

| Objeto | `H-P` **publicado em §3.1** | `H-P` **reconferido em 2026-08-02 11:41** | |
|---|---|---|---|
| **`FND-09` 1.6.0** | `ea5efd35…a8db` | `ea5efd35249c12f9587b7ded68a72d165a14b4adf399fdb2f9dc09dad395a8db` | ✅ **reproduz** |
| **`FND-11` 1.1.0** | `b2cff9f5…6e08` | `b2cff9f59e9f0d47034f02322d32438552d122c867ae0e954bb30fc42cd16e08` | ✅ **reproduz** |
| **`DEP-PRD` 1.2.0** | `b9ac470c…abdd` | `b9ac470c9227dd5bc4445d96faf1ff6b1d67c2d6f40ea6d0fd8a4f37180babdd` | ✅ **reproduz** |
| **`DEP-EXE` 1.2.0** | `a07c765a…19e4` | `a07c765a9134b3f4caad76b2750c38beff53b6f7f2e8659717c42c881c9619e4` | ✅ **reproduz** |

**`4` de `4` reproduzem** — e desta vez a cobertura foi **fechada**. §3 publica `8` linhas ×
`2` colunas = **`16`** valores; §3.1 publica **`4`** `H-P`: **`20` valores publicados ao
todo**. **Os `20` foram remedidos, e os `20` reproduzem.** A reconferencia de §3.2 cobria
**`16`** — deixava de fora os **`4`** `H-N` dos arquivos vigentes, que **agora tambem
reproduzem**: `052a948a…edcf`, `90f4efb7…79ee`, `ce349004…0279` e `537eb9f4…aab6`.

> **`20` de `20`.** Os quatro arquivos vivos continuam **intocados** — `H-A` **e** `H-N` —, os
> quatro candidatos continuam **intocados**, e o `H-P` continua sendo a projecao exata do que
> §3 publica. **Nada mudou entre `1.1.0` e `1.2.0` nos objetos do ato: `0` bytes.**

### 3.4 Reconferencia sobre a versao FINAL `1.3.0` — ordenada pelo terceiro despacho

**Instante da medicao:** `2026-08-02 13:29:39 -03:00`. **Instrumento:** o mesmo `hashes.sh`,
`sha256` = `729cafadee7fd10ec218216919e585dc2ac90315a2b5cb4fd9322a85fc8487ec` — **nao
reescrito, nao reimplementado**, o mesmo binario de texto de §3, §3.2 e §3.3, agora com
copia propria da missao em `_missao-1-13-5-1-2026-08-02/ferramentas/`.

**Controle positivo antes do uso, exercido pela terceira vez.** O instrumento rodou sobre
**si proprio** e devolveu o proprio `sha256`. A razao segue sendo a de §3.3 — o docstring
promete uma calibracao que o arquivo **nao implementa** —, e por isso a calibracao continua
**exercida a mao, nunca assumida**.

| Objeto | `H-P` **publicado em §3.1** | `H-P` **reconferido em 2026-08-02 13:29** | |
|---|---|---|---|
| **`FND-09` 1.6.0** | `ea5efd35…a8db` | `ea5efd35249c12f9587b7ded68a72d165a14b4adf399fdb2f9dc09dad395a8db` | ✅ **reproduz** |
| **`FND-11` 1.1.0** | `b2cff9f5…6e08` | `b2cff9f59e9f0d47034f02322d32438552d122c867ae0e954bb30fc42cd16e08` | ✅ **reproduz** |
| **`DEP-PRD` 1.2.0** | `b9ac470c…abdd` | `b9ac470c9227dd5bc4445d96faf1ff6b1d67c2d6f40ea6d0fd8a4f37180babdd` | ✅ **reproduz** |
| **`DEP-EXE` 1.2.0** | `a07c765a…19e4` | `a07c765a9134b3f4caad76b2750c38beff53b6f7f2e8659717c42c881c9619e4` | ✅ **reproduz** |

**`4` de `4` reproduzem, e a cobertura continua FECHADA: `20` de `20`.** Os **`16`** valores
de §3 — `H-A` e `H-N` dos **`4`** vigentes e dos **`4`** candidatos — foram remedidos no mesmo
instante e **reproduzem um a um**, entre eles os quatro `H-N` dos vigentes: `052a948a…edcf`,
`90f4efb7…79ee`, `ce349004…0279` e `537eb9f4…aab6`.

> **Terceira reconferencia, terceiro `20` de `20`.** Entre `1.2.0` e `1.3.0` esta sessao
> escreveu no acervo — este pacote, o catalogo mestre, o roadmap — e escreveu **fora** dele:
> apagou uma copia, alterou o instrumento de baseline, adquiriu lease, gravou regra no
> `CLAUDE.md`. **Nenhuma dessas escritas tocou os objetos do ato: `0` bytes nos `4` arquivos
> vivos, `0` bytes nos `4` candidatos, `0` bytes nos quatro `H-P`.** E e por isto que a
> reconferencia se repete a cada reemissao em vez de ser citada da anterior — **citar seria
> afirmar sem medir**, que e o defeito que `RD-101` nomeia.

## 4. Rollback por objeto

| Objeto | Ponto de retorno exato |
|---|---|
| `FND-09` | `H-A` vigente `191ff367…1952` — o arquivo **nao foi tocado** |
| `FND-11` | `H-A` vigente `383ee51d…7c20` — idem |
| `DEP-PRD` | `H-A` vigente `0e985116…fc15` — idem |
| `DEP-EXE` | `H-A` vigente `a75a1ffe…7e12` — idem |
| Acervo inteiro | ⚠️ **PARCIAL.** Identificador **integro**: `H-A` de **602** arquivos em `_missao-1-13-5-1-2026-08-02/evidencia/H-A-rollback-pre-escrita.txt`, `sha256` = `a44370df7931e8963e8cd585c5c2cec5cfa9a3dd3325ba70589fc49d49084538` — **reproduz**. **Copia datada correspondente: DESTRUIDA.** Sede vigente e alcance exato no quadro abaixo |

**Antes do ato o rollback e trivial: basta nao emitir.** Depois do ato, e **emenda revogatoria
de mesmo rito** (`FND-04 §2`, C3).

> ### ⛔ A copia de rollback do acervo inteiro foi DESTRUIDA — conferido em `2026-08-02 11:38`
>
> **O despacho de `1.2.0` ordenou mover** `_to_delete/_backups/LucaX-Enterprise-OS_2026-08-02_pre-instrumentos-1-13-5-1`
> **para `_backups/`. Nao ha o que mover.** `_to_delete/` foi apagado pelo script
> `_FAXINA-2-apagar.bat` — `rd /s /q`, que **nao passa pela lixeira** —, entre a emissao de
> `1.1.0` e a chegada do despacho. **Busca em toda a arvore `E:\LucasIA`, ate `6` niveis:
> `0` ocorrencias de `_to_delete` e `0` de `*pre-instrumentos-1-13-5-1*`.**
>
> **O aviso de `1.1.0` descreveu o que aconteceu em seguida, palavra por palavra:** *"basta que
> alguem o esvazie para que a linha acima passe a apontar para o vazio, e ninguem seria
> avisado"*. **O que ele nao previu foi a ordem dos eventos** — o esvaziamento veio **antes** da
> decisao que o teria evitado, e a determinacao chegou a um objeto que ja nao existia.
>
> #### O que sobrou — medido, nao suposto
>
> | O que | Estado |
> |---|---|
> | Manifesto `H-A-rollback-pre-escrita.txt` | ✅ **`602` linhas, `sha256` `a44370df…4538` — igual ao publicado.** **Identifica** o estado pre-escrita; **nao o restaura** |
> | Manifesto conferido contra o acervo vivo | **`595` de `602` reproduzem** · **`0` de `602` ausentes** — nenhum arquivo sumiu · **`7` divergem** |
> | Delta de arquivos | **`608`** vivos hoje; **`6`** fora do manifesto — os **`5`** artefatos desta missao *(`RFC-0027`, `ADR-0032`, `FIT-2026-025`, este pacote, `PT-2026-018`)* mais **`.gitattributes`**, criado pela manutencao externa |
>
> #### Os `7` divergentes, um a um — **`0` sao fonte normativa, `0` sao objeto do item I**
>
> | Arquivo | Pre-escrita | Em `11:41` | O que e |
> |---|---|---|---|
> | `.obsidian/workspace.json` | `86eaed7a…` | `2315fb15…` | Estado volatil de IDE |
> | `README.md` | `6e68c856…` | `94bb99c6…` | Indice de raiz |
> | `decisions/README.md` | `3ff31392…` | `62380f0f…` | Indice |
> | `rfcs/README.md` | `a1209cbf…` | `6e27e06c…` | Indice |
> | `governance/fitness/README.md` | `ceb6cb35…` | `04f67953…` | Indice |
> | `governance/artifact-registry.md` | `6fe12a27…` | `16949bfa…` \* | Catalogo mestre |
> | `governance/roadmap-canonico.md` | `7521a77e…` | `a45e55fe…` \* | Registro de acompanhamento — **autoridade nenhuma, nao normativo** |
>
> \* **Valores de `11:41`, e os dois ja mudaram de novo depois disso, nesta mesma sessao** — o
> catalogo pela **inscricao ordenada** de `RD-101` e `RD-102`, o roadmap pela regra permanente
> de assinalar na mesma sessao que fecha. **Nao se publica aqui o valor corrente de arquivo que
> esta secao ainda esta escrevendo:** seria pôr no §4 exatamente o defeito que `RD-101`
> descreve. **Os `H-A` de fechamento de `1.2.0` estao no Historico**, tomados depois da ultima
> escrita.
>
> **Os `7` sao a pegada declarada desta missao**, nao perda: indices e catalogo que os `5`
> artefatos novos obrigaram a atualizar, mais o registro de acompanhamento e o estado de IDE.
> **Para estes `7`, e so para estes, os bytes pre-escrita nao existem em lugar nenhum** — sobra
> o `sha256` de cada um, que identifica e nao restaura.
>
> #### Sede vigente do rollback — a que existe, conferida
>
> | Sede | Conteudo | Prova |
> |---|---|---|
> | **`_backups/LucaX-Enterprise-OS_2026-08-02_pre-terceiro-despacho`** ⟵ **VIGENTE** | **`234`** arquivos — as **`9`** raizes de `ACERVO` mais `CLAUDE.md` e `.obsidian`, tomada em `13:24:25` **antes da primeira escrita** deste despacho | Manifesto `H-A-pre-terceiro-despacho.txt`, `sha256` `12dac5775b5f4f65160cc7d631fa96a301dc6151edf7132bf16bfc068500330d`, conferido **`234` de `234`** contra o disco — **e a copia REPRODUZ os tres valores da baseline**, `228 · 67.771 · 1513fd45…eb5f` |
> | `_backups/LucaX-Enterprise-OS_2026-08-02_pre-reemissao-minuta-1.2.0` | **`608`** arquivos, tomada em `11:40` | Manifesto `H-A-pre-reemissao-1.2.0.txt`, `sha256` `9696356e…e1ce`, conferido **`608` de `608`** |
> | `_backups/LucaX-Enterprise-OS_2026-08-02_pos-reemissao-minuta-ps-2026-017` | **`607`** arquivos, tomada em `10:47` | Contagem |
>
> **A sede vigente e PROVADA FIEL, nao apenas datada.** As duas anteriores provam que o
> manifesto casa com o disco da copia; esta prova tambem que **a copia mede o mesmo que o
> acervo** — reproduz os tres valores da baseline pelo instrumento vigente. Um ponto de
> retorno que nao reproduz a baseline identifica bytes sem provar que sao **os** bytes.
>
> **O que a copia vigente NAO leva, declarado e nao omitido:**
>
> | Excluido | Por que |
> |---|---|
> | `.git/` | **Nao e fiel a bytes** — `RD-104`: `161` de `607` nao reproduzem, `6` deles normativos. Copiar um anexo que ja se sabe infiel nao acrescenta ponto de retorno. Passou a `NAO_ACERVO` por `IR-BL/3` |
> | `_SAIDA-COMPANY-OS/` | **ESCRITOR CONCORRENTE ATIVO.** Sessao alheia a esta tocou `2` arquivos as `13:04:47` e `13:07:32`. **Copiar arvore em escrita produz alvo movel, nao ponto de retorno** — *instante, nao data*. E `NAO_ACERVO`: **`0` efeito sobre os tres valores**, reconferido **depois** da escrita concorrente |
>
> **Cumpre a determinacao anterior no que ela pedia de fato:** a sede do rollback **saiu de
> uma pasta cujo nome anuncia remocao** e passou a `_backups/`. **O que nao se pode cumprir e
> a parte que supunha o objeto existindo.**
>
> #### Reconferencia do manifesto pre-escrita contra o acervo VIVO — `2026-08-02 13:30`
>
> | Medida | Em `11:41` | **Agora** |
> |---|---|---|
> | Reproduzem | `595` de `602` | **`593` de `602`** |
> | **Ausentes** | **`0`** | **`0`** — **nenhum arquivo sumiu** |
> | Divergem | `7` | **`9`** |
>
> **As `2` divergencias novas estao nomeadas e sao ambas desta sessao ou declaradas:**
> `CLAUDE.md`, onde o item **(c)** mandou gravar a regra de lease, e
> `_SAIDA-COMPANY-OS/01_ESTADO-DA-ANALISE.md`, do **escritor concorrente**. **`0` das `9` sao
> fonte normativa e `0` sao objeto do item I.** Fora do manifesto ha **`7`** arquivos:
> os **`5`** artefatos desta missao, o `.gitattributes` da manutencao externa e
> `_SAIDA-COMPANY-OS/EXTRACAO-LUCAX-REMESSA-2026-08-02.md`, do escritor concorrente —
> **`609`** arquivos vivos contra **`602`** no manifesto, `.git` excluido da contagem.
>
> #### ⛔ A sede que NAO serve — e a recusa esta medida, nao suposta
>
> **O commit `3f6effb9fe80138ed441b55f311a0a3af27fda89`, tag `M1.13.5.1`, chamado *"baseline:
> acervo LucaX Enterprise OS em M1.13.5.1"*, NAO e admissivel como ancora de bytes.** Testado
> em **worktree destacada**, e nao por leitura: **`446` de `607` reproduzem, `161` NAO**.
>
> Dos `161`, **`155`** estao em `_SAIDA-COMPANY-OS/` e **`6` estao DENTRO do acervo normativo**
> — e os seis sao **arquivos `CRLF`**, com `CR` contado em **bytes**, nunca por `grep` de linha:
>
> | Arquivo | Bytes `CR` no disco | Bytes `CR` no commit |
> |---|---|---|
> | **`foundation/10-artifact-framework.md`** — **`FND-10`, uma FUNDACIONAL** | **785** | **`0`** |
> | `governance/fitness/FIT-2026-006-validacao-interclasses.md` | 442 | *normalizado* |
> | `decisions/ADR-0006-adocao-do-enterprise-artifact-framework.md` | 409 | *normalizado* |
> | `foundation/revisao-arquitetural-artifact-framework-2026-07-28.md` | 354 | *normalizado* |
> | `governance/fitness/FIT-2026-002-artifact-framework.md` | 248 | *normalizado* |
> | `rfcs/RFC-0004-enterprise-artifact-framework.md` | 200 | *normalizado* |
>
> **`.gitattributes` com `* -text` existe no disco e NAO esta rastreado** — `git ls-files`
> devolve `0`, `git status` devolve `?? .gitattributes`. Foi criado **depois** do commit e
> **nao protegeu** o commit.
>
> **Consequencia, em uma frase:** o commit que se chama *baseline do acervo* **nao contem o
> acervo tal como o acervo se mede** — contem uma variante normalizada, e o `H-A` de `FND-10`
> tirado dele **nao reproduz** nenhum valor publicado em baseline alguma. **Por isso `git` NAO
> foi publicado como ponto de retorno, e a copia de arquivos foi.**

> ### Dos dois achados candidatos, `1` foi REGISTRADO e `1` continua declarado
>
> | Candidato | O que e | Estado em `1.3.0` |
> |---|---|---|
> | **`RD-103`** | **O ponto de retorno declarado de um ato `C3` pendente foi destruido por processo de manutencao externo**, sem que o ato ou o pacote fossem consultados. `0` bytes de acervo perdidos; **`7` arquivos** ficaram sem bytes pre-escrita recuperaveis | ✅ **REGISTRADO** por ordem do terceiro despacho — [artifact-registry §7](artifact-registry.md), item **126**. Severidade **Alta**, **elevada de Media pelo Soberano**; dono **DEP-GOV**; gatilho reescrito para *"antes de qualquer script de limpeza alcancar `_backups`"*, com a regra permanente que o despacho fixou |
> | **`RD-104`** | **O commit `3f6effb`, nomeado *baseline do acervo*, nao reproduz `161` de `607` arquivos** — `6` deles normativos e todos `CRLF`, incluindo **`FND-10`** —, e `.gitattributes` que impediria isso **nao esta rastreado**. **Terceiro membro da familia de `RD-101` nesta sessao**, e o unico que afirma a propriedade falsa **no proprio nome** | **Continua DECLARADO e NAO registrado.** O despacho mandou registrar **um**, e **um** foi registrado |
>
> #### ⚠️ A premissa do despacho foi CORRIGIDA no registro, por medicao no proprio script
>
> O despacho descreve `_FAXINA-2-apagar.bat` como *"script de faxina que apaga sem
> confirmacao"*. **O script PEDE confirmacao digitada** — `set /p RESP="Digite APAGAR e Enter
> para confirmar: "`, e so prossegue com a palavra exata — **e lista o que sera apagado**.
>
> **O defeito real e outro, e e pior.** A listagem exibida era `dir /b` de **primeiro nivel**:
> mostrava `_backups\` como **uma linha**, com a glosa *"23 snapshots + 3 `_backup-*` soltos
> (~1,33 GB)"*. **O operador confirmou sem poder ver que um ponto de retorno declarado de ato
> `C3` pendente estava la dentro.** Confirmacao cega **produz consentimento aparente**, e por
> isso e pior que a ausencia de confirmacao — a ausencia ao menos nao alega que alguem
> concordou.
>
> **A premissa foi corrigida em vez de repetida.** Repeti-la poria dentro do catalogo uma
> afirmacao falsa sobre um artefato conferivel — exatamente o defeito que `RD-101` nomeia, e
> desta vez **na propria inscricao que o nomeia**.
>
> #### Terceiro candidato, nascido ao executar o item (b) — DECLARADO e NAO registrado
>
> **O portao de split de `baseline.sh` da falso positivo quando a lista medida tem
> exatamente `1` arquivo.** `wc -l` so emite a linha `total` com **`2` ou mais** arquivos, de
> modo que `grep -c ' total$'` devolve `0`, o portao conclui *"`xargs` quebrou a chamada em
> `0` lotes"* e o instrumento **recusa medir arvore perfeitamente medivel**, com `EXIT=4`.
>
> **Descoberto ao CALIBRAR, nunca ao ler** — a arvore de teste de duas pastas tinha um `.md`
> so. **Nao alcanca o acervo**, que tem `228`. **Herdado de `IR-BL/1`**, presente nas tres
> geracoes. **Nao corrigido:** a autorizacao do item (b) alcanca `NAO_ACERVO`, e mexer no
> portao de split seria **mover o medidor sem ordem** — o mesmo motivo pelo qual `RD-81` nao
> foi corrigido pelo executor em 2026-08-01.

> ### 🗑️ Aviso de `1.1.0`, SUPERADO por `1.2.0` — preservado como marca, nao como afirmacao
>
> `1.1.0` publicava aqui um aviso de que a copia datada **havia mudado de lugar** para
> `_to_delete/_backups/`, com os **`602`** arquivos **integros** e **`0` bytes perdidos**, e
> deixava a sede *"a decidir pelo Soberano"*. **As duas afirmacoes de estado ficaram FALSAS**
> quando `_to_delete/` foi apagado: a copia nao mudou de lugar, **deixou de existir**.
>
> **O texto foi retirado, e nao conservado como se ainda valesse** — conserva-lo seria pôr
> dentro do proprio pacote o defeito que `RD-101` descreve. **O conteudo integral do aviso
> permanece recuperavel** em **§9.1, item `8`**, e na linha `1.1.0` do **Historico de versoes**,
> que `1.2.0` **nao tocou**. O quadro vigente e o de cima.

## 5. Classe, aprovador e o que se pede

| Campo | Valor | Fundamento **citado**, nunca por analogia |
|---|---|---|
| Classe | **`C3`** | `FND-04 §2`, bloco `C3` — *"altera ... direitos de decisao ou a propria Fundacao"* |
| Tipo | **2** | `FND-04 §2`, bloco `C3`, campo *Reversao* — *"emenda revogatoria, com mesmo rito"* |
| Instrumento | **RFC → analise de impacto → ADR → ratificacao** | `FND-04 §2`, bloco `C3`; `FND-07 §2.3` |
| `FIT` | **obrigatorio** | `CC-04`, `CV-07`, `QG-6` — em `C2` e `C3` |
| Aprova | **SOBERANO**, indelegavel | `FND-04 §2`; `FND-09 §8.2`, linha `FND` |
| Ratifica | **SOBERANO** | `FND-09 §8.2`, linha `FND`; `LM-02`; `SF-32` |

## 6. Minuta do ato soberano — **reemitida `1.1.0`, e NAO emitida**

> **O que mudou de `1.0.0` para `1.1.0`:** as **tres caixas** que o despacho marcou *(item IV
> segunda, item V primeira, item VIII primeira)*, o **item VIII** que o despacho mandou
> acrescentar, os **fundamentos** do Soberano transcritos sob cada decisao, e a **reconciliacao
> do item VII**, que `1.0.0` deixava incompativel com a caixa marcada em IV. **Itens I, II, III
> e VI: `0` bytes alterados**, exceto a nota de reconferencia acrescida ao fim do item I.
> Lista linha a linha em **§9.1**.

> **O que mudou de `1.1.0` para `1.2.0`: SO a nota de reconferencia do item I**, que passou de
> uma para duas reconferencias e de `12 de 12` para `20 de 20`. **`0` bytes em decisao alguma:
> nenhuma caixa foi marcada, desmarcada ou reescrita, e o item VII ficou como o segundo
> despacho determinou — MANTIDO.** Lista linha a linha em **§9.2**.

```
ATO SOBERANO — EMENDA QUE SANA RD-91
Data: <a definir pelo SOBERANO>

I.   RATIFICO o ADR-0032, classe C3, tipo 2, e promulgo as quatro versoes abaixo,
     que entram em vigor na data deste ato:

     (a) FND-09 1.6.0   H-P esperado: ea5efd35249c12f9587b7ded68a72d165a14b4adf399fdb2f9dc09dad395a8db
     (b) FND-11 1.1.0   H-P esperado: b2cff9f59e9f0d47034f02322d32438552d122c867ae0e954bb30fc42cd16e08
     (c) Carta DEP-PRD 1.2.0  H-P esperado: b9ac470c9227dd5bc4445d96faf1ff6b1d67c2d6f40ea6d0fd8a4f37180babdd
     (d) Carta DEP-EXE 1.2.0  H-P esperado: a07c765a9134b3f4caad76b2750c38beff53b6f7f2e8659717c42c881c9619e4

     Os quatro H-P acima foram RECONFERIDOS DUAS VEZES, depois de cada
     despacho e antes de cada devolucao: em 2026-08-02 10:40 e de novo em
     2026-08-02 11:41, esta segunda sobre a versao FINAL 1.2.0. Reproduziram
     nas duas. Na segunda a cobertura FECHOU: os 20 valores publicados em 3
     e 3.1 foram remedidos, e os 20 reproduzem — 0 bytes mudaram nos quatro
     candidatos e nos quatro arquivos vivos entre 1.1.0 e 1.2.0.
     Medicao e instrumento em 3.2 e 3.3.

II.  DECLARO que a aprovacao de Spec de classe C1 passa a ser de DEP-EXE, e que
     isso NAO altera a classe C1, NAO cria titular e NAO toca regra de conteudo
     de Spec.

III. DECLARO que SPC-001 NAO e reclassificada: nasceu C2 - Tipo 2 validamente.

IV.  QUANTO A LARGURA — decido a questao Q1 de RFC-0027 §9:
     [ ] a emenda fica em SPC, e RD-96 (PRJ) e RD-97 (TPL) seguem abertos
     [X] estendo a emenda a PRJ e TPL, e ordeno missao propria para redigi-la
     [ ] outra: ______________________________________________

     FUNDAMENTO. TPL tem 19 objetos vivos no acervo contra 1 de SPC: o defeito
     com mais efeito real ficaria de fora. ACOLHO a ressalva S2 de FIT-2026-025
     contra a recomendacao de RFC-0027 §6 — emendar uma linha e deixar duas
     quebradas ao lado, com a solucao visivel duas linhas acima, convida a
     copia sem rito.

     ALCANCE DA MISSAO ORDENADA: FND-09 §8.2, linhas PRJ (RD-96) e TPL (RD-97),
     e o que delas cascatear. A missao REDIGE; nao aplica. Este ato NAO se
     estende com ela: os quatro objetos do item I continuam quatro, e os quatro
     H-P continuam os do item I. RD-96 e RD-97 seguem ABERTOS ate o ato proprio
     que os sanar.

V.   QUANTO A C0 - decido a questao Q2 de RFC-0027 §9:
     [X] C0 permanece declarado em RD-91, sem emenda
     [ ] ordeno sanar C0 no mesmo rito

     FUNDAMENTO. Sanar agora obriga reescrever os quatro candidatos e recalcular
     os quatro H-P, atrasando o conserto de C1, que custa 3 artefatos por Spec.
     C0 so ocorre em Spec ja viva, e existe 1 Spec. O risco real e pequeno.

     RD-91 FECHA PARCIALMENTE por este ato: fecha quanto a C1 e PERMANECE ABERTO
     quanto a C0 - T2, com o alcance ja declarado no proprio achado.

VI.  A aplicacao e MINISTERIAL e cabe a DEP-GOV: mover status e ratificacao dos
     quatro objetos, reproduzir os quatro H-P acima, atualizar o catalogo mestre
     e emitir baseline nova. Se qualquer H-P nao reproduzir, a aplicacao PARA.

VII. O congelamento permanece em vigor. Este ato gera EXATAMENTE duas frentes, e
     nenhuma outra: a aplicacao ministerial do item VI, e a missao propria
     ordenada no item IV, que NAO comeca antes de concluida e conferida a
     aplicacao do item VI.

VIII. QUANTO A VERSAO — decido a questao Q3 de RFC-0027 §9, achado RD-99:
     [X] prevalece a PRATICA EXERCIDA: incremento MENOR, por AL-01/CC-02, e os
         numeros do item I ficam como estao — FND-09 1.6.0, FND-11 1.1.0,
         Cartas DEP-PRD e DEP-EXE 1.2.0
     [ ] prevalece a letra de FND-04 §2, bloco C3: incremento MAIOR, e o item I
         e reescrito, com os quatro candidatos e os quatro H-P refeitos
     [ ] outra: ______________________________________________

     DECIDO POR DELIBERACAO, E NAO POR OMISSAO. Os numeros de versao do item I
     NAO decidem Q3 calando: quem decide Q3 e este item, e os numeros do item I
     sao consequencia dele. Sem este item, o ato decidiria Q3 em silencio —
     exatamente o que RFC-0027 §9 e PS-2026-017 §7 recusaram fazer.

     FUNDAMENTO. MAIOR obrigaria a reescrever os quatro candidatos, refazer os
     quatro H-P, e deixaria ADR-0017, ADR-0019, ADR-0022 e ADR-0024 etiquetados
     errado — inconsistencia maior que a corrigida.

     O QUE ESTE ITEM NAO DECIDE. O conflito DENTRO de FND-04 — §2, bloco C3,
     mandando "nova versao MAIOR do documento" contra AL-01/CC-02, que fazem a
     versao seguir o EFEITO — NAO e resolvido aqui, e este ato NAO emenda
     FND-04: 0 bytes. A divergencia permanece ABERTA em RD-99, dono DEP-GOV,
     com o MESMO gatilho de RD-18: proxima emenda a FND-04, que e a sede do
     conflito.
```

## 7. Questoes ao Soberano — **as tres, RESPONDIDAS por despacho de 2026-08-02**

| # | Questao | Por que so ele decide | **Decisao** |
|---|---|---|---|
| **`Q1`** | A emenda fica em **`SPC`**, ou alcanca **`PRJ`** e **`TPL`**, que tem o defeito identico? | **Largura muda o alcance da norma.** Medido: `PRJ` poe `DEP-EXE` propondo **e** aprovando; `TPL` poe `DEP-GOV` propondo, revisando **e** aprovando. `FIT-2026-025` ressalva `S2` registra a assimetria | **ESTENDE a `PRJ` e `TPL`**, com **missao propria** ordenada para redigir. **Segunda caixa** do item IV. `RD-96` e `RD-97` seguem **abertos** ate o ato proprio |
| **`Q2`** | **`C0 · T2`** colapsa pela identica razao. Sanar agora, ou manter declarado? | Escopo, e o piso de criacao e `C1` — `C0` so ocorre em `Spec` ja viva | **PERMANECE declarado** em `RD-91`, sem emenda. **Primeira caixa** do item V. `RD-91` fecha **so quanto a `C1`** |
| **`Q3`** | `FND-04 §2`, bloco `C3`, manda registrar *"nova versao **MAIOR** do documento"*, e as emendas `C3` ja exercidas produziram versao **MENOR**, por `AL-01`. Qual prevalece? | Conflito **dentro de `FND-04`**, achado `RD-99`. **Esta emenda seguiu a pratica exercida** *(MENOR)* e **declara a divergencia em vez de a resolver em silencio** | **PREVALECE A PRATICA EXERCIDA — MENOR.** **Primeira caixa** do **item VIII**, casa criada por este despacho. `RD-99` segue **aberto**, gatilho de `RD-18` |

> **Por que `Q3` ganhou item proprio, e nao so a marcacao de uma caixa.** `Q1` e `Q2` ja
> tinham casa na minuta `1.0.0`; **`Q3` nao tinha**. Um ato que promulgasse `1.6.0`, `1.1.0`,
> `1.2.0` e `1.2.0` **sem dizer por que** decidiria `Q3` **pelos numeros**, calando — e o §7
> de `1.0.0` existe precisamente porque a sessao **recusou** decidir `Q3` em silencio. **O
> defeito foi apontado pela propria sessao que redigiu a minuta**, e o despacho mandou
> corrigi-lo **antes da assinatura**. O item VIII e essa correcao.

> **`0` das tres respostas altera os quatro objetos do item I, os quatro candidatos ou os
> quatro `H-P`.** Conferido: `Q1` difere a largura para missao propria; `Q2` mantem o estado
> declarado; `Q3` ratifica os numeros de versao que os candidatos **ja carregam**. Por isso a
> reconferencia de §3.2 pode reproduzir os valores de §3.1 — **e reproduziu, `12` de `12`**.

## 8. Rastreabilidade — decisao → instrumento → minuta

| Elo | Artefato |
|---|---|
| Achado | **`RD-91`**, [artifact-registry §7](artifact-registry.md), item 115 · [PT-2026-017 §6.2](relatorio-transicao-2026-08-02-primeira-spec.md) |
| Pergunta | [RFC-0027](../rfcs/RFC-0027-separacao-de-proponente-e-aprovador-na-spec-c1.md) |
| Decisao | [ADR-0032](../decisions/ADR-0032-separacao-de-proponente-e-aprovador-na-spec-c1.md) |
| Aptidao | [FIT-2026-025](fitness/FIT-2026-025-emenda-de-sf-10.md) — `apto-com-ressalva`, 4 ressalvas |
| Candidatos | `_candidatos-LucaX-Enterprise-OS-2026-08-02-M1.13.5.1/` — **fora do acervo** |
| Minuta | §6 deste pacote |
| Registro de missao | [PT-2026-018](relatorio-transicao-2026-08-02-emenda-sf-10.md) |
| Despacho que respondeu §7 | SOBERANO, **2026-08-02** — reemissao da minuta em `1.1.0` |
| Segundo despacho — **4 determinacoes pre-assinatura** | SOBERANO, **2026-08-02** — reemissao da minuta em `1.2.0`. Cumprimento em **§9.2** |
| Achados inscritos por ordem | **`RD-101`** e **`RD-102`** — [artifact-registry §7](artifact-registry.md), itens **124** e **125** |
| Rollback vigente do acervo | `_backups/LucaX-Enterprise-OS_2026-08-02_pre-reemissao-minuta-1.2.0` — **`608`** arquivos; manifesto `_missao-1-13-5-1-2026-08-02/evidencia/H-A-pre-reemissao-1.2.0.txt`, `sha256` `9696356e…e1ce` |

## 9. O que cada reemissao mudou, linha a linha — e o que deliberadamente NAO mudou

### 9.1 De `1.0.0` para `1.1.0` — e a **unica evidencia** do estado anterior

> **Esta subsecao e prova, nao so historico.** `PS-2026-017` foi editado de `1.0.0` para
> `1.1.0` **sem copia datada previa** — achado **`RD-102`**, inscrito no catalogo por ordem do
> segundo despacho, [artifact-registry §7](artifact-registry.md), item **125**. O `H-A`
> pre-edicao **`90526d04…a265`** *identifica* o que havia e **nao restaura**. **A tabela abaixo
> e o que restou do conteudo de `1.0.0`, e por isso `1.2.0` NAO a sobrescreveu: `0` bytes
> alterados nas tres tabelas desta subsecao.** So o titulo de §9 mudou, para abrir casa a §9.2.

**Ordenado pelo despacho, e feito:**

| # | Onde | Mudanca |
|---|---|---|
| 1 | §6, item **IV** | **Segunda** caixa marcada `[X]` — *estendo a emenda a `PRJ` e `TPL`, e ordeno missao propria*. **Texto da caixa: `0` bytes alterados** |
| 2 | §6, item **V** | **Primeira** caixa marcada `[X]` — *`C0` permanece declarado em `RD-91`*. **Texto da caixa: `0` bytes alterados** |
| 3 | §6, item **VIII** | **Criado.** Casa de decisao propria para `Q3`, com tres caixas, a primeira marcada, mais a declaracao de que a escolha e **deliberada e nao por omissao** |
| 4 | §3.2 | **Criada.** Reconferencia dos quatro `H-P` com instante, instrumento, controle positivo e resultado |

**Nao ordenado, e feito assim mesmo — declarado aqui para ser conferido ou riscado:**

| # | Onde | Mudanca | Por que |
|---|---|---|---|
| 5 | §6, item **VII** | Reescrito. `1.0.0` dizia *"este ato NAO gera missao alem da aplicacao ministerial do item VI"*; `1.1.0` diz **duas frentes** — a aplicacao do item VI e a missao ordenada no item IV, nesta ordem | **A caixa marcada em IV ordena uma missao.** Com o item VII de `1.0.0` intacto, o ato **ordenaria e proibiria a mesma missao, em dois itens**. Nao ha como marcar a segunda caixa de IV e conservar VII como estava |
| 6 | §6, itens **IV** e **V** | Acrescido, sob cada caixa marcada, o **FUNDAMENTO** do despacho, e o **alcance** do que foi decidido | O despacho **fundamentou** as tres escolhas. Minuta que registra a decisao e descarta o fundamento **perde o motivo**, e o motivo e o que impede a releitura errada depois |
| 7 | §6, item **I** | Acrescida ao fim a nota de que os quatro `H-P` foram **reconferidos** e reproduziram | O despacho pediu a devolucao *"com os quatro `H-P` reconferidos"*. **Nenhum valor foi alterado** |
| 8 | §4 | Acrescida a nota de que a copia datada citada como **rollback do acervo inteiro** foi movida para `_to_delete/_backups/` por manutencao externa, durante esta sessao. **Caminho publicado NAO reescrito** | Descoberto ao conferir o rollback. **Dados integros — `602` arquivos e `sha256` do `H-A` integral reproduzem** —, mas a sede virou transitoria. Reescrever para `_to_delete/` gravaria caminho transitorio em ato assinado. **Decisao do Soberano** |

> **Os itens 5, 6, 7 e 8 sao acrescimo desta sessao, nao palavra do Soberano.** Ficam
> **listados em separado** para que sejam riscados na assinatura, se ele quiser. **O item 5 e
> o unico cuja remocao pura deixa o ato incoerente** — se for para riscar, a caixa de IV
> tambem muda.

**Deliberadamente NAO mudado:**

| O que | Por que |
|---|---|
| Os **4** candidatos, fora do acervo | **`0` bytes.** Nenhuma das tres respostas os alcanca — conferido em §7. `H-A` reconferidos em §3.2 |
| Os **4** `H-P` de §3.1 | **`0` bytes.** Reproduzem em `2026-08-02 10:40` |
| Os **4** arquivos vivos — `FND-09`, `FND-11`, as duas Cartas | **`0` bytes.** `H-A` vigentes reconferidos em §3.2 |
| §1, §1.1, §2, §3, §4, §5, §8 | **`0` bytes.** A emenda alcanca frontmatter, cabecalho, §3.2 *(nova)*, §6, §7, §9 *(nova)* e historico |
| `artifact-registry.md` — a linha de `PS-2026-017` diz **276** linhas e *"3 questoes ao Soberano"* | **Ficou desatualizada, e fica declarado.** Reconciliar catalogo mestre e baseline e a **aplicacao ministerial do item VI**, que so corre **depois** do ato. Emitir baseline agora produziria baseline que a propria aplicacao superaria em seguida |
| `RD-101` — a evidencia de missao que nao reproduz *(§3.2)* | **Nao registrado. Registrar e ato de catalogo, e o congelamento esta em vigor.** Entra pela decisao do Soberano, com a aplicacao do item VI |
| `ADR-0032`, `RFC-0027`, `FIT-2026-025`, `PT-2026-018` | **`0` bytes.** O despacho nao os alcanca: `Q1`, `Q2` e `Q3` sao questoes **da minuta**, e a minuta mora aqui |

### 9.2 De `1.1.0` para `1.2.0` — o segundo despacho, determinacao por determinacao

**`3` das `4` determinacoes cumpridas na letra. `1` impossivel na letra, e a razao esta medida.**

| # | Determinacao | Estado | O que se fez |
|---|---|---|---|
| **1** | Item **VII** MANTIDO como reconciliado; mantidos tambem os outros tres acrescimos de §9.1 | ✅ **CUMPRIDA** | **`0` bytes** em §6 item **VII**, e `0` nos itens **6** e **7** de §9.1. O item **8** — a nota de §4 — foi **substituido**, e nao por escolha: a determinacao **2** ordenou reescrever §4. **O texto original continua recuperavel** em §9.1 e na linha `1.1.0` do Historico, **nao tocadas** |
| **2** | Mover a copia de rollback de `_to_delete/` para `_backups/`, conferir integridade pelo `sha256` do `H-A` integral, reescrever o caminho em §4 | ⛔ **IMPOSSIVEL na letra** · ✅ **cumprida no que restou** | **A copia NAO EXISTE.** `_to_delete/` apagado por `_FAXINA-2-apagar.bat` — `rd /s /q`, sem lixeira —, **antes** de o despacho chegar; `0` ocorrencias em `E:\LucasIA` ate `6` niveis. **Nao houve o que mover, nem o que conferir.** Feito no lugar: **nova copia datada** `_backups/…_pre-reemissao-minuta-1.2.0` *(`608` arquivos, manifesto conferido `608` de `608`)*; **§4 reescrito inteiro** com o alcance real do rollback — `595` de `602` reproduzem, `0` ausentes, `7` divergem, nomeados um a um; e o commit `3f6effb` **RECUSADO** como ancora, por medicao |
| **3** | **`RD-101`** REGISTRAR, sem corrigir, com dono e gatilho, junto a familia | ✅ **CUMPRIDA** | [artifact-registry §7](artifact-registry.md), item **124**. Severidade **Baixa**, dono **DEP-GOV**, gatilho *"aplicacao do ato"*. **`0` corrigidos**, **`0` missoes abertas**. Familia nomeada como ordenado: **`RD-75`** registrado, mais **`2`** membros **nao registrados** — o **docstring de `hashes.sh`**, que promete calibracao que o arquivo nao implementa, e o **`grep` de `CRLF`** que contava linhas em vez de bytes |
| **4** | A edicao sem copia datada — achado proprio, com o `H-A` pre-edicao e o diff de §9 como unica evidencia | ✅ **CUMPRIDA** | [artifact-registry §7](artifact-registry.md), item **125**, como **`RD-102`** — com **`90526d04…a265`** e **§9.1** nomeados como unica evidencia do estado anterior. Qualificado como o despacho mandou: **limite conhecido, declarado — nao incidente**. **Sanado para a frente**, e nao so registrado: a copia datada foi tomada **antes** de escrever `1.2.0` |
| **—** | Reemitir com §4 corrigido e os quatro `H-P` reconferidos sobre a versao final | ✅ **CUMPRIDA** | §4 reescrito; **§3.3** criada, com os `4` `H-P` remedidos em `2026-08-02 11:41` sobre esta versao e a cobertura **fechada em `20` de `20`** valores publicados |

**Mudancas linha a linha:**

| # | Onde | Mudanca |
|---|---|---|
| 1 | frontmatter | `versao` `1.1.0` → **`1.2.0`**; `resumo` reescrito |
| 2 | cabecalho | Bloco `1.2.0` **novo**, com as quatro determinacoes e o impedimento da `2` em destaque |
| 3 | **§3.3** | **Criada.** Reconferencia sobre a versao final, com controle positivo **exercido**, a razao de nao o dispensar, e `20` de `20` |
| 4 | §4, linha *Acervo inteiro* | **Reescrita.** De caminho unico para **PARCIAL**: identificador integro publicado, copia declarada **destruida** |
| 5 | §4 | **Bloco novo** — o que se perdeu, o que sobrou medido, os `7` divergentes um a um, a sede vigente e a recusa medida do `git` |
| 6 | §4 | Aviso de `1.1.0` **retirado**, substituido por marca de superacao. **Motivo: as duas afirmacoes de estado dele — *"mudou de lugar"* e *"`0` bytes perdidos"* — ficaram FALSAS** |
| 7 | §6, item **I** | Nota de reconferencia atualizada: de **uma** para **duas** reconferencias, e de `12 de 12` para **`20` de `20`** na segunda. **`0` bytes nos quatro `H-P` e nas quatro alineas** |
| 8 | §8 | **`3`** elos novos: o segundo despacho, os dois achados inscritos e a sede vigente do rollback |
| 9 | §9 | Titulo generalizado; o conteudo de `1.1.0` passou a **§9.1** com **`0` bytes** alterados nas suas tres tabelas; **§9.2** criada |
| 10 | Historico | Linha `1.2.0` acrescida. **`0` bytes** nas linhas `1.1.0` e `1.0.0` |

**Nao ordenado, e feito assim mesmo — declarado para ser conferido ou riscado:**

| # | O que | Por que |
|---|---|---|
| 11 | A **copia datada** tomada antes de escrever `1.2.0` | Regra propria: copia datada precede sobrescrita. **E exatamente o que faltou em `1.1.0` e virou `RD-102`.** Reemitir de novo sem copia, com o achado ja na mesa, seria escrever o mesmo defeito duas vezes — e a segunda ja nao seria limite, seria escolha |
| 12 | A **recusa medida do commit `3f6effb`** como ancora de rollback, em §4 | **Eu ia publica-lo como sede.** Testado em worktree destacada antes de escrever, ele **reprovou**: `161` de `607` nao reproduzem, e `FND-10` perde os `785` bytes `CR`. **Publicar sem testar teria posto no §4 uma sede que nao restaura** — e §4 e justamente a secao que este despacho mandou consertar |
| 13 | **`RD-103`** e **`RD-104`** declarados em §4 e **NAO registrados** | O despacho mandou registrar **dois**, e **dois** foram registrados. Estes nasceram **ao executar** a determinacao `2` e seguem o caminho que `RD-101` seguiu em `1.1.0`: declarados no pacote, registrados **se e quando** ele mandar |
| 14 | `artifact-registry` foi a **`2.21.0`**, com linha de historico | A inscricao ordenada **e** emenda de catalogo. Inscrever sem versao nem historico deixaria o frontmatter contradizendo o corpo — **`AC-11`**, o mesmo defeito que a emissao `2.20.0` corrigiu declarando em vez de emendar em silencio |

> **Os itens 11 a 14 sao acrescimo desta sessao, nao palavra do Soberano.** O **12** e o unico
> cujo desfazimento muda o conteudo do ato: riscar a recusa do `git` obriga a reescrever a sede
> de rollback em §4.

**Deliberadamente NAO mudado:**

| O que | Por que |
|---|---|
| Os **4** candidatos, fora do acervo | **`0` bytes.** Nenhuma determinacao os alcanca. `H-A` e `H-N` reconferidos em §3.3 |
| Os **4** arquivos vivos do item I | **`0` bytes.** `H-A` **e** `H-N` reconferidos em §3.3 — a cobertura que §3.2 deixava aberta |
| Os **4** `H-P` de §3.1 | **`0` bytes.** Reproduzem em `2026-08-02 11:41` |
| §6 — itens **II** a **VIII**, e as decisoes e caixas do item **I** | **`0` bytes.** So a nota de reconferencia do item I mudou. **Nenhuma caixa foi marcada, desmarcada ou reescrita** |
| §1, §1.1, §2, §3, §3.1, **§3.2**, §5, §7 | **`0` bytes.** §3.2 fica **intacta**: e o registro da primeira reconferencia, e §3.3 nao a substitui |
| §9.1 | **`0` bytes nas tres tabelas.** E a unica evidencia do estado `1.0.0` — `RD-102` |
| `ADR-0032`, `RFC-0027`, `FIT-2026-025`, `PT-2026-018` | **`0` bytes.** As determinacoes alcancam a minuta e o catalogo, nao a cadeia de instrumentos |
| Os **7** arquivos divergentes de §4 | **`0` restaurados.** Restaurar seria **reverter**, e reverter nao foi ordenado — o congelamento esta em vigor |
| Baselines | **`0` emitidas.** Reconciliar catalogo e baseline e a **aplicacao ministerial do item VI**, que so corre **depois** do ato |

### 9.3 De `1.2.0` para `1.3.0` — o terceiro despacho, determinacao por determinacao

**O despacho trouxe `3` determinacoes e `1` inscricao. As `4` foram cumpridas na letra** — e
**nenhuma** foi impossivel, ao contrario do segundo despacho.

| # | O que o Soberano ordenou | Estado | Onde, e com que prova |
|---|---|---|---|
| **(c)** | Adquirir token **antes de qualquer escrita**, e sempre; gravar a regra no `CLAUDE.md` da raiz | ✅ **CUMPRIDA, E PRIMEIRO** | **`fencing_token` 16** adquirido as **`13:24:25`**, **antes da primeira escrita** — `estado_fenceado`, instrumento calibrado, ponto de retorno e copia datada declarados. Regra gravada no `CLAUDE.md`, com o **fundamento medido** ao lado: as tres escritas de `10:47`, `11:50` e `11:59` ocorreram com o lease **liberado**. **Cumprida primeiro porque e ela que autoriza as outras** |
| **(b)** | Declarar `.git` e `.gitattributes` em `NAO_ACERVO`, com **bump e registro**; depois medir a baseline **pelo instrumento vigente, sem variante, em duas execucoes** | ✅ **CUMPRIDA** | **`IR-BL/3`**, `sha256` `0d4f1b3d…4ad7`. **`ACERVO` inalterado**; portao **nao afrouxado**. **`4` provas de calibracao antes do uso**, entre elas **reversao** e **controle positivo da propria mudanca**. Medicao: **`228 · 67.771 · 1513fd45…eb5f`**, `2` execucoes, ambas `EXIT=0` — **§10** |
| **(a)** | Apagar a copia parcial de `12:04`; **registrar a remocao e o motivo**; nao tentar completar | ✅ **CUMPRIDA** | Apagada sob o token 16, com **prova previa de que nenhum byte unico se perde** — `106` de `107` identicos ao vivo, `1` sobrevivendo em **duas** copias datadas, **`0`** ausentes em toda parte — e **reconferida DEPOIS da remocao**. Manifesto do apagado preservado em `evidencia/H-A-copia-parcial-12-04-APAGADA.txt`. **`0` tentativas de completar:** o estado pre-escrita nao existe mais para ser copiado |
| **Inscricao** | Registrar o achado da faxina, com dono e gatilho, **sem missao**, severidade **Alta** | ✅ **CUMPRIDA** | **`RD-103`**, [artifact-registry §7](artifact-registry.md) item **126**, **Alta**, dono **DEP-GOV**, gatilho *"antes de qualquer script de limpeza alcancar `_backups`"*, **`0` missoes abertas**. **Com a premissa do despacho corrigida por medicao no script** — ver o quadro em §4 |

#### O que esta sessao acrescentou SEM ordem — separado para ser riscado

**Tres itens.** Nenhum foi pedido; os tres nascem de medicao feita ao executar o que foi
pedido. **Ficam nomeados aqui para que o Soberano os risque se quiser.**

| # | Acrescimo | Por que se acrescentou |
|---|---|---|
| `1` | **Correcao de um paragrafo FALSO no proprio `CLAUDE.md`** — ele afirmava que o arquivo *"nao esta declarado"* e que o instrumento *"recusa medir"*, quando `CLAUDE.md` entrou em `NAO_ACERVO` ainda em `IR-BL/2` e **`RD-81` esta FECHADO** desde 2026-08-01 | Foi encontrado **ao gravar a regra de lease no mesmo arquivo**. Deixa-lo seria mais um membro da familia de `RD-101` **dentro do arquivo que instrui quem mede**. `CLAUDE.md` **declara-se nao-artefato**: a correcao **nao exige** `ADR`, hash, baseline nem ato |
| `2` | **Declaracao do ESCRITOR CONCORRENTE** em §4 e no lease — sessao alheia tocou `_SAIDA-COMPANY-OS/` as `13:04:47` e `13:07:32` | Descoberto ao conferir a copia parcial. **`0` arquivos do acervo tocados por ela**, e a baseline foi **reconferida depois** sem mover um digito. Mudou **como** a copia datada foi tomada — arvore em escrita nao vira ponto de retorno —, e isso tinha de ficar escrito |
| `3` | **Declaracao do defeito do portao de split** *(falso positivo com lista de `1` arquivo)* | Nasceu **ao calibrar** o instrumento do item (b). Declarado e **nao corrigido**: corrigi-lo seria mover o medidor sem ordem |

---

## 10. A baseline, medida pelo instrumento vigente — item (b) do terceiro despacho

**Instante:** `2026-08-02 13:24`. **Instrumento:** `IR-BL/3`, `sha256`
`0d4f1b3db309c88ef44e1c8ef6aa6588230648c44880f6b772601660082f4ad7`, em
`_missao-1-13-5-1-2026-08-02/ferramentas/baseline.sh`. **Sem variante** — o instrumento
vigente, tal como esta no disco.

| Medida | Valor |
|---|---|
| **Artefatos** | **228** |
| **Linhas** | **67.771** |
| **Impressao digital** | **`1513fd4585e95ef3bfbe10f291010f957c04edcb43f32f9cd05a03153f2feb5f`** |

**Duas execucoes independentes, ambas `EXIT=0`, resultado identico nos tres valores.**

### 10.1 Por que o instrumento precisou mudar antes de medir

**Ate o item (b), a baseline NAO PODIA ser medida** — e a impossibilidade era o instrumento
funcionando. A manutencao externa de 2026-08-02 criou `.git` e `.gitattributes` na raiz;
nenhum dos dois estava declarado, **nem como acervo nem como nao-acervo**; e o **portao de
raiz** parou com `EXIT=2`:

```
ERRO — PORTAO DE RAIZ: entrada nao declarada na raiz do acervo: .git .gitattributes
Nem acervo nem nao-acervo. A baseline NAO foi medida. Achado RD-53.
```

**Quarta ocorrencia da familia `RD-53`/`RD-81`** — depois de `_candidatos/`, de `products/`
e do proprio `CLAUDE.md`. **A recusa nunca foi falha: e o unico mecanismo do acervo que
transforma contagem errada em recusa em vez de silencio.**

### 10.2 Calibracao antes do uso — `4` provas, e uma delas e por reversao

| Prova | O que testa | Resultado |
|---|---|---|
| **1 — Inercia** | Sobre arvore **sem** `.git`, `IR-BL/3` tem de devolver **exatamente** o que `IR-BL/2` devolve | ✅ Ambos: **`228 · 67.493 · a67b466d…7f81`** sobre a copia de `10:47`. **A mudanca nao move o medido** |
| **2 — Portao vivo, com REVERSAO** | Entrada nao declarada **recusa**; retirada a entrada, **mede** | ✅ `EXIT=2` com `zzz-nao-declarado`; `EXIT=0` sem ela. **O verde nao e verde de portao morto** |
| **3 — Controle positivo da MUDANCA** | `IR-BL/3` tem de medir a arvore **com** `.git` que `IR-BL/2` recusa | ✅ `IR-BL/3` `EXIT=0`; `IR-BL/2` `EXIT=2` **sobre a mesma arvore**. A diferenca entre as geracoes e **exatamente** esta |
| **4 — Valor invariante** | O valor medido **nao pode mudar** com a chegada do `.git` | ✅ Identico com e sem: `2 · 5 · fb24fcfa…c979` |

> **O que a prova 2 impede.** Sem ela, um portao que aceitasse tudo passaria nas provas 1, 3
> e 4 — todas medem valores, e valores continuariam certos. **So a reversao distingue portao
> que funciona de portao que nao roda.**

### 10.3 O que a medicao devolve — e o que ela NAO autoriza

`BL-2026-08-02-02` declara **`228 · 67.279 · d9e8b706…e213`**. A medicao devolve
**`+492` linhas**, com **contagem de artefatos INALTERADA**.

**O delta esta atribuido, nao suposto.** Apenas **`3`** `.md` do acervo foram tocados apos o
instante de `BL-02` — [`artifact-registry.md`](artifact-registry.md) as `11:50`,
[`roadmap-canonico.md`](roadmap-canonico.md) as `11:57` e **este pacote** as `11:59` —, que
sao exatamente as escritas de `1.1.0`, `1.2.0` e da inscricao ordenada de `RD-101` e
`RD-102`. Confirmado por medicao intermediaria sobre as duas copias datadas:
**`67.493`** as `10:47`, **`67.538`** as `11:40`, **`67.771`** agora. **Monotonico, e cada
degrau tem dono.**

> ### ⛔ Medir NAO e emitir, e esta secao nao emite
>
> **`0` baselines emitidas.** A divergencia acima e **declarada desde a emissao 2.21.0 do
> catalogo** e continua sendo: reconciliar catalogo e baseline e a **aplicacao ministerial do
> item VI**, que so corre **depois** do ato. Emitir `BL-2026-08-02-03` agora produziria
> baseline que a propria aplicacao superaria em seguida.
>
> **Consequencia que o Soberano precisa ver antes de assinar:** as linhas *Artefatos*,
> *Linhas* e *Baseline vigente* de §2 do catalogo seguem **DESATUALIZADAS POR DECLARACAO**, e
> §2 ganhou uma linha nova que publica o valor medido ao lado delas, **sem substitui-las**.
> **A cada reemissao desta minuta a distancia cresce** — foram `+214`, depois `+259`, agora
> `+492`. **Nao e defeito novo; e o custo, medido, de manter o congelamento.**

---

## Historico de versoes

| Versao | Data | Autor | Mudanca |
|---|---|---|---|
| 1.3.0 | 2026-08-02 | DEP-GOV | **Terceiro despacho do Soberano — `3` determinacoes e `1` inscricao, `4` de `4` CUMPRIDAS NA LETRA, e nenhuma impossivel.** **`(c)` foi executada PRIMEIRO, porque e ela que autoriza as outras:** **`fencing_token` 16** adquirido as **`13:24:25`**, **`0` bytes escritos no acervo antes dele** — e esta e **a primeira emissao do acervo sob lease adquirido**. A regra permanente foi gravada no `CLAUDE.md` da raiz com o **fundamento medido** ao lado: as tres escritas de `10:47`, `11:50` e `11:59` ocorreram com o lease **liberado**; sem regra explicita, a sessao escreve sem lease. **`(b)`** `baseline.sh` vai a **`IR-BL/3`** *(`sha256` `0d4f1b3d…4ad7`, primeira geracao a carregar campo de versao; as duas anteriores identificadas por hash)*: `.git` e `.gitattributes` entram em `NAO_ACERVO` pelo **mesmo fundamento de `RD-81`**, com **`ACERVO` inalterado** e **portao nao afrouxado** — a lista ganhou duas entradas **do lado nao medido**. **`4` provas de calibracao antes do uso**, entre elas **REVERSAO** *(recusa `EXIT=2`; retirada a entrada, mede `EXIT=0`)* e **controle positivo da propria mudanca** *(`IR-BL/3` mede o que `IR-BL/2` recusa, na mesma arvore)* — **so a reversao distingue portao que funciona de portao que nao roda**. §10 nova publica a medicao: **`228 · 67.771 · 1513fd45…eb5f`**, **`2` execucoes**, ambas `EXIT=0`, **`+492` linhas** sobre `BL-2026-08-02-02` com **contagem de artefatos INALTERADA** e **delta atribuido** aos `3` unicos `.md` tocados apos `BL-02`, confirmado por medicao intermediaria nas duas copias datadas *(`67.493` → `67.538` → `67.771`, monotonico, cada degrau com dono)*. **`(a)`** a copia parcial de `12:04` — `107` arquivos, `0` dos `9` diretorios de acervo, sem manifesto, nome afirmando *RECONSTRUIDO* sem o ser — **APAGADA**, com **prova PREVIA de que nenhum byte unico se perde** *(`106` de `107` identicos ao vivo; o `1` divergente sobrevive em **duas** copias datadas; **`0`** ausentes em toda parte)* e **reconferencia DEPOIS da remocao**; manifesto do apagado preservado. **`0` tentativas de completar**, como o despacho mandou. **Inscricao:** **`RD-103`** REGISTRADO no catalogo, item **126**, severidade **Alta** *(elevada de Media)*, dono **DEP-GOV**, gatilho *"antes de qualquer script de limpeza alcancar `_backups`"*, **`0` missoes**. **⚠️ A PREMISSA DO DESPACHO FOI CORRIGIDA, por medicao no proprio script:** ele descreve `_FAXINA-2-apagar.bat` como apagando *"sem confirmacao"* — **o script PEDE confirmacao digitada** e lista o que apagara; **o defeito e que a listagem era `dir /b` de primeiro nivel**, e o operador **confirmou sem poder ver** que um ponto de retorno declarado estava dentro de `_backups\`. **Confirmacao cega e pior que ausencia de confirmacao, porque produz consentimento aparente.** Corrigida em vez de repetida: repeti-la poria afirmacao falsa dentro da propria inscricao que denuncia afirmacao falsa. §3.4 nova: os **`4`** `H-P` reconferidos as `13:29` sobre esta versao, com controle positivo **exercido pela terceira vez** — **`20` de `20`**. §4 conferido contra o acervo vivo: **`593` de `602` reproduzem, `0` de `602` ausentes** — nenhum arquivo sumiu —, **`9` divergem**, as `2` novas nomeadas *(`CLAUDE.md`, desta sessao; `01_ESTADO-DA-ANALISE.md`, do escritor concorrente)*; **`0` das `9` sao fonte normativa e `0` sao objeto do item I**. **Sede vigente do rollback PROVADA FIEL, nao apenas datada:** `_backups/…_pre-terceiro-despacho`, **`234`** arquivos, manifesto `12dac577…330d`, conferido `234` de `234` **e reproduzindo os tres valores da baseline** — copia que nao reproduz a baseline identifica bytes sem provar que sao **os** bytes. **ESCRITOR CONCORRENTE detectado, medido e declarado:** sessao alheia tocou `_SAIDA-COMPANY-OS/` as `13:04:47` e `13:07:32`; **`0` arquivos do acervo tocados por ela**; a baseline foi **reconferida depois** sem mover um digito; e por isso a copia datada **exclui e declara** `_SAIDA-COMPANY-OS/` *(alvo movel — instante, nao data)* e `.git` *(nao fiel a bytes, `RD-104`)*. **`3` acrescimos SEM ordem, nomeados em §9.3 para serem riscados:** a correcao de um paragrafo **falso** no proprio `CLAUDE.md` *(afirmava `RD-81` aberto, fechado desde 2026-08-01)*, a declaracao do escritor concorrente e a declaracao de um **defeito herdado do portao de split** *(falso positivo com lista de `1` arquivo, descoberto ao calibrar, nao alcanca o acervo, **nao corrigido** porque mover o medidor sem ordem seria mover o numero)*. **`RD-104` continua DECLARADO e nao registrado — o despacho mandou registrar um, e um foi registrado.** **`0` atos emitidos · `0` fontes emendadas · `0` bytes nos 4 candidatos · `0` nos 4 arquivos vivos · `0` nos quatro `H-P` · `0` em caixa alguma de §6 · `0` em `ADR-0032`, `RFC-0027`, `FIT-2026-025` e `PT-2026-018` · `0` baselines emitidas · `0` arquivos restaurados · `0` missoes abertas.** **A minuta continua NAO emitida, NAO assinada e NAO aplicada. Congelamento em vigor.** **Conjunto de mudanca desta sessao, fechado e conferido contra a copia datada das `13:24`: `230` identicos, `4` ALTERADOS, `0` criados, `0` removidos, `0` ausentes.** Os quatro sao **`3` do acervo** — este pacote, [`artifact-registry.md`](artifact-registry.md) e [`roadmap-canonico.md`](roadmap-canonico.md) — **mais `CLAUDE.md`**, que e **`NAO_ACERVO`** e por isso **nao move nenhum dos tres valores**. **`H-A` de fechamento, tomados depois da ultima escrita de cada um:** `artifact-registry.md` = `333cf35b8d7d81a3ae2c229d9aa35302f9c42aae092fe4ddd83d2919f3ab821a` · `roadmap-canonico.md` = `0853bfe9ab00af681b46281316595be97aaaa128b785ebcb8fdcec3d1905fefc` · `CLAUDE.md` = `50aa1c18f7ba3c0b762e0ae472fd1af6c3b4158fbcca9a4650028687682f8b62`. **`CR` = `0` bytes nos quatro**, contado por `tr`, nunca por `grep` de linha. **`0` links relativos quebrados.** **O `H-A` deste pacote NAO se publica aqui dentro:** o valor mudaria ao ser escrito — quem quiser conferi-lo mede o arquivo, e `_backups/…_pre-terceiro-despacho` da o estado imediatamente anterior. **Baseline apos esta escrita, pelo instrumento vigente, `2` execucoes: `228 · 68.002 · 0b548d81510a268c3e843d66ee44fbdae0a3accf543f20da2bfb9266f9fa6f8b`** — contagem de artefatos **inalterada**, **`0` emitidas** pela razao de §10. |
| 1.2.0 | 2026-08-02 | DEP-GOV | **Segundo despacho do Soberano — `4` determinacoes pre-assinatura: `3` cumpridas na letra, `1` IMPOSSIVEL, e a razao esta medida.** **`1`** item **VII** MANTIDO como reconciliado, com os outros tres acrescimos de §9.1 — **`0` bytes** em §6 item VII. **`3`** **`RD-101` REGISTRADO sem correcao** no catalogo mestre, item **124**, severidade Baixa, dono **DEP-GOV**, gatilho *"aplicacao do ato"*, **`0` missoes**, com a **familia nomeada** como ordenado: `RD-75` registrado, mais o **docstring de `hashes.sh`** *(promete calibracao que o arquivo nao implementa)* e o **`grep` de `CRLF`** *(contava linhas, nao bytes)*, ambos **nao registrados**. **`4`** a edicao sem copia datada REGISTRADA como **`RD-102`**, item **125**, com **`H-A` pre-edicao `90526d04…a265`** e **§9.1** nomeados como unica evidencia do estado anterior, e **qualificada como o despacho mandou: limite conhecido e declarado, nao incidente** — e **sanada para a frente**, com copia datada tomada **antes** de escrever. **⛔ `2` — A COPIA DE ROLLBACK ORDENADA A MOVER NAO EXISTE MAIS.** `_to_delete/` foi apagado por `_FAXINA-2-apagar.bat` *(`rd /s /q`, sem lixeira)* **antes** de o despacho chegar, levando `LucaX-Enterprise-OS_2026-08-02_pre-instrumentos-1-13-5-1`; **busca em `E:\LucasIA` ate `6` niveis devolve `0` ocorrencias** de `_to_delete` e `0` de `*pre-instrumentos*`. **O aviso escrito em `1.1.0` — *"basta que alguem o esvazie … e ninguem seria avisado"* — foi cumprido pelos fatos antes de ser decidido.** §4 **reescrito inteiro** com o que sobrou, medido: o manifesto `H-A-rollback-pre-escrita.txt` **reproduz** *(`602` linhas, `sha256` `a44370df…4538`)* e conferido contra o acervo vivo da **`595` de `602` reproduzindo, `0` de `602` ausentes e `7` divergentes** — nomeados um a um, **`0` deles fonte normativa e `0` deles objeto do item I**: `4` indices, o catalogo mestre, o roadmap *(sem autoridade normativa)* e o `workspace.json` do IDE. **Sede vigente publicada:** `_backups/LucaX-Enterprise-OS_2026-08-02_pre-reemissao-minuta-1.2.0`, **`608`** arquivos, manifesto `sha256` `9696356e…e1ce`, conferido **`608` de `608`** contra o disco. **`git` RECUSADO como ancora, por medicao e nao por opiniao:** o commit `3f6effb`, tag `M1.13.5.1`, nomeado *"baseline: acervo LucaX Enterprise OS"*, testado em **worktree destacada** reproduz **`446` de `607`** — **`161` nao**, dos quais **`6` dentro do acervo normativo e todos `CRLF`**, incluindo **`FND-10`, uma Fundacional, com `785` bytes `CR` no disco e `0` no commit**; `.gitattributes` com `* -text` existe e **nao esta rastreado**. **Terceiro membro da familia de `RD-101` nesta sessao — e o unico que afirma a propriedade falsa no proprio nome.** §3.3 nova: os **`4`** `H-P` reconferidos em `2026-08-02 11:41` **sobre esta versao**, com controle positivo **exercido** — e a cobertura **FECHADA**: dos **`20`** valores publicados em §3 e §3.1, **`20` reproduzem**, contra os `16` de §3.2. §9 passa a ter **§9.1** *(o texto de `1.1.0`, `0` bytes alterados — e a evidencia de `RD-102`)* e **§9.2** *(as quatro determinacoes, uma a uma, com o que foi acrescido sem ordem separado para ser riscado)*. **`RD-103`** *(rollback de ato destruido por manutencao externa)* e **`RD-104`** *(o commit que se chama baseline e nao reproduz)* **DECLARADOS e NAO registrados** — o despacho mandou registrar dois, e dois foram registrados. **`0` bytes nos 4 candidatos · `0` nos 4 arquivos vivos · `0` nos quatro `H-P` · `0` em caixa alguma de §6 · `0` em `ADR-0032`, `RFC-0027`, `FIT-2026-025` e `PT-2026-018` · `0` baselines emitidas · `0` arquivos restaurados · `0` missoes abertas.** **Conjunto de mudanca desta sessao, fechado: `3` arquivos do acervo** — este pacote, `artifact-registry.md` e `roadmap-canonico.md` —, mais `.obsidian/workspace.json`, **estado volatil de IDE que nenhuma escrita desta sessao tocou**. **`H-A` de fechamento, tomados depois da ultima escrita de cada um:** `artifact-registry.md` = `5b59ae7f5ec49f19fc1b415406760669461db2711a371880166a5653e76fc9aa` · `roadmap-canonico.md` = `f933e2721ba310670c21ccb26fc8e37acab0574d86f62c8d2114e0b8e99a9c80`. **O `H-A` deste pacote NAO se publica aqui dentro:** o valor mudaria ao ser escrito. Quem quiser conferi-lo mede o arquivo — e a copia datada de `_backups/…_pre-reemissao-minuta-1.2.0` da o estado imediatamente anterior. **A minuta continua NAO emitida, NAO assinada e NAO aplicada. Congelamento em vigor.** |
| 1.1.0 | 2026-08-02 | DEP-GOV | **Despacho do Soberano: as tres questoes de §7, RESPONDIDAS, e a minuta REEMITIDA — ANTES da assinatura.** **`Q1`** ESTENDE a `PRJ` e `TPL`, com **missao propria** ordenada *(segunda caixa do item IV)*, acolhendo a ressalva **`S2`** de `FIT-2026-025` **contra** a recomendacao de `RFC-0027 §6`: `TPL` tem **19** objetos vivos contra **1** de `SPC`, e o defeito com mais efeito real ficaria de fora. **`Q2`** `C0` PERMANECE declarado em `RD-91` *(primeira caixa do item V)*: sanar agora obrigaria reescrever os quatro candidatos e refazer os quatro `H-P`. **`Q3`** prevalece a **PRATICA EXERCIDA — incremento MENOR**, e para isso §6 recebe o **item VIII**, casa de decisao que `1.0.0` nao tinha: sem ele o ato decidiria `Q3` **pelos numeros de versao do item I, em silencio** — que e exatamente o que §7 recusou fazer. **O defeito foi apontado pela propria sessao que redigiu a minuta.** §3.2 nova: os **`4`** `H-P` **RECONFERIDOS** em `2026-08-02 10:40` com o mesmo instrumento *(`sha256` `729cafad…87ec`, com controle positivo sobre si antes do uso)* — **`4` de `4` reproduzem**, e com os `H-A`/`H-N` dos candidatos e os `H-A` dos vigentes, **`12` de `12`**; a projecao `O4` provada alcancar **`2`** linhas por objeto, nem uma a mais. **Divergencia encontrada e declarada, na EVIDENCIA e nunca no pacote:** `hashes-candidatos.txt` da missao **nao reproduz** para as duas Cartas — **`6`** valores de **`24`** —, porque foi escrito as **01:31:14** e os dois arquivos foram tocados as **01:39:33**; os seis valores superados **so existem fora do acervo**, e nenhum artefato os propaga. Candidato a **`RD-101`**, **nao registrado** por congelamento. **Segunda divergencia declarada, em §4:** a copia datada citada como **rollback do acervo inteiro** foi movida para **`_to_delete/_backups/`** por manutencao externa **durante esta sessao** — **dados integros**, `602` arquivos e `sha256` do `H-A` integral **reproduzindo**, mas a sede de rollback de um ato `C3` passou a ser um diretorio marcado para exclusao. **Caminho publicado NAO reescrito**, por ser `_to_delete/` transitorio; decisao do Soberano antes da assinatura. §7 passa de *questoes* a *questoes respondidas*; §9 nova lista **linha a linha** o que mudou, **separando o que o despacho ordenou do que esta sessao acrescentou** — o item **VII** reconciliado, os **fundamentos** transcritos e a nota de reconferencia do item I —, para que sejam riscados na assinatura se ele quiser. **`0` bytes nos 4 candidatos · `0` nos 4 arquivos vivos · `0` nos quatro `H-P` · `0` em `ADR-0032`, `RFC-0027`, `FIT-2026-025` e `PT-2026-018` · `0` no catalogo mestre · `0` baselines emitidas.** **A minuta continua NAO emitida, NAO assinada e NAO aplicada.** |
| 1.0.0 | 2026-08-02 | DEP-GOV | Criacao. Submete ao Soberano a emenda **`C3 · Tipo 2`** que sana **`RD-91`**: a aprovacao de `Spec` `C1` passa do **proprietario** — que e quem a propoe — para **DEP-EXE**. **4 objetos**, com `H-A`, `H-N` e `H-P` publicados: `FND-09` **1.6.0**, `FND-11` **1.1.0**, Cartas `DEP-PRD` e `DEP-EXE` **1.2.0**. **Diff literal e reversivel** em §2; **`0` bytes escritos nos arquivos vivos**, conferido por `sha256`. Traz os **tres numeros de custo** em §1.1: **5** *(medido)*, **2** *(derivado de norma)* e **7** *(medido no precedente `PS-2026-008`)*. §7 leva **tres questoes** que so o Soberano decide — a **largura** *(`PRJ` e `TPL` tem o defeito identico e ficam de fora)*, **`C0`**, e o conflito *versao MAIOR × `AL-01`* dentro de `FND-04`. Minuta do ato **redigida e NAO emitida**, com os quatro `H-P` como condicao de parada da aplicacao. |
