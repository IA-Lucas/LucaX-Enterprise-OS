---
name: contrato-fabrica-acervo-custa-c3
description: Gravar o contrato fabrica-acervo (F35 §2.2) com forca normativa custa C3 com ato; so registrar que o Fundador decidiu custa C1 — e nao cabe em governance/
metadata: 
  node_type: memory
  type: project
  originSessionId: ebf4b3b3-1f86-4ef3-a418-6c39a9df3057
  modified: 2026-08-09T01:31:30.014Z
---

O texto do contrato fabrica↔acervo esta em
`E:\Research\missoes\F35-O-CONTRATO-ENTRE-FABRICA-E-ACERVO.md` §2.2 — cinco linhas, sem
acento, com regra explicita de **nao reacentuar, nao reescrever, nao renumerar** (§2.1).
**Ate 2026-08-08 nao foi gravado no acervo.** Custo medido nessa data, e sao **duas
perguntas diferentes**:

- **Registrar que o Fundador decidiu** (sem forca normativa) → Diretiva `MSG`, envelope
  `FND-05 §3`, em `memory/operacional/`. Classe **`C1`**, sem ato, sem ADR, sem RFC.
  ⚠️ **Nao faz o contrato valer** — vira registro de que algo foi dito.
- **Fazer o contrato VALER** → **`C3`**. As hipoteses de `FND-04 §2` incidem **tres vezes**:
  **hierarquia normativa** (linha 1 institui zona isenta de rito; linha 4 declara onde fica
  a autoridade), **direitos de decisao** (linha 5 poe o gatilho de admissao do acervo na
  `F34`, que vive **fora** do acervo), e **a propria Fundacao** (linha 3 pede
  `executor`=`AGT`, que `FND-02 §10` proibe nesta fase). Rito: `RFC` → analise de impacto →
  `ADR` → ratificacao do SOBERANO (indelegavel) + plano de reversao + `FIT` (`CV-07`) + `REV`.

**Nota de Decisao esta VEDADA por texto, nao por juizo:** `FND-10 §4.2` poe *"Precedente —
vira ADR"* como conteudo proibido do tipo, e incidem `DR-5` (este contrato **e** o
precedente dos dezenove setores) e `DR-3` (afeta os nove Departamentos).

**Economia disponivel:** o rito `C3` bate exatamente com o custo fixo que a
[[m-01-relatorio-so-na-transcricao]] mediu para a migracao dos setores — **o contrato e a
migracao cabem no mesmo ato**.

**NAO cabe em `governance/`, ao lado do `roadmap-canonico`, por duas razoes medidas:**
`FND-10 §4` so autoriza ali `EXC`, `INC`, `FIT` e o indice — `ADR` vive em `decisions/`; e
o `roadmap-canonico.md` **nao e vizinho a imitar, e achado `RD-80` ABERTO** (unico arquivo
nao classificado de 253, gatilho disparado cinco vezes), com `autoridade: nenhuma` — a
unica propriedade que um contrato nao pode ter.
